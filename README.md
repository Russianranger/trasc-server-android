# TRASC Server for Android

Standalone Android management app for the Triptych server. Initial implementation is being added with runtime packaging, source/maps/database imports, server controls and on-device builds.
