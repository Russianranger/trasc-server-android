# Active build: 0.6.1 in-app Microsoft toolchain (2026-09-24 UTC)

User explicitly authorized implementing the Microsoft toolchain download button in
the next APK. Base main `4885ad0b9f9d5e96b9fd779dc16e2bce462e0423`, branch
`codex/in-app-microsoft-toolchain`, version **0.6.1/code50**. Retain app identity and
signing certificate; publish via the existing preview channel. Named Beta 0.6 stays
unchanged. No server/fork, boat, era or map-import changes are part of this build.

Implementation adds a catalog-bound Microsoft license review and explicit acceptance,
then a queued download, checksum verification, private MSI extractor preparation,
SDK packaging and import. The pinned open-source downloader and existing portable
packer are reused. Linux package dependencies are downloaded/extracted privately,
not installed/upgraded. Progress, cancellation and verified-download retry use the
existing runtime/job machinery. Staging/caches live under work/cache (excluded from
session backup); installed compiler remains backed up. Native launch guards prevent
compilation/game launch during replacement, failed activation rolls back, and a
runtime restart recovers an interrupted compiler swap. Manual ZIP import remains.
Compile and Deploy stay separate. See toolchain-without-windows.md for device steps.

Latest private Fold6 log bundle `logs-6060790551431168012.zip` identifies 0.6 on
SM-F956U1/SDK36. Runtime, source/database import, maps import, server build/deploy/start
and three dynamic zone workers are confirmed. Client runtime was not installed and
no named-zone map load/player login was recorded. Map import failures were three
missing-argument `'file'` errors, three rejected archive layouts, one unsupported
archive and a timeout before a successful retry from grumpy-gaming/eqemu-maps. Those
input-validation/archive-recognition issues are not fixed in this pass; fork ownership
is not required. Original EACCES logs predate the successful update. Raw logs stay private.

Catalog verification found that Microsoft's mutable VS2019 channel advertises a size/hash different from the actual official catalog response; independent GitHub ARM64 CI reproduced this. The downloader now pins the reviewed official VS2019 `16.11.60+37627.13` catalog directly: **11,154,648 bytes**, SHA256 `406969c30f4eb8bf0075a0850e339340ac83942705b76269156bb5b70f01b631`. Catalog bytes/version and every selected payload SHA256 remain strictly verified; no checksum bypass or trust-on-first-use fallback was added. The selected 84 packages/324 payloads have an estimated total of 973,531,982 bytes. Live downloads showed that catalog payload sizes can differ from actual, checksum-matching bytes, so payload sizes are estimates with per-file and cumulative actual-byte limits; the catalog pin itself retains its exact size check. Metadata/consent tokens are tied to the shipped pin. The first PR CI failure is this catalog issue, not a package-extractor failure: the private ARM64 msiextract ran successfully and installed-package inventory stayed unchanged.

Development uses backend/UI/review subagents. Local regression, live dependency/catalog
checks and signed build/release verification are in progress; this entry is not a
published APK claim. Append final source/build/artifact identity when publication is
verified. Preserve prior working Thor compiler, client and server, accepted Qeynos–Erudin
travel/log cleanup, and deferred OOT/Overthere/Traditional/era/backup-provider work.

---

# Released: Beta 0.6 runtime installation repair (2026-09-24 UTC)

**Repair and publication are complete; Fold6 / Android 16 device acceptance is pending.** [PR #11](https://github.com/Russianranger/trasc-server-android/pull/11) merged at `cbb634d18f67004acf2a8566073c7b66eb3e3961`. [Beta version 0.6](https://github.com/Russianranger/trasc-server-android/releases/tag/v0.6) is published as a regular, non-draft GitHub release and is **Latest**. [Download the signed APK](https://github.com/Russianranger/trasc-server-android/releases/download/v0.6/trasc-server-android-beta-0.6.apk). Tag `v0.6` points to the verified merge. App/Android version is **0.6/code49**, retaining package `io.github.russianranger.trasc.preview` and the existing signing certificate. Official Beta 0.5 (internally 0.4.23/code40) and all later previews can update in place; `v0.5` remains available unchanged.

**Delivered:** both online and offline server/client runtime installers now expand hard-link members into normal file copies. Executable/data modes and guest symlinks are preserved, expanded bytes count toward storage/size limits, and invalid paths/targets or attempts to replace an existing file are rejected. No runtime package or archive-format change was needed. This fixes the confirmed extraction operation in the supplied logs; their earlier isolated connection-abort error is separate. It does not claim complete Android 16 gameplay compatibility or resolve the earlier backup-provider/renderer-loss issue.

**Verification:** the restrictive host adapter denies every hard-link call. The previous extractor reproduced `link failed: EACCES` with the actual published server archive; the repaired extractor passed targeted mode/content/chain/empty-file/symlink/path/limit tests. Both published archives passed complete extraction and session backup/restore with every indexed file hash verified: server runtime 1.1, SHA256 `2acbaeedec4355a3a7dd77e7dff83528895b61775681b8d75cdb34434a52b4b6`, six hard links; client runtime 1.0 from the preceding release, SHA256 `108a1b45cf700b989abaa784daef2eb309ab4d31a2e9eb113dde5bb9c5c3b058`, 45 hard links. The existing runtime CI checks also use the restrictive adapter. Android 16's libcore source was checked for support of the guarded file-read option used by the copy path. Publisher checks validated artifact matching, draft ownership, refusal to publish corrupted bytes, and preservation of an already-published named release.

Final PR head `8114a117e307d30f38ac0b529898e88c6069b11d` passed all seven [PR build jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/36046835997) and [native server verification](https://github.com/Russianranger/trasc-server-android/actions/runs/36046835553). All eight [signed main build/release jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/36047024585) passed, including backend/host checks, all six browser suites, Android build/lint/signing, database import/migration/restore, native server/DLL builds and direct/PRoot software, Vulkan/DXVK and VirGL rendering/input/runtime-session checks. No release gate was waived. Host restriction tests and Linux ARM64 runtime checks do not substitute for the affected user's physical-device acceptance.

**Published identity:** APK 15,189,399 bytes; SHA256 `1706e8c10fd5b53f5d0f564e9c3c26e453c7d828677f71b06caad30da54b1e9b`. Local `apksigner` verified the full signature and certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`; `aapt2` verified actual version 0.6/code49/package. All 71 packaged backend/UI assets match the merge. Main artifact `10829840544`, ZIP SHA256 `8c0cc07bd0018935ef3be5b4c44ed522045fb2d3410906c16f9866bddab7c098`. Public `launcher-sources.tar.gz` SHA256 `d3ae2358ea7302bd0e9ee631357244a3744c5c8e5387fcdcba8325cf1fb62098`; public `beta-build.json` SHA256 `bdc343f01763a6dbf7c2d168eae9553798a3a9b00cd3ec1855e694313297298b`. Public APK and native-source archive match the signed main artifact byte for byte; the public manifest identifies the same commit and build run. Verification evidence is under ignored `runtime-work/060`.

**Next device check:** stop any running client/runtimes and install Beta 0.6 over the app without uninstalling or clearing storage. On the affected fresh Fold6 setup, keep **TRASC Custom** selected and try **Setup → Download runtime** once (or **Choose runtime .tar.gz** with an existing intact official archive). Confirm **Runtime ready**. If needed, install the separate client runtime from its matching Client panel. If a new failure appears, retain the displayed error/last action and export Logs rather than repeatedly reinstalling. Existing working setups need no runtime reinstall, database/client reimport, server/DLL rebuild or repeat ferry test for this repair. Full instructions and offline links: [beta-06-release.md](beta-06-release.md).

Traditional profiles remain preparation-only pending the chosen fork's Android build/deploy milestone; era presets and Ocean of Tears/Overthere work remain deferred. Qeynos–Erudin travel and log reset remain accepted. Preserve the successful external backup and existing re-export workaround. Raw user logs were not committed. No subagents. This release-record update is documentation-only `[skip ci]`; the published APK remains the verified merge above.

---

# Active repair: Beta 0.6 runtime extraction (2026-09-24 UTC)

The user explicitly authorized repairing the runtime installation failure and publishing a named **Beta 0.6** release. This supersedes the preceding question-only restriction. Base is `bbad628eb106b9660349ed2c6bb4179873e9adc9`; branch `codex/beta-06-runtime-install`; app version 0.6/code49. Preserve the existing package/signing key and publish `v0.6` as a regular, Latest GitHub release, retaining `v0.5` and the preview channel.

**Confirmed evidence:** the affected user reports Galaxy Z Fold6 / Android 16 / official Beta 0.5 (internally 0.4.23/code40). Private `app.log` contains one connection-abort download failure at 11:51 UTC and six later `runtime_install` failures at `TarExtractor.extract:58` → `Os.link` → `EACCES`, from 17:51 through 18:55 UTC on September 24. The 0.5 code reaches that extraction only after download and SHA256 validation. Private `android-exit.log`, collected at 19:00 UTC, contains `exits: []`; it supplies no recorded process-crash evidence. The logs establish denied hard-link creation, not the exact SELinux audit rule. Android's app policy prohibits hard links; this is consistent with the failure. Source verified in both v0.5 and current main. Raw logs remain uncommitted.

**Repair:** the common server/client extractor materializes archive hard links as separate regular files. Source permission bits determine executable mode, avoiding `canExecute()` because Android can deny direct execution of guest files even with mode 0755. Copies count toward the total unpacked-byte cap and available-space reserve; targets must be regular files, destinations cannot already exist, paths remain confined, and guest symlinks are restored separately without changing their targets. Runtime packages/archives need no repack. Normal staging/activation, data preservation and profiles remain unchanged.

**Regression coverage:** the host Android adapter now denies `Os.link` instead of allowing Linux hard links, so existing real server/client archive and PRoot checks exercise this restriction. New targeted coverage checks independent copy bytes, executable and data modes, empty files, hard-link chains, relative/absolute guest symlinks, invalid/directory/missing/duplicate targets, traversal and expanded-byte limits. Local host suite passes. Full real-archive, negative-control and CI/release verification are pending; do not treat this entry as published or device-accepted.

The release workflow publishes the signed main artifact as `trasc-server-android-beta-0.6.apk` with corresponding launcher sources and `beta-build.json` after every existing release gate. The named published release is immutable; a retry may resume only a draft for the same commit. See [Beta 0.6 notes](beta-06-release.md) for Fold6 instructions and [profile guide](traditional-profiles.md) for the still-pending Traditional compilation milestone. Do not start era/fork/boat-route work or the separate backup-provider resilience repair in this pass. No subagents.

---

# Released: 0.5.8 traditional world profiles (2026-09-21 UTC)

**Implementation and release are complete; Thor acceptance of the new profile flows is pending.** [PR #10](https://github.com/Russianranger/trasc-server-android/pull/10) was merged at `54bb4e0c5f5a53baddb0417ce71e0f968958f4dc`. The signed **0.5.8/code48** preview is published: [Download APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=54bb4e0). The public APK, source archive, build manifest and preview tag were verified against that merge and the signed main artifact.

**Delivered:** a persistent switch between TRASC Custom and Traditional EQEmu, independent world/client/runtime/settings/backup homes, all ten tabs in both profiles and ten unique generated 16-bit adventure scenes in Traditional. Existing Custom paths and data remain in place. Native operation leases and stale-request checks prevent switching during runtime/client/display/picker/import/export activity. Profile identity is checked on complete-session restore; legacy unmarked sessions belong to Custom. Traditional content preparation includes quests, Perl plugins, Lua modules and server assets, with explicit replacement, retained previous directories and interrupted-swap recovery; ordered split PEQ SQL seed imports are supported. Maps, source and separate clean ROF2 imports use the selected profile. Custom ferry/spell/map/DLL modifications stay in Custom. Exact artwork paths and built-in image-generation prompts: [tab-artwork-058.json](tab-artwork-058.json).

**Scope boundary:** Traditional is a preparation workspace, not yet a playable upstream Android server. Traditional Build, Deploy, binary rollback, Start and generated client-data preparation/export remain blocked in both UI and backend until the chosen fork's compilation/deployment adapter is implemented and verified. No fork was modified, no era preset was applied and no Traditional login is claimed. The exact intended fork URL/ref still needs identification; EQEmu/master is only an editable default. The [profile guide](traditional-profiles.md) records all additional components and the next implementation order: pin fork/content/client and recursive submodules; audit ARM64 Debian/PRoot C++20/CMake/vcpkg, Perl/LuaJIT and native dependencies; compile/test targets; wire local login, configuration/opcodes, shared memory and matching client exports; then verify on Thor. Do not remove readiness guards to bypass this work. Era presets follow that milestone.

**Update and device check:**

1. Camp out, stop the client and both runtimes, and install the APK over the existing app. Do not uninstall or clear app storage. Custom requires no server/DLL rebuild, runtime download, client reimport or ferry reinstall.
2. Confirm Custom is initially selected and the existing settings/files remain. With runtimes and file pickers closed, choose **Traditional EQEmu → Switch world** above the runtime panel. Confirm the selected-world label, ten classic scenes/tabs and separate empty setup. Selection should survive reopening the app. The switch should remain unavailable while a runtime, client display or transfer is active.
3. If preparing content now, install the server runtime within Traditional, import the chosen source/database/maps, then use **Setup → Traditional world content** for quests/plugins/Lua/assets. The supplied ProjectEQ repository can be imported separately for quests, plugins and Lua modules. Use a separate clean ROF2 import in Client. The checklist means imported/present, not proven compatible. Build/Start staying disabled is expected in this release.
4. Stop Traditional's runtimes, return to **TRASC Custom → Switch world**, and confirm the original setup starts normally. No repeat of the accepted ferry route or full backup export is required. If selection/import fails, retain the error and export the selected profile's logs.

**Verification:** all 172 Python tests pass with Lua enabled, the separate eight ferry protocol tests pass, host JVM/C/C++ management tests pass, and all six browser suites pass (profiles, session, management, Spire, boats and ferry). Profile tests cover path/selection/operation/backup isolation, stale requests, component replacement and recovery, split database imports, all tabs/artwork and switching back to Custom. Phone/Thor-sized screenshots were inspected, including 1280×720, 854×480, 393×852 and 360×740, with no horizontal overflow. Final PR head `865c3eefc938cbcfc34997ce8cf8abac733b4122` passed all seven [PR build jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/35555670017) and [native server verification](https://github.com/Russianranger/trasc-server-android/actions/runs/35555669863). All eight [main release jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/35556063729) passed, including APK/lint/signing, database migration/rollback/restore, native server/DLL compilation and direct/PRoot software, Vulkan/DXVK and VirGL rendering/input/session checks. No release gate was waived. These checks do not replace actual Thor acceptance of the new profiles.

**Published identity:** APK 15,188,355 bytes; SHA256 `fe7e2e19825e8956d8183505b923b888e085b2268260758a04ef949eea5bcf0a`; package `io.github.russianranger.trasc.preview`; actual manifest 0.5.8/code48. Local Android `apksigner` verified the full APK signature and preserved certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`; `aapt2` verified the actual manifest. All 71 packaged backend/UI assets match the merge byte for byte. Main artifact `10620976826` ZIP SHA256 `dfd7c336455585bcac1e5d2bb4bec6ea527eec46adcf9726055f74255765990d`. Source archive SHA256 `fed065004c4ee2d536a776fee85d0f1e7159dfaf2e69e7b9b9126ecbbfd1514d`; public build manifest SHA256 `2247d7c6c00bcd022a93c9b87e749fb67f35487a3012fd3e0df8baeeb779825f`. Public APK and source archive match the main artifact exactly. Evidence remains under ignored `runtime-work/058` and `ui-reports`.

**Preserve closed/deferred work:** Qeynos–Erudin round-trip travel and log reset remain accepted and complete. Ocean of Tears/Freeport–Butcherblock transit, Overthere barge repairs and era presets remain shelved. Preserve the successful external backup and the existing re-export workaround; Downloads-provider dependency death and management-renderer loss remain unresolved. No raw user logs were committed. No subagents were used. This release record is documentation-only `[skip ci]`; the APK remains the verified merge above.

---

# Active milestone: 0.5.8 traditional world profiles (2026-09-21 UTC)

**The user's latest request supersedes the earlier instruction to defer profile implementation.** They authorized starting the clean traditional EQEmu profile, a switch between worlds, all shared tabs, 16-bit adventure backgrounds and quest/plugin imports; the next milestone is making their chosen fork Android-ready. Era presets, Ocean of Tears transit and Overthere barge repairs remain deferred. No upstream fork or era preset was changed.

Implemented on `codex/traditional-profiles`: fixed independent profile homes preserving existing Custom paths; persistent native selection; operation leases/stale-request rejection; runtime/client/display/picker guards; profile-specific complete-session manifests and cross-profile restore rejection; clean Traditional client options; separate quests/plugins/Lua/assets imports with retained replacements and interrupted-swap recovery; ordered split PEQ seed bundles; ten generated classic backgrounds with the same ten tabs. Traditional compilation/deployment/start/client-data exports are blocked until the next milestone. Existing Custom adapters/repairs remain in their profile.

See [traditional-profiles.md](traditional-profiles.md) for the full dependency table, storage/backup behavior and implementation order. The exact traditional fork URL/ref still needs identification before the next milestone; upstream EQEmu/master is only an editable default. Its CMake/vcpkg/LuaJIT/submodules differ from the existing Custom Lua 5.1 build recipe. Do not enable build/start by removing guards without implementing and verifying the traditional adapter.

Local verification so far: 172 Python tests pass with Lua enabled; host JVM/C/C++ management tests pass, including profile path/persistence/operation/backup isolation. Profile browser coverage passes for ten tabs/scenes, clean DLL settings, scoped content imports and switching back to Custom. Existing browser suites and signed APK/release gates are being completed; this entry is not a release claim. App version is 0.5.8/code48. Artwork paths and exact built-in prompts: [tab-artwork-058.json](tab-artwork-058.json). No subagents.

Preserve the earlier accepted ferry and log-reset milestones. The successful session re-export is user-confirmed; Downloads-provider dependency death and renderer-loss resilience remain unresolved, and this release does not claim to fix them. No repeated backup or ferry test is requested. After publication, append the exact commit, CI and APK identity plus concise profile-switch/import/back-to-Custom checks.

---

# Diagnostic review: session-save provider failure and renderer kill (2026-09-20 UTC)

Reviewed private `logs-5737403697047618156.zip`, exported at 23:35:59 UTC from 0.5.7 on Android 13 / AYN Thor. The diagnostic ZIP contains 148 entries and passes its CRC check. Raw logs remain private and uncommitted. This review narrows the preceding user report: there are **two distinct failure events**, including an actual app-process exit before the recoverable management-screen loss.

**Timeline (UTC):**
- 20:50:33: `prepare_session_backup` starts; server shutdown records `errors=[]`.
- 20:52:26: backup preparation completes. MariaDB logs Normal shutdown and Shutdown complete. In source, successful preparation requires the SQL snapshot to finish when a database is imported, and MariaDB to exit cleanly.
- 20:52:27: runtime shutdown completes. A `ferry_diagnostics` socket error occurs because the snapshot hook runs after backup preparation has already stopped MariaDB. The hook catches/logs the failure, and shutdown continues. This diagnostic-ordering issue is not evidence that the SQL snapshot failed.
- 21:07:25.148: Android records the TRASC app process exiting with reason 12, `REASON_DEPENDENCY_DIED`: it depended on `com.android.providers.downloads/.DownloadStorageProvider` in dying `android.process.media`. This establishes that Android's Downloads storage-provider failure caused this app termination; the provider's own reason for dying is absent. It does not identify a particular destination path or prove a filesystem, archive-size or memory limit.
- 21:28:07: the control service restarts, with the game server stopped.
- 21:29:29.290: `management_display` records `WebView renderer exited: crashed=false priority=2`. Android's documented meaning of `didCrash=false` is that the system killed the renderer. This matches the in-app tap-to-reopen recovery message. Memory pressure is possible, not established by these records. Do not conflate this with the earlier Downloads-provider death.
- 22:04:30: runtime stops with `errors=[]`; the diagnostic snapshot again finds no MariaDB socket. At 22:04:38 Android records a separate reason-10/user-requested-class app stop. The reason-13 “isolated not needed” WebView records accompany process cleanup and are not independent proof of a WebView crash.

**Evidence limits:** `android-crash.log` is empty and no `save_export` exception is recorded. App-level exception handling cannot by itself prevent a dependency-death process termination. The status export shows about 168.6 GB free internal storage at collection time; that is not a measurement of free space or RAM at either earlier failure. The complete-session ZIP is not in this diagnostic bundle: retain the user's report that the second export produced a valid backup, without claiming an independent full-backup checksum or restore test. No repeat export/restore is requested.

**Next repair scope:** investigate export handling that survives storage-provider loss, preserve transfer/result state independently of the WebView, and recover the management screen without losing access to the retained backup. Avoid the redundant ferry snapshot once MariaDB has already shut down. A targeted fix should verify provider death, renderer loss and re-export of the existing ZIP without recreating the archive or touching world/client data. No product changes or new APK were made in this diagnostic review. Do not recommend database reimport, runtime reinstall, DLL rebuild, or repeating the accepted boat tests.

Android references checked: [ApplicationExitInfo dependency death](https://developer.android.com/reference/android/app/ApplicationExitInfo#REASON_DEPENDENCY_DIED) and [RenderProcessGoneDetail.didCrash](https://developer.android.com/reference/android/webkit/RenderProcessGoneDetail#didCrash()). Published 0.5.7 remains merge `9b7d54e160a5a414b9d9145ad28a15ac19c546d5`. Documentation-only `[skip ci]`; era/profile implementation and Ocean of Tears/Overthere work remain deferred. No subagents.

---

# Device follow-up: complete-session export workaround confirmed (2026-09-20 UTC)

Following the 0.5.7 release, the user reported that saving a complete-session backup through Android Files appeared to crash. The suggested workaround was to reopen the app, start only the server runtime, open Files → `exports`, select the existing `session-*.zip`, and export it again, trying internal Download as the destination. Do not assume which destination the user actually chose.

**Latest user result:** the workaround worked; the user saw an in-app message approximately “management window is closed. Tap to reopen,” and the second export produced a valid backup. Accept the retry/export workaround as user-confirmed. The user has not supplied this archive for independent checksum/ZIP verification or reported restoring it, so this is not a newly verified full restore test.

**Source-supported interpretation:** MainActivity's `onRenderProcessGone` handler logs `management_display` with the renderer's `crashed` flag and exit priority, replaces the WebView with “Management screen stopped. Tap to reopen.” and permits recreating the management screen. The observed recovery message identifies loss of the management WebView renderer; it does not establish why the renderer exited or that the whole Android app process crashed. Memory pressure, a renderer crash and their relationship to the export remain unproven without fresh diagnostics. Do not label the underlying failure fixed.

The completed session ZIP is retained under the app workspace's `exports/` before the Android save picker opens. Re-export that existing ZIP rather than repeatedly creating complete backups. Preserve the user's successful external copy and the existing app data. If further diagnosis is requested or the issue recurs, collect Logs → Export log bundle after reopening, especially `management_display`/`save_export` records and Android exit diagnostics. Do not require repeating a failed full backup just to reproduce this observation.

This update records device feedback only, with `[skip ci]`; no product changes, new APK, server/DLL rebuild, restore operation or subagents. Published 0.5.7 remains merge `9b7d54e160a5a414b9d9145ad28a15ac19c546d5`. Era/profile work and the Ocean of Tears/Overthere follow-ups remain deferred; accepted ferry/log-reset milestones remain closed.

---

# Released: 0.5.7 Fixes, artwork and shared session controls (2026-09-20 UTC)

[PR #9](https://github.com/Russianranger/trasc-server-android/pull/9) was reviewed and merged at `9b7d54e160a5a414b9d9145ad28a15ac19c546d5`. The **signed 0.5.7/code47 preview is published**. [Download APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=9b7d54e). The public APK, source archive, build manifest and preview tag match the verified release. Implementation is complete; the new UI/status behavior awaits the user's Thor check.

**Delivered:** a designated Fixes tab now contains Qeynos–Erudin ferry management, Nektulos maps, spell compatibility and Wine prefix recovery. Spire retains content browsing/editing. The retired Erud's Crossing trial is cleanup-only: status, preview removal and save reviewed removal, with existing ownership/backup safeguards. Its old install/reset controls are no longer offered. Each of the ten tabs has a distinct offline fantasy scene; seven new illustrations preserve the green/gold high-fantasy/adventure theme. Paths and exact built-in image-generation prompts are in [tab-artwork-057.json](tab-artwork-057.json).

The shared runtime panel now provides Start server, Stop server and Restart beside the runtime controls, plus independent top-right server/client indicators on every tab. Controls respect runtime/database/binary/process/job state. Status failures clear stale running claims. Client status distinguishes a running game, open Wine runtime, desktop, compilation and lifecycle transitions. “Running in background” requires a recent positive observation of a live eqgame.exe in the current launch, excluding zombies; merely leaving Wine open is insufficient. The existing bounded sampler observes the process about every ten seconds; management polls about every 2.5 seconds. Read-only status requests time out after ten seconds, clear pending replies and cannot overlap indefinitely. Long operations retain their existing behavior.

**Update and device check:** camp out, stop the client/server/runtime and install over the existing app. Keep the installed server, client DLL, runtime, client import and working route. **No server/DLL rebuild, runtime download, client reimport or ferry reinstall is required.** Start the runtime and server from the same top panel, check the Running status and startup control gating, then launch RoF2. Return to management with Android Back and visit another tab; allow the next process observation for Running in background. Return through Client → Return to client. Review the artwork/Fixes layout. After camping and stopping the client, stop the server from the top panel; the runtime should remain available for editing. Export Logs if a control/status fails. No repeat of the accepted ferry journey is requested; Refresh route may be used for a read-only check.

**Verification:** all 163 Python tests, host JVM/C/C++ checks and all five browser suites passed locally. Browser coverage includes shared lifecycle controls, background-client status with the server runtime closed, missing/stale process observations, native/backend failure recovery, retired-trial cleanup and existing management/content workflows. Thor/phone screenshots were inspected, including 1280×720, 854×480, 393×852 and 360×740 layouts. Final PR head `8b6a0aa96f933f7862464b6a26c0c14d0d2c5660` passed [content/database/browser verification](https://github.com/Russianranger/trasc-server-android/actions/runs/35532080730), [native server verification](https://github.com/Russianranger/trasc-server-android/actions/runs/35532082413) and all seven [PR build jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/35532082569). All eight [main release jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/35533450140) passed, including APK/lint/signing, database migration/rollback/restoration, Microsoft DLL/native server compilation and direct/PRoot Software, DXVK and VirGL rendering/input/session checks. No gate was waived. Browser/runtime fixtures do not constitute actual Thor acceptance of the new UI.

**Published identity:** APK 12,111,982 bytes; SHA256 `5f85c6ddac49bcde800212bea1db74e77c25845341fcd2a6c478885d444e56b8`; package `io.github.russianranger.trasc.preview`; actual manifest version 0.5.7/code47. The actual APK v2 certificate matches the preserved `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`; CI apksigner verified the full signature. All 59 packaged backend/UI assets match the merge. Main artifact `10611879098` ZIP SHA256 `a22d234496fbb631e09afcc04f92d12d5d112308e2f67c837932bbbd4f2d6968`. Source archive SHA256 `573bd5aaa33d3b3b9cfc2809c4ccbc3f6c6395a0804555de9bdf5748e47c9f2d`; build manifest SHA256 `47bfdb7182a5be3e596832a0003d62071b6e1ca83ea9dd38f26a7f569e394125`. Public downloads were compared against the main build artifact; preview points to the merge above. Local evidence is under ignored `runtime-work/057` and `ui-reports`.

**Deferred scope:** the user explicitly requested recording the proposed traditional EQEmu/era implementation order without starting it: (1) isolated world profiles preserving the custom world; (2) standard EQEmu with a separate clean RoF2 client proven on Thor; (3) one tested Luclin preset; (4) classic presentation, additional eras and storage optimization. A clean client remains a recommendation, not a completed selection. No era/profile implementation was started. Freeport–Ocean of Tears–Butcherblock transit and Overthere barge repairs remain shelved. Qeynos–Erudin round-trip travel and log reset remain accepted and complete, with the previously recorded 48-hour-cleanup evidence limit. Preserve all other closed milestones and saved settings. No subagents were used. This release-record update is documentation-only `[skip ci]`; the binary remains the verified merge above.

---

# Active scope: Fixes, artwork and shared session controls (2026-09-20 UTC)

The user authorized app UI work: retire obsolete ferry test controls from Spire, place repairs in a designated Fixes section, give each tab distinct high-fantasy/adventure artwork, and add server start/stop controls alongside the runtime controls with top-right server/client activity status. Preserve the accepted ferry route and installed server/DLL, saved settings and all closed milestones. No new boat-route work is authorized.

**Traditional EQEmu / era profiles are deferred. Do not implement them yet.** Record the proposed implementation order for a later request:

1. Add isolated world profiles while preserving the current working custom setup.
2. Prove standard EQEmu with a separate clean RoF2 client on the Thor.
3. Build one tested Luclin preset, coordinating rules, content and matching client data.
4. Add classic presentation, then additional eras and storage optimizations.

The clean-client approach remains a recommendation, not a completed user selection. Freeport–Ocean of Tears–Butcherblock transit review and Overthere barge repairs remain shelved. Qeynos–Erudin round-trip travel and diagnostic log reset are accepted and complete.

Development branch: codex/fixes-and-runtime-ui; candidate 0.5.7/code47. Release and verification status will be recorded above this entry when complete. No subagents.

---

# Current scope: ferry milestone complete; traditional EQEmu planning only (2026-09-20 UTC)

The user reconfirmed acceptance after the latest log review. **Qeynos–Erud's Crossing–Erudin round-trip travel and diagnostic log reset are complete.** Retain the evidence and limits in the acceptance entry below. Keep the released 0.5.6 APK, deployed server and client DLL; no rebuild or repeat of the accepted tests is needed.

**Shelved boat follow-ups (explicit user request):**

- Review **Freeport ↔ Ocean of Tears ↔ Butcherblock Mountains** transit.
- Investigate and fix **the Overthere barge** issues.
- Both are parked for a later implementation request. Do not begin these routes as part of the era-profile planning work.

**Active discussion — planning only:** the user wants the app to support a traditional EQEmu server with era-appropriate rules/content while retaining RoF2, alongside the existing custom world. They have not selected a clean versus modified client or authorized implementation. Earlier goals include a traditional Luclin-era experience and classic presentation. Do not treat the following recommendations as approved product decisions.

**Proposed direction:**

- Add isolated world profiles: existing TRASC/custom and a traditional EQEmu/PEQ world. Scope server source/build/deployment, database and characters, quests/maps, settings, client imports/Wine prefix, generated client files, backups and logs to the selected profile. Run one world at a time. Preserve the current working profile during migration.
- Prefer a separate clean, supported RoF2 import for the traditional profile initially. Keep the existing custom client intact. Reuse proven Android runtime/graphics infrastructure where compatible; audit and port controller/camera/compatibility DLL changes separately from custom gameplay and content assumptions. A copy of the modified client is a fallback baseline with more unknowns. Shared immutable client assets plus per-profile overlays are a later storage optimization, not a prerequisite.
- Establish a pinned upstream EQEmu server + PEQ database + matching quests/maps baseline on the Thor before adding era presets. The app currently has one server workspace/client root and custom-source build/start hooks; conventional EQEmu requires a profile-aware build/import adapter and verification of its scripting dependencies. Do not automatically apply custom source/ferry patches to this new flavor.
- An era preset should version its rules, expansion/content flags, database/quest corrections, client exports, and optional RoF2-compatible UI/model/zone assets together. Keep historical settings separate from optional solo/quality-of-life settings. Start with one tested Luclin preset before broad Classic/Kunark/Velious/Luclin selection.
- Expansion filtering is available upstream, but historical completeness depends on content tagging and quest behavior. Rules cannot restore old client zone geometry, icons/UI or every historical mechanic. Client and server zone/map/door/spawn data must agree. Generate spells_us.txt, dbstr_us.txt, SkillCaps.txt and BaseData.txt from the selected profile rather than copying custom-world exports.
- Proposed flow: choose world → choose era/options → review changes → snapshot/apply with client and server stopped → launch the matching world/client. Normal profile switches should select already prepared files, without rebuilding the server or recopying the entire client each time. Changing an established world's era backward needs a snapshot/clone and compatibility checks for characters, locations, items and AAs; do not silently rewrite progress.

**Upstream references checked:** [EQEmu source](https://github.com/EQEmu/EQEmu), [server rules](https://docs.eqemu.dev/server/operation/server-rules/), [expansion/content filtering](https://docs.eqemu.dev/server/expansions/expansion-content-filtering/), [client/content design considerations](https://docs.eqemu.dev/server/expansions/design-considerations/), and [supported clients](https://docs.eqemu.dev/play/play-guide/). Current source has Expansion:CurrentExpansion and Expansion:UseCurrentExpansionAAOnly; World:UseClientBasedExpansionSettings defaults true and overrides World:ExpansionSettings, so the era implementation must explicitly coordinate server content and client expansion settings. The design-considerations page includes proposals and open questions, not proof that every historical feature is implemented.

This update changes documentation only, with [skip ci]. No product implementation, APK/server/DLL build, device change or subagents.

---

# Device acceptance: 0.5.6 round-trip ferry and log reset (2026-09-20 UTC)

The user reports both features working. Reviewed private bundle `logs-6927299419959095253.zip`, exported at 17:31:31 UTC from 0.5.6 on the Thor. **Accept the revised Erudin departure and passenger return to South Qeynos, completing the Qeynos–Erudin round-trip milestone. Accept diagnostic log reset.** Keep the current APK, deployed server and client DLL. No new build or repeat of these accepted tests is needed.

**Boat evidence:** the backed-up route update completed at 16:34:09 UTC before server startup. All five logged boundaries transferred one passenger and completed with `failed riders=0`: Qeynos → Crossing (sequence 1), Crossing → Erudin (2), Erudin → Crossing (3), Crossing → Qeynos (4), then Qeynos → Crossing again (5). The Erudin controller traversed all revised departure waypoints and still transferred its rider at 17:01:58; the return crossing into Qeynos began at 17:14:55. Together with the user's successful test report, this closes the previously unaccepted Erudin departure/return behavior. The fresh native `ferry-state.json` confirms a Qeynos dock pause from 17:17:16 to 17:20:16 (180 seconds), plus the subsequent island pause from 17:27:39 to 17:28:39 (60 seconds).

**Client evidence and limits:** the bounded client boat logs retain only the later Crossing leg, not Erudin. They contain 706 consecutive attached samples, followed by detachment with movement input near the island, a stable dry position and normal camping/logout at 17:30:30–17:30:40. There is no recorded repeat of the earlier passive Erudin rock detachment/death. Do not invent an uninterrupted full-trip client trace from this limited tail. Skiff suppression and configured speed 0.60 remain the applied release behavior; this export does not contain a separate NPC census or an independent speed measurement. The user's working result is accepted without demanding another unchanged test.

**Log cleanup evidence:** 89 exported files are zero bytes; 87 were nonempty in the preceding supplied 0.5.5 bundle. Fresh session logs are being written, and all 17 exported JSON records remain nonempty and parse successfully, including DLL build/deploy metadata and the new ferry-state snapshot. This substantiates reset-to-zero plus continued logging. The bundle does not identify which cleanup button was pressed or independently demonstrate the 48-hour cutoff, so do not claim both cleanup modes were separately exercised. The client stopped normally and the server runtime remained available during export.

**Next scope:** this route and log-reset milestone are closed. Other ferry routes remain deferred until the next implementation request; preserve all accepted client, controller, camera, loading, particle and player-restoration milestones. This is a documentation-only acceptance update with `[skip ci]`; no product/source/runtime changes, new APK, or subagents. Raw device logs remain private and uncommitted. Published 0.5.6 remains merge `e7585ee6e3627fe814edf7af3cca363372a46b0a`.

---

# Released: 0.5.6 Erudin departure, timing and log cleanup (2026-09-20 UTC)

[PR #8](https://github.com/Russianranger/trasc-server-android/pull/8) was reviewed and merged at `e7585ee6e3627fe814edf7af3cca363372a46b0a`. The **signed 0.5.6/code46 preview is published**. [Download APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=e7585ee). All release gates passed; the public download has been verified against the signed build artifact.

**Update:** park on shore and stop the client, game server and runtime. Install over the existing app, then open the existing runtime with the game server stopped. In **Spire → Qeynos–Erudin ferry**, select **Preview route update → Save reviewed route change**, then start the server. A full database backup and quest journal are created first; route IDs and interrupted-rider recovery tickets are retained. The service restarts at South Qeynos. Keep the server built with 0.5.5 and the current client DLL: no server/DLL rebuild is required for this update. The earlier one-time passenger-transfer patch is bundled with the app and applied to its local imported server source during Build; the upstream source repository was not modified.

**Changes:** Erudin departure moves southwest and then west offshore, avoiding the old rocky-shore leg. South Qeynos/Erudin stops are 180s; Erud’s Crossing remains 60s; configured ferry speed is 0.60 with existing movement modes retained. Erudin version-zero race73 skiff-only spawns are disabled, with their original instance-zero disabled records retained and restored on service removal. Saved Erudin skiff state is cleared so old copies cannot return. Other-zone NPCs/skiffs and unrelated disabled rows are preserved. The APK alone does not update an installed route: the new Spire update action is required.

**Logs:** stop client and runtime, then use **Logs → Clear diagnostic logs → Clear files older than 2 days** (whole diagnostic text files last modified more than 48h ago), or **Reset all diagnostic logs** (truncate diagnostic text files to 0 bytes). This does not trim dated lines from recently modified logs. Settings, JSON build/deployment records, chat, backups and exports are preserved; later activity writes fresh logs. Native export and orderly runtime shutdown now request a bounded ferry-state snapshot, so an offline export can retain the last captured route state. Export evidence before clearing it.

**Verification:** all 163 Python tests, all 8 Lua protocol scenarios, JVM checks and pinned Erudin base-terrain geometry verification passed locally. The old route and observed detachment intersect the rocky shore; the new centerline clears base terrain by 75.156 units including berth and over 150 after its first departure waypoint. This is not full hull/placed-object or device collision acceptance. Final head `4091d0a9d86ef55925d77c224e12cd60df2db197` passed the [content/database/browser suite](https://github.com/Russianranger/trasc-server-android/actions/runs/35513116489), [native server verification](https://github.com/Russianranger/trasc-server-android/actions/runs/35513118229) and all seven [PR build jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/35513118321). All eight [main release jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/35514410873) passed, including APK/lint/signing, real database migration/rollback/restoration, Microsoft DLL compilation, native server compilation and direct/PRoot Software, DXVK and VirGL graphics/input/session checks. Phone/Thor screenshots reviewed; no gate waived.

**Published identity:** APK 10,590,721 bytes; SHA256 `674e9d6d37e471e50288523cacd33b5f9d2a0f72223b549e33037135fed81626`; package `io.github.russianranger.trasc.preview`; manifest version 0.5.6/code 46. The actual APK v2 certificate matches the preserved `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`, with full signature verification passed by CI apksigner. All 52 packaged backend/UI source assets match the merge; compiled DEX includes native LogCleanup. Main artifact `10605464967` ZIP SHA256 `8c9e6405ab2cdc3e92794e19363b3dc968351aa4ea6056150b8cc42681d2b889`. Public source SHA256 `217983cd8480fb2106c77713e5d9248e09b3f530519db5e639be20759424a9dd`; build manifest SHA256 `3316453c1e7f84b715268d05aa2d2a505be64ab1a654ca54c1e00175ca6e24a5`. Public APK bytes, source/manifest digests and preview tag match the verified merge. Evidence is under ignored `runtime-work/056`; private device logs remain uncommitted.

**Next device acceptance:** retain accepted boarding/carrying/turning and outbound cross-zone handoffs. Enable Boat investigation recording, confirm the Erudin skiffs are gone and the port pause is 180s, then remain aboard through the revised Erudin departure and return crossings. Export Logs before resetting/clearing. Revised physical departure and the return with a rider remain unaccepted until the next Thor result. Preserve the existing client, runtime and closed settings/milestones. No subagents used. This handoff update is documentation-only `[skip ci]`; the published binary remains the verified merge above.

---

# 0.5.6 implementation: Erudin departure, timing and log cleanup (2026-09-20)

User confirms outbound zone-border travel works, reports moving Erudin skiffs and passive detachment onto a rock while departing Erudin, followed by swimming and shark death. Requested 180-second port waits, speed 0.6, and a way to clear old logs/reset contents. This is authorized implementation; preserve accepted boarding/carrying/turning and outbound handoffs. Return travel with a rider is not yet accepted.

Bundle `logs-8797392406544205673.zip` (private, uncommitted) is 0.5.5 on AYN Thor. Erudin zone log records `handoff complete; failed riders=0 phase=3 sequence=2`; departure later transfers zero riders. In `client-boats-rollover.log`, the passive detachment occurs at ticks12418680, X=-424.711,Y=-214.898,Z=17.593 with zero motion input; preceding ship origin was X=-443,Y=-178.750,Z=-19.375, heading255.750. This is distinct from the earlier deliberate step onto and back off the Erudin pier. The collision map places rocky shoreline on the old southbound leg. Death is logged at12:45:21 UTC.

Branch `codex/ferry-erudin-log-cleanup`, version0.5.6/code46: new departure heads southwest to(-500,-40), then west via(-700,-80), avoiding the old(-443,-220) shoreline waypoint. Port waits180s, island60s, speed0.60. Preview route update migrates installed0.5.5 records/files transactionally with backup, unchanged IDs and offline recovery tickets. Erudin version-zero race73 skiff-only spawns are disabled with prior disabled rows retained for removal; their saved zone state is cleared. Other-zone skiffs remain. No native server/DLL change; retain the0.5.5 build.

Native Logs adds explicit older-than48h-by-file-mtime and reset-all-to-zero controls, requiring stopped client/runtime. JSON settings/deployment records, chat, backups and exports are excluded. Native export now requests a bounded ferry snapshot while the runtime is available; this bundle proved the previous native export omitted the Python-only snapshot.

Publication and final verification are recorded above. See preview-notes.md for the concrete update/test flow. Do not claim revised berth/departure collision is accepted before the device result. No subagents.

---

# Released: 0.5.5 Qeynos–Erudin ferry handoffs (2026-09-20 UTC)

The user authorized the Qeynos ↔ Erud’s Crossing ↔ Erudin route first, including zone boundaries and ship/passenger transfer; other routes remain deferred. [PR #7](https://github.com/Russianranger/trasc-server-android/pull/7) was reviewed and merged at `652f898b7a2fdeae124c3ff5ca27a49f3a6609a3`. The **signed 0.5.5/code45 preview is published**. [Download APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=652f898). Read [qeynos-erudin-055.md](qeynos-erudin-055.md) for the exact setup and test.

**Setup:** park the character on shore at the South Qeynos dock. Stop client/server/runtime, install over the existing app, and start the existing runtime. With the server stopped, remove the completed Erud’s Crossing trial using its preview/removal controls. Run one server **Build → Deploy build** with the existing source. This is required for the native passenger observer and managed-ship waterline patch; an APK update alone is insufficient. Keep the existing client DLL and runtime. In **Spire → Qeynos–Erudin ferry**, Refresh route → Preview installation → Save reviewed route change, then start the server.

**Device acceptance next:** enable Boat investigation recording, board `TRASC_Voyager` at South Qeynos, and stay aboard through both outbound loading screens. Confirm deck attachment, resumed travel, berth clearance and disembarking at Erudin. Ports pause 90 seconds; the island stop pauses 60 seconds. Allow **20–25 minutes outbound**: the route calculation is about 18.4 minutes before initial boarding wait, turns and variable loading. This supersedes the earlier approximate 15-minute estimate in the merge's release text. Export Logs at Erudin before testing the return, then export again after returning. A server restart returns the service to Qeynos. Do not repeat the rejected gender-2/PRE collision test or unchanged single-zone carrying tests. Physical cross-zone reattachment and the new berths remain unaccepted until the device result arrives.

**Implemented:** one owned service, three invisible controllers and static route zones, four source/destination handoffs for a round trip, actual vehicle-packet passenger observation, destination ship preparation before zoning, preserved relative passenger position/heading, wait-for-attachment and interrupted-crossing recovery. Walking off cancels transfer; nearby bystanders do not transfer. The accepted race72/gender0 model, size/flymode and deck relationship remain. Qeynos/Crossing origins remain Z=-39.5; Erudin uses Z=-19.5 for its 20-unit-higher pier. Harbor speed is 0.55; open water runs. The installer backs up, journals quest/database changes, rejects edited owned content, retains offline recovery tickets, and restores original launcher/idle settings on removal. Other routes, ordinary walking zone points, player/account data and existing client fixes remain unchanged.

**Verification:** all 163 Python tests, including eight Lua protocol scenarios, pass. Actual pinned-source client_packet.cpp and waypoints.cpp compile. Real MariaDB lifecycle, rollback, ownership, recovery retention and lost-response tests passed in [focused run 35485802194](https://github.com/Russianranger/trasc-server-android/actions/runs/35485802194), with inspected Thor/phone screenshots. All seven required [PR jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/35486127545) passed on final head `2dc5c82e783e43a56fcb38ef9c8d58ffaea92ef0`; all eight [main jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/35487191710), including publication, passed. Existing database, JVM, Android lint/signing, Microsoft DLL and direct/PRoot Software/DXVK/VirGL graphics/audio/input/restart gates were retained. No gate was waived and no subagents were used. Protocol simulations do not prove RoF2 physical attachment after loading.

**Published identity:** APK 10,587,081 bytes; SHA256 `5db0f5bb5afd16d94e0316331707b711c222ab7dda1e48ef673c0d34ea3c52f2`; package `io.github.russianranger.trasc.preview`; actual APK manifest version 0.5.5/code45. Existing certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869` passed main CI apksigner verification. Main artifact `10598392252` ZIP SHA256 `3db2d405e4a52cc72da96406772d106f7056e31631218748f7a304c9d9932a95`. All 35 backend/header/Lua assets and 17 UI assets match source; deployment names are present in compiled DEX. The public preview tag points to the merge, and published APK/source/manifest sizes and digests match the verified artifact. Source archive SHA256 `3dbf756a5e3af970a3f303c38f64e491ff52e233674f3c36ccd99e5560353820`; build manifest SHA256 `40888d40842c871b3f1de9d99f6915ace432ac6e488c427d19c6573701813b71`. Evidence is under ignored `runtime-work/055-release`; private inputs were not committed. This handoff update changes documentation only, with `[skip ci]`; the tested binary remains the merge above.

---

# Active Qeynos–Erudin implementation (2026-09-20)

User authorized resolving Qeynos ↔ Erud’s Crossing ↔ Erudin zone boundaries and ship/passenger transfers first; other routes later. Branch `codex/qeynos-erudin-ferry`, candidate 0.5.5/code45. See [qeynos-erudin-055.md](qeynos-erudin-055.md). This builds on accepted race72/gender0 collision, carrying and height. The existing DLL stays in use.

Implementation includes native vehicle-packet observation/waterline preservation, Lua source/destination handoffs, a reversible database/quest installer, and app controls. Local 163 Python tests pass, including eight Lua protocol scenarios. The pinned source’s actual client_packet.cpp and waypoints.cpp compile successfully in native run 35485423420. Real MariaDB and phone/Thor UI checks passed under 35485802194 on code commit `9b4a60266d8a68bdf2897d27c9c4c210aa2d8ac4`; screenshots were inspected. Earlier fixture/collation failures were fixed. PR #7 is ready for review. Preview notes now describe 0.5.5 and the required server Build/Deploy, superseding stale diagnostic instructions. Full release gates and physical RoF2 crossing acceptance are still pending. Existing 0.5.4 remains published until release verification completes. Installation requires one server Build and Deploy; an APK update alone cannot add the native support. Do not claim simulated protocol tests prove post-load deck attachment.

---

# Device result: 0.5.4 moving ferry and turns accepted (2026-09-20 UTC)

The user reports: the ferry works, height is correct, horizontal placement is off, and travel is slightly slow. Read [boat-ride-054.md](boat-ride-054.md) for the evidence and remaining tuning. The four screenshots show hull/pier overlap. **Accept boarding, moving passenger support and turning for this single-zone ferry.** Do not request another unchanged carrying test or repeat the rejected gender-2/PRE collision test.

The latest export, `logs-5331243628418525720.zip`, contains 1,027 attached samples across two separate launches. The current launch includes **649 consecutive attached samples spanning 331.365 seconds**, with actual straight travel and five turns at zero movement input. The previous launch adds three such turns. Player displacement follows the boat; passenger radius stays stable through turns. All attached samples have `wet=0`; the separate zero-valued `passenger` byte is still not an acceptance criterion. Current V2 rollover and continuation are observed on-device. Native dinput8 is loaded, and all seven deployed header hashes match. Raw private inputs remain uncommitted.

**Preserve the working Z `-39.5`, model, size and flymode.** Logged Z `-39.375` is not grounds to alter the correct height. Dock XY and heading still need calibration together; the manual spawn pose proved boarding, not a correctly surveyed berth. Screenshots establish overlap but do not establish exact replacement coordinates. The later player viewpoint is not a desired boat origin. Obtain a deliberate berth reference or dock/model geometry before assigning corrected XY/heading; check departure and return clearance.

The observed 159-unit leg takes approximately 38 seconds. The source-supported modest speed candidate is **runspeed 0.50 → 0.55**, which changes derived walking speed from 7 to 8 (about 14.3% faster; predicted 33–34-second leg). A change to 0.60 would instead be about 28.6% faster. These are proposed values, not deployed changes. Keep the 90/15-second pauses. Direct SQL/Spire edits to owned rows would invalidate the ferry's manifest: tuning must use a supported transactional update or backed-up removal/reinstallation with revised defaults.

This follow-up changes documentation only; metrics, screenshots, source speed calculation, deployed hashes and whitespace were checked. The signed [0.5.4 APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=b45a028), merge `b45a02831f74cb14ad7f57c15ec3e963d5e27986`, and existing deployed DLL remain current. No new APK, database change or new device test is supplied by this entry. Full Qeynos–Erudin cross-zone service remains unfinished. Preserve closed controller, camera, particle, loading, camping and player-restoration milestones. No subagents used. Documentation-only `[skip ci]`; normal product release gates remain required for the next implementation.

---

# Released: 0.5.4 solid ferry and timed passenger route (2026-09-19 UTC)

The user authorized moving forward with the fix. [PR #6](https://github.com/Russianranger/trasc-server-android/pull/6) was reviewed and merged at `b45a02831f74cb14ad7f57c15ec3e963d5e27986`. The **signed 0.5.4/code 44 preview is published**. Read [boat-ferry-054.md](boat-ferry-054.md) for the exact route and device procedure. This supersedes older instructions to remain on the 0.5.3 APK; the existing deployed 0.5.3 DLL remains appropriate.

[Download 0.5.4 APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=b45a028). Stop the client, game server and runtime; install over the existing app, then start the existing runtime. Keep the server stopped while choosing **Spire → Erud’s Crossing ferry trial → Preview installation → Save reviewed ferry change**. A full database backup is created first. Start the server, enable **Boat investigation → Record collision and passenger state**, visit the same Erud’s Crossing dock, and select `/target TRASC_Ferry`. Board during its 90-second dock pause, release movement controls, and ride through the 15-second far pause and return. Stop the client and Export Logs. If the ferry has not appeared within three minutes at the dock, stop and export logs. No manual spawn, account-status change, server build, DLL recompile, runtime reinstall or client/source reimport is required.

**Delivered:** a separate managed ferry using the user-confirmed solid race-72/gender-0 `SHIP`, body type 1, size/flymode 0 (matching the successful command), at X 607.125, Y -1782, Z -39.5, heading 308.5. Its far waypoint uses the existing seed grid-59 departure corridor’s X/Y, 625.304/-1939.82, retaining Z -39.5. Previewed install/reset/removal includes server-stop guards, full backups, transactional ownership and audit records, stale-change rejection and cleanup limited to the ferry’s own content and saved state. Reset restores its dock start on the next zone load; removal permanently removes the managed trial. Existing route spawns, quests, player/account data and client behavior are preserved.

**Verification:** all 150 Python tests and the [focused content/browser suite](https://github.com/Russianranger/trasc-server-android/actions/runs/35472267217) passed on final PR head `0946f6222e1fe28790d7efea90b5a00fddd9d8bd`. All six [PR verification jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/35472269394) passed. All seven [main release jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/35473487713) passed, including publication. Real-seed-schema MariaDB tests passed on local 10.11, CI 11.4 and the published ARM64 runtime: install/reset/removal, backup failure, changed rows/references, transactional rollback, scoped saved-state cleanup, duplicate rejection and lost-response recovery. Browser workflows and Thor/phone screenshots were reviewed. Existing JVM, Microsoft DLL compilation, Android lint/signing, direct/PRoot Software/DXVK/VirGL graphics, input, audio and restart gates were retained; none was waived.

**Published identity:** APK 10,569,749 bytes, SHA256 `c5d0d6e2680039dcc1e035b2adb93b4569a1da5d8d2f0ec3c4646b2e793673aa`; package `io.github.russianranger.trasc.preview`; version 0.5.4/code 44. The main CI `apksigner` gate verified the preserved certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Downloaded main artifact `10593568073` has ZIP SHA256 `24c7350ecdcf0b28b31c62aea958076e824175abb0bdee98e8b6905ea6bfd366`. All 30 backend/header assets and 16 UI assets match source; compiled DEX contains the new backend deployment name. The public APK/source/manifest sizes and digests match the verified artifact and exact build manifest. Source SHA256 `492043660b1370e1910c8b8ca2b93be1f637f4929034f575591f14e67e015243`; manifest SHA256 `56fbe3583556fbbea4e8c22b3e987c879ddb1dff40021c15553920358958e9b7`. Public preview tag matches the merge. Evidence is under ignored `runtime-work/054-release`; no private binaries, logs or video were committed.

**Next acceptance:** moving passenger support and turning on the new ferry. Stationary collision/boarding of this model was already accepted from the user’s prior test and 63 valid attachment samples. Do not repeat the withdrawn unchanged gender-2/PRE collision test. Judge the ride from actual boat/player motion, deck support and `vehicle_valid`/vehicle ID, not the separately logged zero-valued `passenger` byte. Full Qeynos–Erudin cross-zone service remains unfinished; the four original routes remain disabled as before. Preserve all closed controller, camera, loading, particles, camping and player-restoration milestones. No subagents used. This final handoff update is documentation-only `[skip ci]`; the published binary remains the verified merge above.

---

# 0.5.4 implementation: solid ferry with a timed single-zone route (2026-09-19 UTC)

The user authorized moving forward with the fix. Read [boat-ferry-054.md](boat-ferry-054.md) for scope, safeguards and the next device procedure. Development is on `codex/boat-ferry-trial`, version 0.5.4/code 44. The earlier authorization to review, merge and deliver a signed APK remains applicable; complete all existing release gates. Publication status will be recorded above this entry after verification.

Spire now offers a previewed, backed-up install/reset/removal of one separate Erud’s Crossing ferry. It uses the confirmed solid race-72/gender-0 `SHIP` at the successful origin X 607.125, Y -1782, Z -39.5, heading 308.5, then follows a fixed-height two-stop route with a 90-second dock pause and 15-second far pause. The new route prepares movement/turning acceptance without requiring GM movement privileges. No original disabled route, account permission, player record, quest or client/DLL behavior is changed. Passenger carrying and cross-zone service remain unaccepted; do not call automated database checks proof of either.

The user’s collision observations are final for these tested variants: solid higher/lower gender-0 ship, boarding accepted when lower, pass-through gender-2 ship. **Do not repeat the withdrawn PRE/gender-2 test.** Use the existing deployed 0.5.3 DLL with the new APK; no DLL/server recompile is needed. Preserve closed milestones and settings. Raw private inputs remain uncommitted; no subagents used.

---

# User clarification: retire the repeated failed collision test (2026-09-19 UTC)

The user explicitly rejects repeating the unchanged gender-2 (`PRE`) collision test. They clarify that the higher spawn of the working gender-0 (`SHIP`) variant was solid and blocked entry even when level with the deck; the lower spawn remained solid and allowed boarding. The original gender-2 variant allows pass-through. Accept this user-observed distinction. **Do not ask for the previously proposed `TBTLow` / gender-2 height comparison again.** Any such instructions in historical entries below are withdrawn.

The existing evidence already accepts stationary boarding and vehicle attachment for race 72/gender 0: 63 valid attached samples in `logs-642380592878288980.zip`. The earlier original-model attempt has no attachment and user-confirmed absent collision in this installation. Its underlying asset/client cause is still unknown; another identical device test is unnecessary. Keep the existing APK/DLL and closed settings/milestones.

Next investigation uses the known solid gender-0 ship for controlled rotation and passenger carrying. [Updated boarding notes](boat-boarding-053.md) record the source-checked rotation command, differing command access requirements, acceptance criteria and why translation needs a fixed-height endpoint. No new device test is requested by this correction; prepare its setup before requesting one. No permissions, routes, client assets, product code or APK changed. Documentation and source-review follow-up only; no subagents used.

---

# Device result: stationary boarding and vehicle attachment confirmed (2026-09-19 UTC)

Read [boat-boarding-053.md](boat-boarding-053.md) first. The user reports collision and successful boarding of the gender-0 ship after spawning it lower. `logs-642380592878288980.zip` confirms **63 samples with a valid vehicle pointer to the second ship**, including 46 consecutive samples spanning 22.926 seconds. The video shows movement aboard the wooden model. This accepts stationary boarding/attachment on the existing signed 0.5.3 APK and deployed DLL; no new code or APK was needed.

The first gender-0 spawn (entity 107, Z -20.375) has no attached samples. Its replacement (entity 108, Z -39.500) attaches; X/Y/heading also changed. Thus the original gender-2 model is not proven to lack collision: the earlier attempt used a higher origin. Both new commands were `#spawn TBT0 72 1 0 10000 0 1 0 0 0 1`; the server strips numeric name characters, so the log calls both `TBT000`. Both cleanup commands are recorded. The `passenger` byte stays zero even when `vehicle_valid=1`; do not use that byte as the boarding gate.

**Next:** keep the current build and settings. Test the original gender-2 model at the same successful lower location and facing with `#spawn TBTLow 72 1 0 10000 2 1 0 0 0 1`, then `/target TBTLow`. Desired ship X,Y,Z is 607.125,-1782.000,-39.500, heading 308.500; the logger prints Y,X,Z. Try reaching the deck from above/dock for 20–30 seconds, remove the selected temporary ship, stop the client and export. The spawn uses the player's current position; confirm actual placement in the next log. The successful stationary ship is a positive control; movement, rotation and route handoff remain untested. Do not enable all disabled routes or replace their models from this result.

Documentation-only follow-up; video/log inspection, command correlation, matching deployed header hashes and whitespace checks completed. No product code, database or settings changed. Private inputs remain uncommitted. No subagents used. Previous no-attachment entries below are historical for their individual test attempts, not the current overall milestone.

---

# Device result: TBT captured; no sampled attachment (2026-09-19 UTC)

Read [boat-tbt-053.md](boat-tbt-053.md) first. The latest user video and `logs-1735854186408412618.zip` show a visible, selected race-72 ship named TBT, pass-through during the attempt, and 112 V2 target samples over 56.595 seconds. Its race flags are already 3 and its graphics actor pointer is nonzero; the player's vehicle pointer and passenger field stay zero. Native dinput8 and the correct 0.5.3 headers are confirmed. Selected-ship V2 capture is now accepted on-device; collision, carrying and route handoff remain open. This supersedes the older no-target capture immediately below. Short-session capture does not prove on-device rollover, and side pass-through alone does not establish a deck approach from above.

The exact original executable/graphics DLL were recovered. The offline verifier now passes 17 cases, including the floor filter's separate graphics collision mask and the original model registrations: race 72/gender 2 (TBT) selects `PRE`, while gender 0 selects `SHIP`, both with race flags 3. The live collision mask/model geometry remains unknown; no flag override or force-attachment fix is justified.

**Next device action:** keep the existing 0.5.3 APK and deployed DLL. At the same Erud's Crossing dock/spawn point, use `#spawn TBT0 72 1 0 10000 0 1 0 0 0 1`, then `/target TBT0`. This changes only the gender/model argument from the recorded command. Try boarding, including approaching the deck from above where possible, for 20–30 seconds; record whether it renders as a ship and supports the player. Remove the selected temporary ship with `#depop`, stop the client and export logs. The previous TBT cleanup command is already present in the supplied server log. No recompile, reinstall or route enablement is needed.

This follow-up updates only documentation and the offline private-input verifier; no new APK was built or repair claimed. All 17 verifier cases, Python syntax and whitespace checks pass. Private binaries, logs and video remain uncommitted. Preserve closed player restoration, camping, particles, controller, camera and loading behavior. No subagents used.

---

# Released: 0.5.3 continuous boat capture; collision remains open (2026-09-19 UTC)

The user can target the corrected synthetic race-72 ship but walks through it. The latest export (`logs-904919649256838973.zip`) contains exactly 512 mostly idle boat samples over 43.04 minutes, then stops before later session activity; no selected/attached ship was captured. The recorder exhausted its quota. The four actual Qeynos/Erudin route spawns are disabled in the installed screenshot and pinned seed. Read [boat-capture-053.md](boat-capture-053.md) first for evidence, corrected body-type-1 spawn command, cleanup and next testing steps. Do not enable complete routes or claim a collision diagnosis from this trace.

[PR #5](https://github.com/Russianranger/trasc-server-android/pull/5) merged at `cb89116f185243b3a20d1695810c1478d42ea8e6` after review and all six [PR verification jobs](https://github.com/Russianranger/trasc-server-android/actions/runs/35463528351) passed on `c8f2dc2974f37c2a57596d817b29e1ce6d74d725`. All seven jobs in the [main release run](https://github.com/Russianranger/trasc-server-android/actions/runs/35464831641) passed, including publication. The **signed 0.5.3/code 43 preview is published**. This supersedes the earlier 0.5.2 recorder limit; it does not repair boat collision.

[Download 0.5.3 APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=cb89116). Stop ROF2, the game server and runtime; install over the existing app, then start the existing runtime. With the server stopped, compile/deploy `dinput8.dll` once using this app and the existing imported SDK/source. Start the server, keep native dinput8 enabled, choose **Boat investigation → Record collision and passenger state**, and test the same temporary ship in one zone. Keep it targeted for 10 seconds, attempt boarding for 20–30 seconds, remove only the selected temporary ship with `#depop`, then stop the client and Export Logs immediately. No runtime reinstall, server build or source/client reimport is needed. Preserve working graphics, loading, particles and input settings.

**Change:** V2 boat capture has no finite sample quota and rotates between two 256 KiB segments (`client-boats.log`, `client-boats-rollover.log`). Both use existing launch retention/export. Active/idle report rates remain two per second/one per five seconds; inspection is limited to twice per second. An older V1 DLL is rejected with a compile/deploy prompt, and inherited V1/V2 enablement is cleared. Capture stays read-only, with the original exact-client and invalid-pointer guards; shared camera/particle loggers are unchanged.

**Verification:** 147 Python tests, JavaScript syntax, Android build/lint, existing browser/JVM/database/session checks, actual Microsoft x86 DLL compilation, and every existing ARM64 direct/PRoot Software/DXVK/VirGL graphics, audio, input and restart gate passed. Expanded Microsoft tests cover two idle hours followed by ship capture, another active hour, tick wrap, repeated bounded rollover and I/O failure/recovery. No gate was waived. Physical-device V2 capture acceptance and the collision/attachment repair remain outstanding; do not repeat already closed camping, player-restoration or particle acceptance tests.

**Published identity:** APK 10,562,511 bytes, SHA256 `4f5cc1740c41c48f1bf596ff48d8e6535e0ee35d653f0b363444c77133c12155`, package `io.github.russianranger.trasc.preview`, version 0.5.3/code 43. The main CI `apksigner` gate verified the existing certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. The downloaded APK hash matches both the public asset digest and build manifest; all 29 backend/header assets and 15 UI assets match the merge source. Public preview tag matches the merge commit. Downloaded build-manifest SHA256 `666235c5de47f0527dbb6909d9aa899a5130f462f44a5f930e14b033f393e3e7` matches its public asset digest. Local verification evidence is under ignored `runtime-work/053-release`; raw device exports/private binaries were not committed. No subagents used. This final handoff record is documentation-only `[skip ci]`; the tested binary remains the merge commit above.

---

# Released: 0.5.2 merchant item picker and reopened boat investigation (2026-09-19 UTC)

The user reports the 0.5.1 Spire release working and requests easier merchant sell-list additions and attempting boats now. This supersedes older boat deferrals. Review found no blocking issues or outstanding review threads; [PR #4](https://github.com/Russianranger/trasc-server-android/pull/4) merged at `3fd8136341aaa76d55d5957a6eed3d462952d75c`. The **signed 0.5.2/code 42 preview is published**. Read [boat-investigation-052.md](boat-investigation-052.md) for implementation, exact-client evidence, current limits and the device test. The existing official Beta 0.5 release remains separate.

[Download 0.5.2 APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=3fd8136). Stop the client, game server and runtime, install over the existing app, then start the existing runtime. Merchant changes need only this APK update. Keep the server stopped for Spire saves. Boat diagnostics additionally need one compile/deploy of dinput8.dll using the existing imported toolchain/source. No runtime reinstall, server rebuild or source/client reimport is required.

Merchant helper: NPC/inventory → Add item → searchable item picker → suggested free slot → existing Preview/Save/backup/audit. A shared merchant inventory affects all linked NPCs; suggestions never reserve or overwrite an occupied slot. Existing manual editing and saved compatibility/export behavior remain.

Boat work: recovered the exact private executable, traced vehicle classification, collision attachment, floor cache and coordinate conversion. Ten original-code checks pass using synthetic graphics objects. Standard ship race flags already enable the original attachment path. No demonstrated general boat repair follows from these isolated checks. This candidate adds a default-off, read-only diagnostic option and bounded `client-boats.log` to locate a real single-zone failure. No force-attach/coordinate/collision override is implemented or claimed. Compile/deploy dinput8 once only for the boat test; no server/runtime/source rebuild or reimport is needed. Existing working particle/camera/input/loading settings are preserved.

**Verification:** the [focused Spire run 35443828628](https://github.com/Russianranger/trasc-server-android/actions/runs/35443828628) and all six [PR verification jobs 35443830759](https://github.com/Russianranger/trasc-server-android/actions/runs/35443830759) passed on `29047a9ec53b6b5bfcdbee01f4ac468a05b1a340`. All seven jobs in [main release run 35445063123](https://github.com/Russianranger/trasc-server-android/actions/runs/35445063123) passed, including publication. Coverage includes 147 Python tests, real MariaDB/seed/content/slot-concurrency checks, browser/JVM flows, Microsoft x86 DLL compilation and read-only boat snapshots, Android build/lint/signing, and every existing ARM64 direct/PRoot Software/DXVK/VirGL graphics, input, audio and session gate. Phone and Thor merchant-picker screenshots were reviewed. No gate was waived.

**Published identity:** APK 10,561,995 bytes, SHA256 `61506a198826bf7fe5e714e32e1ed4edd9ba6dbaeef6e5833260a5b4748988d0`, package `io.github.russianranger.trasc.preview`, version 0.5.2/code 42. Independently verified APK v2 RSA/SHA256 signature/content and preserved certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`; all 29 backend/header assets and all UI assets match source. Compiled deployment/launch code includes the new boat header and option in `classes2.dex`. Main artifact `10585368244` ZIP SHA256 `62abfc2e6b0d5911856eb63bde2a82a787a2db4abb7e1b9926c434ec9cf95e13`. Public preview tag matches the merge commit; all public asset sizes/digests match the verified APK, source archive and exact expected build manifest. Source SHA256 `09a7adb9f67e63f6160627919e40251b0ec97358b2c4ec25d40bdc460e9efc4c`; manifest SHA256 `74ab1f35cecf729f0ceac39f7caf465d669183ed8d2bb9fcd0f3ce6ff8a7ab71`. Evidence is under ignored `runtime-work/052-release`; private executable/disassembly remain uncommitted.

**Next device work:** the specific new merchant picker and boat diagnostics need acceptance. Target one named ship in one zone, attempt boarding/riding for 1–2 minutes, then stop and Export Logs; report ship, zone and fall-through versus sliding. A verified boat repair remains open pending that trace. Spire base functionality is user-reported working. Camping, player restoration and first-person particles remain closed as user-confirmed. No subagents used. This final release record is documentation-only `[skip ci]`; the published binary remains the verified merge commit above.

---

# Released: 0.5.1 Spire content workspace (2026-09-19 UTC)

The user explicitly authorized review, merge and delivery of an APK with testing instructions. Review found no blocking issues, the tested PR head and base were unchanged, and [PR #3](https://github.com/Russianranger/trasc-server-android/pull/3) merged at `c2a35e0acfccef1d3114093c78d4718bbc583e63`. The **signed 0.5.1/code 41 preview is published**. Earlier statements that this feature is awaiting merge approval or has only a PR-signed candidate are historical. The existing official Beta 0.5 release remains available separately.

[Download 0.5.1 APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=c2a35e0). Read [Spire scope and Thor tests](spire-051.md). Stop ROF2, stop the game server, then stop the runtime; install over the existing app and start the existing runtime. Keep the game server stopped for Spire saves. No DLL/server rebuild, SDK/source refresh, runtime reinstall or client reimport is needed. Keep the working client settings.

**Delivered:** a dedicated Spire tab; searchable items/NPCs/loot/merchants with related-record links and friendly names; previewed common-field edits, merchant/loot additions and removals, validation, full backups, stale-edit rejection and transactional change history; spell/string/AA editing using the original client export and saved RoF2 spell policy. Standard item effect, NPC and spell column names are covered against the actual bundled schema. Custom fields and player/account data are preserved. AA definitions/effects refresh after server restart and relog; their strings and spells use Export & sync. High-ID compatibility remains separate.

**Verification:** [final Spire suite 35439132450](https://github.com/Russianranger/trasc-server-android/actions/runs/35439132450) and all six [PR verification jobs 35439134250](https://github.com/Russianranger/trasc-server-android/actions/runs/35439134250) passed on `26033571bdb0de75072330318f92802da0f081c7`. After merge, [main release run 35440858451](https://github.com/Russianranger/trasc-server-android/actions/runs/35440858451) passed all seven jobs including publication: 145 Python tests, browser/JVM flows, real seed/database/player/content integration, Microsoft DLL compilation, Android build/lint/preserved signing, and every existing ARM64 direct/PRoot Software/DXVK/VirGL graphics, audio, input and session gate. No gate was waived. Phone, Thor-landscape and wide screenshots were reviewed. Physical Thor acceptance of Spire remains outstanding; use the checklist rather than treating automated checks as device confirmation.

**Published identity:** APK 10,555,909 bytes, SHA256 `cce4c7f60d1b296a4858cb3dd5647260db83d0e0bbaed75e947673cd47b51477`, package `io.github.russianranger.trasc.preview`, version 0.5.1/code 41. Independently verified APK v2 RSA/SHA256 signature/content and preserved certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`; all 28 backend/header assets and all UI assets match source, and compiled deployment code names both new Spire modules. Main artifact `10583982175` ZIP SHA256 `fa06bc2a2d3afe1e8aa54ef0eee3a64efda501dbbd63ccb4d6df3d7fa62cc6d3`. Public preview tag matches the merge commit, and public asset digests match the downloaded candidate and exact expected build manifest. Source archive SHA256 `cbca6bf7f367742c7128b5b05473f60a906f5d3e8dac645e4665aceaae2fa053`; build manifest SHA256 `512dc4e1e5c21d862c2906a3da0db74193c525e439a3e3cb54c07529bc07b3d6`. Local verification evidence is under ignored `runtime-work/051-release`.

**Deferred/closed:** boats remain explicitly shelved. Camping, player/account restoration and first-person particles remain closed as user-confirmed. Preserve the existing camera/controller/loading/reconnect behavior. No subagents used. This final release record is documentation-only `[skip ci]`; the published binary remains the verified merge commit above.

---

# Authorized development: Spire content workspace; boats deferred (2026-09-19 UTC)

The user authorized all three selected Spire milestones and a dedicated **Spire** tab. This supersedes the older planning-only restriction below for those features. Development is on `codex/spire-content`, version 0.5.1/code 41. Read [spire-051.md](spire-051.md) for scope, safeguards, limits and device acceptance. Beta version 0.5 remains the released 0.4.23/code 40 APK until a new candidate completes verification and publication is addressed.

Native launcher/Python implementation includes linked content browsing; common item/NPC, merchant and loot edits; spell/string/AA editing; save previews, live-schema validation, backup, stale-edit protection and transactional change history. It reuses the existing client export and preserves the current RoF2 filtering/restore behavior. High-ID compatibility remains separate. Preserve working particles, camera, controller, reconnect, graphics, loading and player restoration. Camping remains closed as user-confirmed.

**Boats are shelved at the user's direction.** Record collision, passenger attachment/carrying, rotation and zone handoff as a future investigation. The client/server references from the discussion are leads, not a diagnosis or implemented repair. Begin with one ship inside one zone when this is reopened. No boat code changes belong in this milestone.

Implementation is in [PR #3](https://github.com/Russianranger/trasc-server-android/pull/3). The focused [Spire verification run 35437991661](https://github.com/Russianranger/trasc-server-android/actions/runs/35437991661) passed on implementation commit `0c98032`: 145 Python tests, real MariaDB edit/rollback/export integration, all twelve bundled schemas, existing management/browser flows and responsive Spire workflows. Phone, Thor-landscape and wide screenshots were reviewed. Final schema review also added the standard item spell columns (`clickeffect`, `proceffect`, `worneffect`, `focuseffect`, `scrolleffect`), retaining underscore aliases for custom forks, with real-seed coverage to prevent silent omissions. Full Android/ARM64 regression results are tracked in the PR checks and must pass before merge/release. The new checks supplement every existing release gate. A PR-built APK uses a CI debug certificate and must not be installed over the existing app; the preserved release certificate is applied by the main build after merge approval. Do not uninstall the app to test a differently signed candidate. No subagents used.

---

# Official release: Beta version 0.5 (2026-09-19 UTC)

The user requested promotion of the verified APK to an official release labelled Beta version .5. [Beta version 0.5](https://github.com/Russianranger/trasc-server-android/releases/tag/v0.5) is published as a regular GitHub release (not draft or prerelease) and marked Latest. Tag `v0.5` points to the tested implementation `095fcc9137f2d671986b90718dde975cd7c58a51`. Read [beta-05-release.md](beta-05-release.md).

This is a release-label promotion of the **identical 0.4.23/code 40 APK**, not a new app build. Android and the launcher still show 0.4.23. Package ID, signature, settings and all source/runtime/DLL behavior are unchanged. Existing 0.4.23 users do not need to reinstall. The release APK is named `trasc-server-android-beta-0.5.apk`, 10,538,843 bytes, SHA256 `f6c27a924033d435dae5c88eff0436508d8bb1357486e5ec8ebf681b3bcc1c9f`; corresponding native sources and `beta-build.json` are attached. Manifest SHA256: `84255c7cf6e04713a2fad92f5c24ebc808ad8a7f0af41f316c184d7875bc0c52`.

[Promotion run 35412824812](https://github.com/Russianranger/trasc-server-android/actions/runs/35412824812) succeeded. It downloaded immutable main artifact10574102684 from successful run35410359686, required the approved source commit, checked APK/source hashes and the preserved certificate, uploaded all assets to a draft, then published and checked the tag, release flags and every public asset digest. The one-time workflow is retained on `codex/publish-beta-05`; it is not part of main's build pipeline. Preview/runtime releases are unchanged. The 0.4.23 verification and remaining device/backlog milestones below still apply. No subagents used. This handoff update is documentation-only `[skip ci]`.

---

# Released: 0.4.23 launcher layout; camping and player migration confirmed (2026-09-19 UTC)

The user explicitly approved merging PR #1 and publishing 0.4.23. [PR #1](https://github.com/Russianranger/trasc-server-android/pull/1) is merged at `095fcc9137f2d671986b90718dde975cd7c58a51`; the signed preview is published. The earlier publication-approval block is resolved for this release. Read [launcher-0423.md](launcher-0423.md).

The sticky Server runtime panel now has a complete border and opaque background. Startup says “Starting server…” and reports “Server started” only after success. Client dropdowns and checkboxes are grouped into compact responsive layouts with expandable help; Gameplay controls are also condensed. All existing control IDs, values, defaults and saved-setting paths are retained. The footer and APK both report 0.4.23 / code 40. Preserve the working DLL, particle repair, camera, reconnect, loading, controller and graphics settings. No server/DLL build, runtime reinstall, source/SDK refresh or client reimport is required.

**Verification:** all six PR verification jobs passed on [run 35408972837](https://github.com/Russianranger/trasc-server-android/actions/runs/35408972837). After approval and merge, [main release run 35410359686](https://github.com/Russianranger/trasc-server-android/actions/runs/35410359686) passed all seven jobs, including publication. This includes 138 Python tests, host/JVM and browser flows, database/session/player migration tests, real Microsoft add-on compilation, Android build/lint/preserved signing, and every existing ARM64 direct/PRoot Software/DXVK/VirGL graphics, audio, model, input and restart gate. No gate was waived. Phone, Thor-landscape and wide UI screenshots were reviewed; physical Thor acceptance of the new layout remains the next user check. No subagents used.

**Published identity:** APK 10,538,843 bytes, SHA256 `f6c27a924033d435dae5c88eff0436508d8bb1357486e5ec8ebf681b3bcc1c9f`. Independently verified APK v2 RSA/SHA256 signature/content, manifest 0.4.23/code 40, application ID `io.github.russianranger.trasc.preview`, certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`, all 26 backend/header assets and all UI assets against source. Public preview tag matches the merge commit; public APK/source digests match the downloaded main candidate. Exact reconstructed build-manifest bytes match the published manifest digest. Client-runtime publication also succeeded. Native-source archive: 114,558,375 bytes, SHA256 `21f985bdea5b8b55213d805f8d1acdbb61b6b448b87a3361d81200d4de714d78`. Evidence is under ignored `runtime-work/0423`. This final record is documentation-only `[skip ci]`; the tested release remains the merge commit above.

**Update:** stop ROF2 and the runtime, [install the preview APK in place](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=095fcc9), then reopen the installed runtime. Check the runtime border, start-status wording and grouped launch settings in the usual orientation. Keep existing game/server files and settings. Future Spire work below remains planning only.

## Newly closed device milestones

- [x] **Player/account snapshot restoration across a database update:** user explicitly confirms inventory and progression survived. This is separate from the previously confirmed complete-session backup/restore. Do not request the same migration test again.
- [x] **Camping:** user reports the camp fix worked after changing `Custom:CampTimerMs` from2900 (2.9s) to30000 (30s) and restarting. Screenshots showed Berolinen and Yehaos with `character_data.gm=0`, account `eqemu` with `status=0`, and ruleset1 with `Character:EnableHackedFastCampForGM=false`. No evidence restoration granted GM status. Close the reported camp issue; older entries saying camp is parked/unresolved are historical. This is user-confirmed behavior, not a newly captured packet trace or proof of which update introduced2900. No new camp code/automatic database edits are needed.
- [x] **First-person particles:** retain the 0.4.22 device acceptance below. Additional lifecycle regression coverage can accompany normal play without repeating the successful baseline.

## Future development: selected Spire features (planning only)

User selected recommendations1–3 from `Russianranger/spire-valorith`. These are backlog milestones, not part of0.4.23 and not authorization to implement them now. Prefer integrating focused screens into the existing Android launcher/Python backend; no separate full Spire service has been selected.

1. **Content browser:** searchable items, NPCs, loot and merchants; follow relationships between records. Read-only first, with pagination, useful names/icons where available, and mobile-friendly linked detail views.
2. **Focused content editing:** common item/NPC fields, merchant inventories and loot. Preview changes, validate against the installed schema, back up before writes and record changes. Preserve custom fork fields; define when restart/reload is needed.
3. **Spells, database strings and AA editing:** connected editors with reference checks and the existing four-file Export & sync workflow. Retain RoF2 compatibility filtering. Full high-ID support/remapping is separate work: an editor must not imply excluded IDs>=45000 are usable. Confirm which data needs client export versus server reload/restart before enabling edits.

Not selected: full Spire replacement, Sage/3D world editing, live telnet/API management and additional character-recovery tools. Keep the existing player snapshot tools.

Other open work remains: high-ID spell compatibility, intermittent cold-start name corruption, actual zone-state persistence acceptance, the older unproven server-stop/app-exit report, longer background/power/thermal validation, and separately scoped UI/XML/global-model loading optimization. Do not reopen working camera/reconnect/controller/particle fixes or conflate game FPS with presentation rate.

---

# Device-confirmed: 0.4.22 fixes the reported first-person particles (2026-09-18)

Read [particles-0422-device.md](particles-0422-device.md) first. User reports the fix worked. Bundles `logs-1107241098681185606.zip` (diagnostics) and `logs-8361952957421422627.zip` (repair) confirm `install=ready` in both: hidden root actor with permission0 and attempts0 in profile, then `repaired hidden=1 before=0 after=1 attempts=1`, followed by `permitted before=1 after=1 attempts=1` in repair. The second bundle preserves the first particle log as previous. This is live device confirmation of the missing permission and correction, supported by visible success; the earlier initialization sequence itself remains untraced.

On-device build/deploy succeeded: DLL1,716,224 bytes, SHA256 `a5bda1fe2af6367e8fbeacc3054bb8089fbb33681d5c8b61eb73f869a73a6566`; all six adapter-header hashes match released source. Working loading/controller/Turnip26/Native Surface settings remain active. Spell loading1.752s/2.067s, identical counts/checksum bytes and zero fallbacks; no particle speedup claim. No adapter failure/retry loop or current unhandled-exception signature. Both logs still show disconnect-to-character-select and return, so keep camp separate and parked. The captured pair does not establish a second in-world login or zone-to-zone transition; do not invent that acceptance or repeat the completed baseline.

**Current action:** keep Repair + diagnostics enabled; its logs are bounded and this pair is only221/379bytes. No new APK, DLL rebuild, runtime/SDK/source reinstall or client reimport. Preserve the successful fix and other settings. Further loading optimization is separate work, guided by UI/XML/global-model timings. This update only records acceptance with [skip ci]; implementation `dde9476e9a82950bf497a0082fbb94579cdb2db6` and the published0.4.22 APK remain current. No subagents used.

---

# Released: 0.4.22 optional first-person particle repair (2026-09-18)

Read [particles-0422.md](particles-0422.md). User authorized proceeding with the fix. Added default-off **First-person spell particles**: Original behavior, Diagnostics only, Repair + diagnostics (experimental). Wraps the verified first-person update, calls the original first, then uses the original permission setter only for a hidden, unpermitted current local root actor. Exact-binary/layout, thread, lifetime, scope and writable-memory guards apply; attempts/logs are bounded. Profile observes without repair; off does not hook. The new header is included in the Android deployment contract. Existing controller/reconnect/loading code is preserved; camp parked.

Implementation `dde9476e9a82950bf497a0082fbb94579cdb2db6`, version0.4.22/code39, [run35385991190](https://github.com/Russianranger/trasc-server-android/actions/runs/35385991190): **all seven jobs successful**. 138 Python tests, native/JVM/browser checks, Microsoft v142 fixture and real add-on build, ARM64 Wine/Box64 particle fixture and every existing direct/PRoot Software/DXVK/VirGL graphics/audio/model/input/restart, database/session, Android lint/signing and publication gate passed. No gate waived. Original two-private-binary verifier still passes all11 scenarios. Portrait/landscape option screenshots reviewed. No subagents; no private game binaries/SDK committed.

Independent APK v2 signature/content and preserved certificate verified; manifest0.4.22/code39; all26 backend Python/header assets and UI files match source; compiled Java contains new header/mode; 33 native source/recipe files match. APK10,537,055 bytes, SHA256 `5b7ec5d94dbe5afb2678b8dab6a42987df2f7a6b4805a5e3e1d61b31fb794cdc`. Native sources114,558,421 bytes, SHA256 `29fe73561a1f48e3210cdb6fb6b6ed935e4194ea7544d8d8203a4eee2442e1f0`. Public preview tag and asset digests match the candidate. Deterministic build-manifest reconstruction matches the published asset's full SHA256. Local verifiers/evidence under ignored runtime-work/0422. Final handoff update is docs-only [skip ci].

**Next device steps:** stop client/server/runtime, update APK in place, restart existing runtime with server stopped, compile/deploy dinput8.dll once using existing SDK/source, then start server. Compare fresh first-person Minor Healing: Diagnostics only versus Repair + diagnostics, each before any view switch. Export each short run; repeat repaired relog/zone, then check normal third person, input and reconnect. Preserve working loading/controller settings; keep sound/verbose diagnostics off. No runtime/SDK/source reinstall or client reimport. Opt out by Original behavior and relaunch. Actual Thor startup cause and visible repair remain unmeasured: `repaired hidden=1 before=0 after=1` plus restored first-cast effects supports diagnosis; an already-permitted failing baseline requires emitter-state investigation instead of a broader override.

---
# Graphics DLL verified: hidden-actor particle suppression reproduced (2026-09-18)

Read [particles-graphics-0421.md](particles-graphics-0421.md) first. User supplied the installed `EQGraphicsDX9.dll`; size/hash exactly match the recorded 1,604,608-byte binary. No more game-file uploads are needed. Actual actor constructor `0x1003AB80` clears the permission byte; setter/getter `0x1003B1A0`/`0x1003B190` access actor `+0x5C`. Five actual actor vtables/RTTI classes confirm slots `+0x23C`/`+0x240`. The particle processing branch at `0x100726E4` suppresses hidden actors lacking permission, subject to existing effect-sentinel/disabled-state conditions.

New read-only `tools/verify-client-particles.py` executes original code from both exact binaries. All 11 checks pass: constructor default, four visibility combinations, late-actor/view-roundtrip reproduction, original actor replacement losing permission, one setter restoring the path without changing camera/body visibility, independent disabled-state behavior, normal leave/visible behavior, and the effect exception. Original actor assignment transfers application data/type but not this flag. Tests use synthetic objects and a bounded particle-processing branch, not a full rendered game or observed Thor login. Root cause on the device remains unmeasured; missed/replaced actor initialization is the leading explanation.

**Next development:** add bounded actor/flag diagnostics and a default-off correction at a verified first-person update point. Only for the current in-world local actor, when hidden and permission is false, use the original setter and confirm its result; retain normal leave, other visibility checks, module/byte guards and opt-out. Confirm root/attachment ownership and thread/lifetime first. If permission is already true while effects fail, trace emitter state instead. Detailed hook candidate and fresh-login/relog/zone acceptance are in the note. No automatic view switches or actor recreation. No repeat test/audio trace needed on unchanged 0.4.21. This commit is analysis/verifier-only [skip ci], no product change or APK; keep existing release gates for any future build. Preserve controller/reconnect/loading, camp parked. No subagents.

---

# Verified view routines: first-person entry enables hidden-actor particles (2026-09-18)

Read [particles-view-routine-0421.md](particles-view-routine-0421.md) first. Recovered and hash-verified the user's original `eqgame.exe`; no repeat executable upload is needed. Actual mode dispatcher is VA `0x48ADF0`; first-person enter/leave are `0x797220`/`0x7971B0`. Enter calls local graphics actor vtable `+0x23C` with true, leave with false. Matching public RoF2 definitions name this method **ShowParticlesWhenInvisible**. First-person frame paths hide the player model. Entry skips the setter if player/actor is absent; the inspected actor-assignment code does not reapply it. A missed/reset flag is now a concrete hypothesis, not yet a measured device root cause.

Added read-only `tools/verify-client-view.py`: six scenarios execute the exact original x86 dispatcher/callbacks in Unicorn and pass, including absent actor, later entry, view roundtrip, absent player, other-camera transition and original restriction. It checks stack/register preservation and callback ordering (global mode changes after callbacks). Graphics calls are recording stubs: no live rendering or login-order claim. Notes pin all addresses, source provenance and limits. No executable/SDK included; no app or installed DLL change, no APK, analysis/tool-only [skip ci]. No subagents.

**Next:** inspect the installed `eqgraphicsdx9.dll` (1,604,608 bytes, SHA256 `164fc072547aab752567ba88bf6936d0e328c44a16a1480f340d27aef0ba6290`), which was not found among available prior artifacts. User can export from Files → `client/current` → search `eqgraphicsdx9.dll` → select → Export file. Verify actual setter/getter, flag initialization and render gate, then capture bounded actor/flag transitions on the game thread before choosing a repair. Do not write an unverified actor field, force camera toggles, recreate actors, request duplicate spell tests or another audio trace. Keep 0.4.21/current deployed DLL and working loading/controller/reconnect behavior; camp stays parked. Full acceptance plan is in the new note.

---

# Particle follow-up: a view-only switch restores first-person effects (2026-09-18)

Read [particles-0421-view.md](particles-0421-view.md) first. The user completed the requested test: one first-person Minor Healing cast had no particles; a few seconds in third person **without casting**, followed by a first-person cast, produced particles. A third-person spell cast is not required. The view-only recovery sequence works; the exact state change remains unproven, and a matched elapsed-time control was not performed. Do not request the same cast/view comparison or another audio trace.

Bounded review of the CI-pinned add-on finds camera-switch/actor declarations and conditional forwarding stubs, not verified engine implementations or addresses for those paths. The current launcher camera adapter only reads state/recenters the cursor. Next development work is to recover the previously supplied exact executable, verify its view-switch/player-actor path, and capture bounded state transitions before choosing a patch. Full evidence, source links and acceptance criteria are in the note. Do not blindly call actor creation or silently force camera changes. Existing 0.4.21 and deployed DLL remain current; no new APK or particle fix is claimed. Documentation-only [skip ci], no subagents.

---

# Device-confirmed: 0.4.21 DLL build repaired; particles depend on camera state (2026-09-18)

Read [loading-0421-device.md](loading-0421-device.md) first. New bundle `logs-7280042745619990780.zip` confirms successful on-device DLL compilation/deployment, SHA256 `1be0374b5c8d323057ba6c665203309645f8a0d71c27780158e3677da53ef96e`, with all three adapters including display V1. The missing-header failure is resolved. No further update/compile/reimport is needed for the next test.

With Faster spell loading enabled throughout, the model-pause off/on pair records server-selection to character UI **32s → 29s**; a later enabled diagnostic session takes 30s. Global scope is 12.214s → 10.189s, but the direct wait saving is only **94.523ms** across 89 calls. Do not attribute the entire three seconds to the wait patch or present single samples as averages. UI/XML and global model work remain the larger targets; all spell counters/hashes remain consistent. Published APK stays 0.4.21; this is documentation only with [skip ci].

User reports Minor Healing particles absent for three first-person casts, visible in third person, then working in first person; the initial failure repeats after relogging. Camera-dependent setup/visibility is a hypothesis, not a proven cause. The latest session has no texture-load error. Earlier `zapmuze.dds` failures remain, but that file is present and the previously inspected Minor Healing definition references three other textures. The off-mode run never enters the world, so it cannot establish a particles-off/on comparison.

**Next:** disable Sound diagnostics (61.6MB cumulative trace in this run), keep working settings fixed, and after a fresh login compare first-person casting before/after a brief third-person view **without casting there**. Only if still broken, cast once in third person and retest first person. Ask which step restores particles. This separates displaying the character from creating the effect; do not force camera changes or replace assets speculatively. Keep camp parked and preserve the working controller/reconnect fixes. No subagents used.

---

# Released: 0.4.21 repairs omitted runtime header deployment (2026-09-18)

Read [dll-header-0421.md](dll-header-0421.md) before the historical 0.4.20 release note. The user could not compile the new DLL: `/opt/trasc/eq_display_loading.h` was missing. The APK included the header, but RuntimeManager's explicit backend-copy list omitted it. ClientRuntime mounts that copied directory for the compiler. Added the header to that list; restarting the existing runtime after updating copies it automatically. No adapter or game behavior changes.

The new Android deployment-contract regression fails on 0.4.20 with precisely this missing header and passes with the fix; all 136 Python tests pass locally. Version 0.4.21/code38. Native/JVM checks also pass. Published implementation `9700df77e1924b841c7f7e61740166123ad32609`, [run35372949456](https://github.com/Russianranger/trasc-server-android/actions/runs/35372949456), all seven jobs successful; no release gate waived. Candidate APK manifest confirms 0.4.21/code38, all25 Python/header assets match source, and compiled Java contains the corrected header deployment string. CI preserved the signing certificate. Public tag/build manifest and APK/native-source asset digests match the downloaded candidate. APK10,531,871 bytes, SHA256 `0871b26ec39cf4d49bd8d761092ab46cea17b1f0aeb42b03a37dbe5157a04a2c`. Full details and checksums are in the linked note; local verification evidence is under ignored runtime-work/0421. No subagents used.

Next: have the user update in place, restart the server runtime with the game server stopped, compile and deploy a newly successful DLL using the existing SDK/source. Keep Faster spell loading enabled and resume the model-wait off/on comparison, with a separate particle test. No runtime/SDK/source reinstall or Mac tools. The failed attempt preserved the earlier DLL. Physical Thor compilation and the model/particle tests remain unconfirmed.

---

# Released: 0.4.20 loading-pause experiment and particle diagnostics (2026-09-18)

Read [loading-0420.md](loading-0420.md). User authorized the next optimization and reports first-person spell particles begin working after switching to third person and back. Added independent, default-off **Reduce model-loading pauses (experimental)**, scoped to 18 verified Sleep(1) sites in global model loading; it retains scheduler yields and all model/service calls. New UI/XML/model/wait timings will identify the remaining larger costs. Camera V2 and spell loader V2 headers/behavior are unchanged. Keep the device-confirmed Faster spell loading enabled. No Thor speed claim yet; the new wait optimization may provide only a modest gain.

**Released and verified:** implementation `6276054d2d0d62b9a6f85afe519269d61e842a59`, version0.4.20/code37, [run35362750049](https://github.com/Russianranger/trasc-server-android/actions/runs/35362750049), all seven jobs successful. All135 Python tests, native/JVM/browser checks, actual Microsoft v142 add-on build, Windows/x86 fixtures, full ARM64 Wine/Box64 fixture execution, database/session roundtrips, Android lint/preserved signing and all existing direct/PRoot Software/DXVK/VirGL graphics/audio/model/input/restart gates pass. No failed check waived. New fixture tests actual relocated CALL and Sleep instruction forms, boolean/ABI passthrough, import preservation, opt-out and scope/thread exclusion. Isolated128-wait sample is136,892us original/211us yield: mechanism-only evidence, not a game loading speedup. All30 CALL sites,9 import operands and18 wait sites verified against the exact supplied executable; no proprietary executable/SDK uploaded.

Independent APK v2 RSA/SHA256 signature/content check, manifest0.4.20/code37, all39 Python/header/UI assets,33 corresponding native source/recipe files and phone layout verified. APK10,532,759 bytes, SHA256 `c129d3fc46ad5fad8d1f6238606c494f9d3e796630902667b2db2544e20d06fc`; native sources114,558,331 bytes, SHA256 `90c7f61631939df76d8873cf05253a8a10b73e8f3af6cfb71ff4b1b8bed781f4`. Certificate remains `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Public preview tag and asset digests match the verified candidate. Runtime publication succeeded. Local evidence/verifier/downloads under ignored runtime-work. This final update is documentation only with [skip ci]. No subagents used.

The particle report now checks `zapmuze.dds` loose/header metadata and optionally four named spell archives/graphics DLL hash. No rendering cause/fix is proven. Camera-dependent initialization/visibility is a hypothesis; do not label the texture missing or force camera/particle settings. The original three texture failures do not prove a checksum regression. Preserve the source-DLL reconnect fix, #tim, shared-memory capture and idle skips; camp remains parked.

**Next device steps:** stop client/runtime, update in place, compile/deploy dinput8.dll once using the existing SDK/source. No Mac tools, runtime reinstall, server rebuild or client reimport. Keep Faster spell loading and all other working settings fixed; compare two fresh launches to the same character screen with Reduce model-loading pauses off/on, then export logs. Compare outer global_models total and model_wait/model_load timings; XML timings are nested/inclusive, not additive. For a separate particle test, enable Sound diagnostics, cast the same self-spell in first person, then third person, then first person; note spell name/behavior and export logs. Disable diagnostics afterward. Particle cause and physical Thor gain remain open.

---

# Device-confirmed: 0.4.19 cuts the recorded character-select wait from 73s to 31s (2026-09-18)

Read [loading-0419-device.md](loading-0419-device.md) first. The user says the enabled option is much faster; bundle `logs-3472540275696201050.zip` confirms it. In the paired 0.4.19 off/on sessions, spell loading is **43,296ms → 2,107ms**, before-screen wait **58s → 18s**, and server-selected → character-select UI **73s → 31s**. These are one run per mode and engine landmarks, not averaged physical-button-to-frame timings.

The source-backed checksum target was correct: per-record CRC is **26,546ms → 232ms**, spell-file CRC **14,339ms → 135ms**. Both runs construct the same 40,914 records and retain identical spell-file hashes/settings. Fast mode processes 40,916 checksums / 79,627,413 bytes and 8,960,166 integer fields with zero fallbacks. DLL SHA256 `1083365ea7c14c50671ecef554a36b936f0fa32a3ca1e9c01491f165212d787d` confirms the V2 build was deployed. Keep Faster spell loading enabled; no further rebuild/reimport/runtime install or Mac tools are needed.

Both sessions enter the world, subsequently reach character select again and end by stop request, without a recorded stack overflow/unhandled exception. The existing camp/disconnected behavior remains parked. Existing asset/runtime warnings remain; the on-run additionally fails to load particle texture `zapmuze.dds` three times during gameplay without stopping execution. Do not attribute that to checksum optimization without evidence. Camera V2 is unchanged; off-run has 434 warps, on-run does not exercise look (zero look polls). Preserve the prior device-confirmed controller and source-DLL reconnect fixes. Shared-memory capture and unchanged-frame skips still work.

The next loading focus is **display/UI initialization and global models**: the enabled run spends 12s from display-init start to display initialized (including a 7s UI XML/default-resource landmark interval), then 10s in global data. Original line reading is only 712ms. Profile those remaining stages before choosing another implementation; do not treat coarse UI landmarks as exclusive parser timings. The unequal Wine-prefix preparation occurs before game startup and is outside this comparison. No product changes/new APK in this review. Published 0.4.19 remains `2233d068303ab543237403cce6e5b0e1ad041ac5`; this evidence update is documentation only with [skip ci]. No subagents used.

---

# Released: 0.4.19 spell checksum optimization (2026-09-18)

Read [loading-0419.md](loading-0419.md) first. The user authorized the next loading optimization after confirming that the controller works. The actual imported checksum implementation scans the entire registered-patch list for every byte, including disjoint spell records/files. The new optional path checks overlap once under the existing lock and computes the identical CRC; overlapping or unsupported ranges retain the complete original handling. It is scoped to this exact client's spell checksum calls on the loading thread with **Faster spell loading (experimental)** enabled. The previous integer optimization remains. Camera V2 is unchanged and device-confirmed. Loader V2 adds timings for text/records, per-record CRC, associations, file checksums and post-load mapping. A block-reader prototype was removed; original file/line handling remains.

**Released and verified:** implementation `2233d068303ab543237403cce6e5b0e1ad041ac5`, version 0.4.19/code 36, [run 35350753552](https://github.com/Russianranger/trasc-server-android/actions/runs/35350753552), all seven jobs successful. All 133 Python tests, native/JVM/browser checks, Microsoft v142 actual add-on compilation and executable loading/checksum fixtures, Android lint/preserved signing, database/session roundtrips and existing direct/PRoot Software/DXVK/VirGL graphics/audio/input/model/restart gates passed. The initial Windows run caught a test harness macro collision and initializer-list dependency; these were fixed without weakening a gate. All twelve stage-call targets were verified against the supplied executable. No proprietary executable or SDK was uploaded.

The ARM64 Wine/Box64 same-source checksum fixture processed 4,096 records / 5,144,576 bytes against 120 patches in **3,850 ms original versus 46 ms optimized**, with identical checksum `2089226240`. Both keyed/unkeyed checksum functions, overlap fallback, list mutation and boundaries passed. The full patched x86 loader fixture passed on Windows and Wine, including ABI/results, opt-out and scope cleanup. This is a hosted component benchmark, not an EQ/Thor loading-time claim. Actual device improvement remains unmeasured. Downloaded DXVK direct/PRoot evidence confirms free menus and continued camera movement after recentering.

APK 10,527,178 bytes, SHA256 `7a89702bf09301acbb5383de2c372ebb2667249a163368b9dacbcb50dee674f1`. Independent APK v2 signature/content verification, package/version, preserved certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`, and all 38 bundled Python/header/UI assets checked. Phone options layout reviewed. Source archive 114,558,335 bytes, SHA256 `ea022aec8f5a721ce55c66bd8a5c09211fad22e62968711e36c7a452907dc99f`. Public preview tag and asset digests match the verified candidate; runtime publication succeeded. Downloaded artifacts, evidence and verifiers are under ignored runtime-work. This final evidence update is documentation only with [skip ci].

**Next device actions:** stop client/runtime and update in place, then compile and deploy dinput8.dll once with the existing SDK/source. A 0.4.18 DLL still supports the working camera adapter, but cannot activate Loader V2. Compare fresh game launches to the same character screen with Faster spell loading off and on, stopping/relaunching between tests, then Export Logs. Compare the pre-screen/total wait and client-loading.log stage timings/counters; check ordinary spells, inventory/camera and character-select reconnect. No Mac tools, runtime reinstall, server rebuild or client reimport. Preserve the source-DLL reconnect fix, #tim and shared-memory/idle capture. Camp remains parked. No subagents used.

---

# Device-confirmed controller fix; 41s pre-screen spell-loader work remains (2026-09-18)

Read [loading-0418-device.md](loading-0418-device.md) first. User confirms controller works; preserve camera V2 and the source-DLL reconnect fix. New bundle logs-3542532455672139460 contains off/on tests. Before-screen waits are58s/55s, visible-screen phases14s/12s, totals72s/67s; retained0.4.17 is59s/15s/74s. The recorded landmarks show no pre-screen regression, although they do not measure the physical button press. One run per mode is not a controlled speedup result.

The actual spell-loader wrapper takes42,302ms off and41,405ms on. Fast parser processes8,960,166 fields with zero fallbacks; it works but saves only897ms in these samples. Main thread remains near100% of one CPU; MIT-SHM/idle skips work and seven five-second windows have zero new frames. Next isolate the main text/record pass, association load, file checksums and post-load mapping using exact call boundaries documented in the new note. Their individual costs are not yet measured; do not guess which consumes41s or claim the parser microbenchmark ratio for EQ. No product changes/new APK in this review; published0.4.18 remains16e989b. No rebuild/reimport/Mac tools needed. Camp remains parked. Documentation-only [skip ci], no subagents.

---

# Released: 0.4.18 camera correction and loading experiment (2026-09-18)

Read [loading-0418.md](loading-0418.md) first. Latest bundle logs-7057105716911952164 confirms V1 DLL deployment, normal startup/gameplay and failed camera recentering. Root cause: Wine 10 does not implement GetProperty(DIPROP_AXISMODE), so V1 always exits. V2 uses the verified game mouse buffer/relative format and now tests the full input handler in live Wine. Bounded client-camera.log records real activity.

First server-to-character load is 74s: 43s before race/display init with the main thread saturated, then 25s display/models. Added precise spell-loader timing and an optional, default-off faster integer parser for this exact executable. Original reader handles every non-simple field; spell/client/server data unchanged. Requires APK update and one on-device DLL compile/deploy; existing SDK/runtime/source suffice. No Mac tools. Keep camp parked and preserve the user-confirmed source-DLL reconnect fix. No subagents used.

**Released and verified:** implementation `16e989b12263b5f3df619fa74b04911914c5f7dc`, version0.4.18/code35, [run35339568776](https://github.com/Russianranger/trasc-server-android/actions/runs/35339568776), all seven jobs successful. 132 Python tests, native/JVM/browser checks, Microsoft v142 actual add-on compilation and executable parser/layout/ABI fixtures, Android lint/preserved signing, database/session roundtrips and every existing direct/PRoot Software/DXVK/VirGL graphics/audio/input/model/restart gate pass. The full production camera handler now runs in the live fixtures. Downloaded DXVK direct/PRoot evidence confirms free menus and continued camera motion after recentering. The open ARM64/Wine parser fixture takes761ms versus92ms for7,364,520 fields with matching checksums; this is not an EQ load-time result.

APK10,522,502 bytes, SHA256 `ed9042bac801c25cb26fc6ddd24441107181b057ac13a3dccd82f50314f5829e`. Independent APK v2 signature/content check, preserved certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`, package/version and all37 bundled Python/header/UI assets verified. Phone options layout reviewed. Source archive114,558,292 bytes, SHA256 `fc2292e1021a51cea5ea32cc9aaf1908400725252d06a7260e9b82f1471f92f1`. Public preview tag and APK/source digests match the verified candidate. Runtime publication also succeeded. Artifacts/evidence/verifier are under ignored runtime-work. This final handoff commit is documentation only with [skip ci].

**Next device actions:** stop client/runtime, update in place, then compile/deploy dinput8.dll once using the existing SDK/source. Enable camera recentering and check startup/server/character menus, held/toggled full rotations, inventory and reconnect. Compare two fresh launches to the same character screen with **Faster spell loading (experimental)** off then on, and export logs. New bounded client-camera.log records why recentering is gated and how many warps occurred; client-loading.log measures the actual spell-loader interval and parser use. Actual EQ/Thor camera correctness, normal spells and total loading gain remain unconfirmed. Turning the options off and relaunching disables their behavior; no runtime/server rebuild is needed.

---

# Released: 0.4.17 camera-only recentering (2026-09-18)

Read [camera-mouse-0417.md](camera-mouse-0417.md) first. Latest user confirms force recentering traps startup menus with the working compiled DLL. Uploaded exact eqgame.exe SHA256 `4a456734af62b465660610794780e48ac3b0161f7b96e13aee86267c45ea49a3` was inspected read-only. 0.4.17/code34 resets Wine force to default even for saved-on settings, and replaces it with an optional camera-only adapter compiled into dinput8. It requires the verified executable, in-world held/toggled look, hidden cursor and a game-owned foreground window. Imported source and game code stay intact; only two forwarding source files are overlaid in the build directory.

**Released and verified:** implementation `4d03e4c95cf17499cc5c508b265fe6a1527b0f9c`, [run35332966690](https://github.com/Russianranger/trasc-server-android/actions/runs/35332966690), all seven jobs successful. 131 Python tests, native/JVM checks, browser, actual Microsoft v142 add-on compilation and state fixtures, Android lint/preserved signing, database/session roundtrips and all existing direct/PRoot Software/DXVK/VirGL graphics/audio/input/model/restart gates pass. New fixtures verify free menus before/after camera look and continued positive relative deltas during recentering. Actual EQ/Thor 360-degree camera acceptance remains required.

APK10,518,492 bytes, SHA256 `f27d9c7ede46db38694b11bec5ebff6171f228c61bbabd2d572de3817555fcd2`. Independent APK v2 signature/content check, preserved certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`, and all35 bundled Python/header/UI assets verified. Phone options layout reviewed. Source archive114,558,375 bytes, SHA256 `6fb4380f8b48a86326717b0af8912289123002f7f6331beb8a30f6d41e1431db`. Public preview tag/asset digests match the candidate. Local APK/source/verifier and downloaded evidence are under ignored runtime-work. This final handoff commit is documentation only with [skip ci].

**Next device actions:** stop game/runtime and update in place. The APK alone restores normal startup pointing. To test camera-only recentering, open the server runtime with the game server stopped, compile and deploy dinput8.dll using the existing imported source/SDK, enable the new option and relaunch. Check startup/server/character menus, held/toggled full rotations, inventory, focus and reconnect. Turning the option off and relaunching deactivates the adapter. No runtime reinstall or tools on the work Mac. Keep the confirmed reconnect fix, #tim and shared-memory/idle-frame capture. Camp remains parked. Loading profiling remains open: static loading landmarks are recorded, no speedup claimed. No subagents were used.

---

# Reconnect resolved with compiled DLL; CPU-heavy loading and mouse look next (2026-09-18)

Read [reconnect-resolved-loading-0416.md](reconnect-resolved-loading-0416.md) first. User confirms deploying the source-built DLL fixed reconnect. New bundle logs-4436019274023234972 confirms deployed SHA256 `556ead4699b1af797eb2c676ef40dacc92ade932526ebe551f36382de7424c28`, backup `backups/client-setup/20260918-004503-d062b1`, and three successful character-select initializations after server selections in one process. The previous binary came from the NMS/Project Triune patcher; its exclusive features were not usable locally. Its nms_crash.txt is zero bytes: do not request it again. The earlier unidentified-DLL/reconnect-blocker notes below are historical.

Current goals are mouse look and faster server-select→character-select. Timed attempts take69/88/86s. The largest41/62/60s gaps precede display init; eqgame.exe's main thread consumes about99.6–99.9% of one core during these gaps, with available-core affinity and frame capture mostly idle. Authentication takes3–4s. Verbose diagnostics still generate47.3MB but the longest retained gap is trace-quiet: disable diagnostics for the baseline without claiming that resolves the dominant CPU work. MIT-SHM/unchanged-frame skips still work. Next export client/current/eqgame.exe through Files search to inspect the precise loading implementation, and separately retest the existing experimental recenter option with the now-installed compiled DLL (turn off/relaunch if still unsuitable). Current successful session has mouse_warp=default; no mouse-look fix is yet confirmed. No new APK/product changes in this investigation. Preserve #tim, compiled DLL, working runtimes, signing/test gates; camp remains parked. Documentation-only [skip ci].

---

# Installed DLL identified: reconnect points to add-on window handler (2026-09-18)

Read [reconnect-installed-dll.md](reconnect-installed-dll.md) first. The user supplied the installed DLL and SDK metadata after 0.4.16 file search. Installed DLL SHA256 `0a59be5dbd0c746ecd4168d3628b056dc490e0eabbaa6050c3d533802b13465b`, 3,445,760 bytes, differs from the staged compiler output `556ead46...`, 1,695,232 bytes. SDK is the expected MSVC14.29.30133/SDK10.0.19041.0. The installed add-on contains an ImGui/NMS loot/browser integration missing from the examined imported/public client source.

Actual binary RVA0x9EE4 is a call to GetTickCount inside an ImGui-related window procedure; its call instruction, 0x140-byte local stack frame, EAX and ESP-minus-four fault address match the device trace exactly. The stack is already exhausted at the call. Window-procedure chaining/re-entry on reconnect is the leading hypothesis, not a proven cycle: there is no caller backtrace or live saved-procedure value. The DLL's own exception handler attempts to write `client/current/nms_crash.txt`, which the prior log export did not include and which the secondary overflow may have interrupted. Next ask for the installed DLL's download/source origin and this small crash report if present, using Files search. Do not infer its function names from the unrelated cloud map or silently replace it with the different-feature staged add-on. No product fix or new APK is claimed. Keep force recenter off and restart the game client before reconnecting as the workaround. Preserve working #tim/shared-memory capture; camp remains parked. This handoff is documentation only with [skip ci].

---

# Released: 0.4.16 file search and complete folder browsing (2026-09-18)

Read [file-search-0416.md](file-search-0416.md). User could not reach client files past the silent 2,000-entry browser cap. Full-folder case-insensitive name search and 200-entry Previous/Next pages now make them reachable. Search results keep Select → Export file, with explicit counts/empty states and stale-response protection. Version0.4.16/code33; preserve signing and all existing release gates. Published and independently verified: implementation `b04db7ba0f20fce535251a7e65cadc58a007fc1e`, run35289099016, all seven jobs successful.129 Python, native/JVM, browser, Android lint/signing and all existing graphics/input/audio/runtime gates passed. Portrait/landscape search/export screens reviewed. APK10,514,646 bytes, SHA256 `461a8268765ead949c7f3222a6626325aa2decb5cfa7d51566a38c370c21d814`; corresponding source114,558,412 bytes, SHA256 `6a508bc5c2b03eb2590afaad08bb60d42c7cd5b1c37ff2084e557cc838c9e53e`. Public preview tag and asset digests match the verified candidate. Preserved signing certificate, package/version and all33 bundled source assets checked. Local deliverables/verifiers are under ignored runtime-work. This final handoff commit is documentation only with [skip ci]. No client/DLL/runtime/graphics changes are part of this update. Keep recentering off; export client/current/DINPUT8.dll and client/toolchain/sdk.json after installation to resume the separate reconnect diagnosis.

---

# Latest Thor feedback: keep recentering off; reconnect DLL fault captured (2026-09-17)

Read [reconnect-0415-device.md](reconnect-0415-device.md) before the historical release notes below. The user confirms gear `#tim` works in EQ. Forced mouse recentering crashes during graphics startup; keep it off. With recentering subsequently reset to default, character select → server select → Play produces `c00000fd` first at native DINPUT8.dll RVA `0x9EE4`, followed by a secondary Wine exception-handler overflow. Android exit history also identifies the earlier 21:44 stop-time app closure as Chromium's unhandled WebView renderer crash; it predates the 0.4.15 recovery handler.

An isolated Windows symbol build succeeded (run35287847157, diagnostic branch25ae5b5), but its binary layout does not match the device fault. Do not infer a function from that map. The installed DLL identity is not in this bundle; only a staged-build record is present. Next request the actual `client/current/DINPUT8.dll` and tiny `client/toolchain/sdk.json` through the app's Files → Export file. No Mac tools or full-session upload needed. Published app remains verified0.4.15/09e5e0b. No product code, runtime, prefix, database or client files changed during this investigation. Keep the working #tim sequence and shared-memory path; restart the client before re-entering the server as the temporary reconnect workaround.

---

# Released: 0.4.15 input follow-up and crash diagnostics (2026-09-17)

Read [input-crashes-0415.md](input-crashes-0415.md). The new device bundle confirms MIT-SHM capture at about0.95ms, with idle/duplicate skips, but contains no usable Android crash or reconnect stack trace. Gear #tim now uses paced keys without Ctrl+A; optional Client → Recenter mouse for camera look enables Wine's own per-application warp handling after relaunch. It is off by default and requires camera/inventory acceptance on the Thor. Android exit history/uncaught-Java capture, WebView recovery and notification shutdown lifecycle handling are added. **Neither the reconnect overflow nor the reported stop crash has a proven root cause or confirmed device fix.**

**Released and independently verified:** implementation **09e5e0b5334073c25a7fdca4a39240add0fc66e1**, version0.4.15/code32, [run35283326055](https://github.com/Russianranger/trasc-server-android/actions/runs/35283326055), all seven jobs successful.125 Python tests, JVM/C/controller/archive/cancellation tests, browser, Android compilation/lint/preserved signing, Microsoft DLL, database and every direct/PRoot software/DXVK/VirGL graphics/audio/input/model/shutdown/restart gate passed. Six real Windows receivers got exact #tim; buffered/polled motion continued past clipped desktop bounds and Wine recentered correctly. These are open fixtures, not physical EQ/Thor acceptance. First candidatebb6685d/run35281306782 was superseded after review corrected recovery ordering so no input callback targets a failed WebView; do not deliver its APK.

**Final files:** APK10,513,934 bytes, SHA256 `f1f39f02ae6e192bd0e29c6ad11018efe18368f60b0c5fe9a8b4e22e14330d05`; source archive114,558,369 bytes, SHA256 `fb6a0784df644d91366c0f7fdf48feec98e20930f739f2e561d5df8da3bc5571`. Preserved certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`, package `io.github.russianranger.trasc.preview`. Independent APK v2 signature/content verification, all33 bundled Python/UI assets, ARM64 binaries/memfd marker/helper hash and matching native sources passed. Artifacts and verifiers are under ignored `runtime-work/`; final APK is `trasc-server-android-0.4.15.apk`.

**Publication recovered:** preview now points to the tested09e5e0b commit and its APK/source digests match the verified candidate. Both client-runtime-v1 archives and manifest are published successfully; fresh online runtime availability is restored. The old0.4.13/14 publication blocker below is historical. Do not retry its recovery branch or reinstall the working Thor runtime. All workflows are finished. This handoff is documentation-only with [skip ci]; it does not change the tested implementation.

**Next device actions:** stop client/runtime and update in place. Test gear #tim in-world. Enable Client → Graphics, audio & launch options → Recenter mouse for camera look and relaunch; compare held/toggled camera look and inventory, disabling/relaunching if pointing snaps to center. Export Logs after updating; Android may still retain the earlier stop-crash record. For the character-select → server-select → reconnect overflow, turn on Wine diagnostics for one short reproduction, export, then turn verbose diagnostics off. A fresh client launch avoids that reconnect sequence while diagnosis remains open. No server/DLL rebuild, database/camp change, prefix repair, client reimport or Mac tools are needed.

---

# 0.4.14 implemented and verified; public release upload blocked (2026-09-17)

The latest user authorized the model/settings, shared-memory/power and mouse-look milestone. Read [models-capture-0414.md](models-capture-0414.md) for scope, evidence, exact artifact hashes and device checks. Camp remains parked. No tools need installing on the user's work Mac and no subagents were used. The staged INI remains untouched and is not committed.

Implemented Client → Game settings · eqclient.ini (model presets, individual flags and existing advanced entries), original/previous backups, stale-edit protection and stop-before-save. Gear → Toggle classic NPC models issues #tim. PRoot enables --sysvipc with a memfd allocator for modern Android. Native Surface uses XDamage to skip idle captures and exact comparisons to avoid duplicate transfers/posts. A private XTest relative-input channel removes controller pointer edge clamping; optional Android physical-mouse capture is available from the gear menu. Current/30 remains default and Native Surface remains optional.

Tested source `93487815e9c18d9cabef37ba30d4b8347807af77`, tree `df065c77db66b941c87e296e303a7ca7d7ee7896`, [run 35268952881](https://github.com/Russianranger/trasc-server-android/actions/runs/35268952881). All 124 Python tests, JVM/C checks, browser flows, Android compilation/lint/signing, Microsoft compilation, database import/restore, and every original direct/PRoot Software/DXVK/VirGL rendering/model/audio/input/shutdown/restart gate pass. Runtime installer and session roundtrip pass. Five build jobs succeeded. The client-runtime job failed only in its publisher; preview was skipped. Do not call the whole workflow green. Initial run 35267141600 caught the CI helper's 108-byte Unix-socket limit; the fixed run retains every gate. Android's allocator was also changed from ashmem-only to memfd-first, and CI verifies that actual path.

Downloaded DXVK/PRoot evidence confirms MIT-SHM at 1280×720, 2 captures for 25 requests with 23 idle skips, correct resumed pixels, and 6,240 pixels of polled DirectInput movement with a one-pixel cursor clip. These are CI observations, not proof of Thor power/FPS or actual EQ behavior. A repaint of identical pixels still needs capture for comparison; idle XDamage state can skip capture entirely.

Signed APK 0.4.14/code31 is independently verified and stored locally at `runtime-work/trasc-server-android-0.4.14.apk`, SHA256 `a494a6eb1a5dc4acdc70d20b29ec1bcea7f70b77d07b03a9861ba695f3bed273`. Immutable Actions artifact 10517459357 contains this APK and corresponding native sources. Preserve signing identity and installed runtimes. Update in place after stopping game/runtime; no runtime reinstall, compiler/server rebuild or client reimport is intended. Deliver the verified APK directly. Corresponding native sources are `runtime-work/launcher-sources-0.4.14.tar.gz`.

**Publication:** runtime upload returned HTTP 500 twice at 20:31:29 and 20:31:31 UTC. Public preview still serves 0.4.12. client-runtime-v1 is still partial (only the older manifest); do not remove/reinstall the user's working runtime. Runtime recovery artifact 10518054067 from this run contains the runtime payload/source archives and manifest; ZIP399,740,763 bytes, SHA256 `845bda86b46a5abc2aa9f70639378523c4512f374d421cdf0361e01dc1c0e9b1`. No speculative publisher change or additional retry was made. All workflows are finished. Device acceptance remains for actual EQ models/#tim, continuous mouse look, rendering and power usage. The documented CI results are not a Thor battery/FPS claim.

---

# 0.4.13 implemented and verified; public release blocked (2026-09-17)

The user's authorized controller, HUD and presentation changes are complete. Read [controller-layers-0413.md](controller-layers-0413.md). Camp remains parked. No subagents were used and no tools need installing on the user's work Mac. The verified APK has been supplied directly; GitHub publication is blocked by repeated HTTP 500 responses uploading runtime archives. Do not describe the public preview as 0.4.13.

**Implemented:** four editable Thor layers (Main, Hotbar 2, Spells, Inventory), up to six named layers, wrapping Next/Previous, direct selection and nested held overrides. Main exactly matches the requested controls, including LT cycling, RT Tab, Start I, Select Esc, RB left click/LB right click. A faint top-center label, gear/launcher editors and migration of saved profiles are included. Existing users apply **Thor defaults / cycling → Apply preset → Save** once to adopt the requested layout. Launcher **Show DXVK FPS / stats** is saved for the next launch; stop/relaunch is required. DXVK 2.5.3 reads HUD configuration at construction, so there is no live gear toggle. Native Surface now batches packed frame payloads instead of 720 row sends and records capture/send/pacing/call counts and SHM failure reasons. Current/30 remains the default; Native remains optional. Version 0.4.13/code30.

**Final tested source:** `f4131dd2d317bc2b279e2318939d9a3c4ffabc6c`, tree `4959914c3c59ef2a8625abefaea4c5915279b8e9`, [run 35254262724](https://github.com/Russianranger/trasc-server-android/actions/runs/35254262724). All application and rendering tests passed: 119 Python tests; host JVM controller/RFB/management checks; strict C frame/color/transfer tests; browser flows; Android compilation/lint/preserved signing; real Windows MSVC compilation; database import/restore; and every original direct/PRoot Software/DXVK/VirGL graphics/audio/input/model/shader/exit/restart gate. Five build jobs succeeded. The client-runtime job completed all tests, then was canceled during its stalled publisher; the final preview job did not publish. Do not call all seven jobs green.

The initial run 35248592584 caught an incorrect new socket-permission assertion: Unix bind with umask0077 produces0700, already owner-only. The corrected test requires owner read/write and no group/other access; production permissions were not relaxed. All tests passed on replacement run35249900083/sourcebce7362 before its publisher stalled. Sourcef413 preserves that application/test code and adds serial uploads, bounded timeouts, one retry and manifest-last publication. Its full replacement run also passed all tests before publication stalled. No failed test gate was waived.

**Verified install candidate:** [Actions artifact 10511784814](https://github.com/Russianranger/trasc-server-android/actions/runs/35254262724/artifacts/10511784814), `trasc-server-android-debug`, contains the signed APK and matching native source archive. ZIP size124,899,125 bytes, SHA256 `eb9e6712ad8b39efc718ba293d746f64e8c3b43aacfcf2a27c2b6fbaa17da20a`.

- APK: `runtime-work/trasc-server-android-0.4.13.apk`, 10,491,746 bytes, SHA256 `1b17d6c2df78119f64f25940b5c2c96b4ea4d2e2111020c099e5473c2f73bb23`.
- Matching sources: `runtime-work/launcher-sources-0.4.13.tar.gz`, 114,552,978 bytes, SHA256 `3a1dc710278ba08f9d932594b6774a850776dba80b05d4d3d5104ff5712d850a`.
- Independently verified APK v2 RSA/SHA256 signature and complete signed-content digest; preserved certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Package `io.github.russianranger.trasc.preview`, version0.4.13/code30. All31 packaged Python/UI assets match tested source; both presentation binaries are ARM64 ELF, helper matches its bundled hash, and all6 presentation sources/recipes match the archive. Verifier and metadata are under ignored `runtime-work/`.
- Runtime recovery artifact10511874660, `client-runtime`, same run: ZIP399,748,770 bytes, SHA256 `4df9a620f06dab2b14f3a591290c858a5703c7da617dbafb82589ea938466c16`. Contains runtime root/source archives and manifest. This is for publication recovery; the user should keep the already-installed working runtime.

**Rendering evidence:** final downloaded DXVK/VirGL reports confirm eight XGetImage frames across two connections with two actual sends/frame. Fullscreen DXVK verifies1280×720, 3,686,400 bytes/frame, SHM flag0 and expected colors; normal SHM coverage remains. Real DXVK integration launches with HUD off. Browser screenshots of the unchanged application UI were inspected. These are CI software-rendering results, not proof of Thor speedup or physical controller/banner acceptance.

**Publication blocker and durable recovery:** branch `codex/recover-preview-0413` contains a publication-only workflow, currently commit `0effb08a514e97637c46758362bc3796797e1bbd`. It validates the original source/run identity, five successful build jobs, all nine critical runtime test steps and all pre-publication steps, then verifies immutable artifact hashes and signing identity. Run35258706554 tried bounded GH CLI uploads with HTTP/1.1; both attempts timed out. Run35260279002 switched to direct HTTP/1.1 curl uploads with the same guards. Both archive uploads returned HTTP500 after partial transfers: 41,943,040 bytes at18:41:57 UTC, then62,783,488 bytes at18:42:30 UTC. The run completed failure at18:42:34. These are observed server responses; their underlying cause is not established. All workflows are finished; stop speculative retries and do not rebuild application features to address publication.

Public `preview` still points to **0.4.12**, commit `1bf596e7ac8d3ad50bd2b9640fa153adae794367`. The earlier `gh release upload --clobber` attempts left `client-runtime-v1` partial: only its manifest is present; the two large archives are absent. Fresh online runtime downloads are therefore unavailable until recovery. Existing device runtime is unaffected. Do not instruct this user to remove/reinstall it.

**Next publication action:** recover from the immutable artifacts above after upload service/network behavior recovers; use the recovery workflow as the reviewed starting point. Its main-head guard is pinned to f413. This final documentation-only handoff advances main with `[skip ci]`, so review that docs-only diff and adjust the separate main-head guard before retrying. Keep SOURCE_SHA, original run and artifact hashes pinned to testedf413; do not label a later docs commit as tested. Publish runtime archives/manifest, then APK/matching sources/build metadata and preview tag, and verify public sizes/digests/tag. Recovery workflow changes remain on their separate branch, not in main. Public release completion remains outstanding.

**Next device check:** stop client/runtime and update in place using the supplied verified0.4.13 APK. Apply/save **Thor defaults / cycling** once; check four layers and the faint label. Held overrides must restore the selected layer on release. Turn HUD off in Client and stop/relaunch. Compare the same warmed scene at Current/60 and Native Surface/60 for about a minute each with identical HUD/settings, then export Logs. No runtime reinstall, server/DLL rebuild, database edit, client reimport, prefix repair or Mac tooling is needed. A Thor performance gain remains unproven.

---

# Device comparison: Current/60 leads; Native Surface needs transport work (2026-09-17)

User supplied `logs-2976453518264923596.zip` after the three-mode comparison. Read the new Thor comparison section in [controller-presentation-0412.md](controller-presentation-0412.md). During Greater Faydark gameplay, controls closed/focused, median Current/30 Bitmap draws were 30.13/s, Current/60 were 40.72/s, Native/60 Surface posts were 25.13/s. These are display-delivery metrics, not DXVK game FPS; camera/movement equivalence is not established by logs. Native stayed active without fallback and Xvnc sent zero RFB pixel updates, but **all native frames used XGetImage, not MIT-SHM**. Native capture ~5.29 ms, request/receive ~38.49 ms, Android lock/copy/post each ~0.4–0.45 ms; payload ~92.65 MB/s versus 62.42 MB/s Current/60. Helper consumed ~50.5% of one CPU core. Source sends 720 payload rows per 720p frame: batching is the next concrete optimization, with transfer/SHM diagnostics and a real non-SHM integration case. Exact SHM setup failure is not logged; do not invent it. No speedup is proven and no new performance patch/APK was made in this log-review turn.

Recommend Current display/60 for this Thor comparison, keeping shipped Current/30 default and Native Surface optional. No new renderer crash or presentation fallback; normal quit/stop markers present. Android thermal status 0 does not prove no throttling. Legacy Wine/MIDI/gamma warnings and a few audio underruns recur across modes. Visual correctness and controller feel are not established by logs; user observation requested. Camp remains explicitly parked. This evidence-only handoff update uses `[skip ci]`; published release remains tested source 1bf596e, version 0.4.12.

---

# Released: 0.4.12 controller and presentation milestone (2026-09-17)

User shelved camp and authorized controller/gear-menu polish plus measurement and an optional more-direct display prototype in the same pass. Read [controller-presentation-0412.md](controller-presentation-0412.md). Existing profiles and Current display/30 defaults remain; Native Surface bypasses RFB pixels/Java Bitmap but still uses X11 readback. It is not a zero-copy implementation or a proven Thor performance improvement. No server/database/DLL change is included. Version 0.4.12/code29; preserve the existing signing cache/certificate and every release gate. Published and independently verified; physical Thor acceptance remains next.

**Published and verified:** source **1bf596e7ac8d3ad50bd2b9640fa153adae794367**, [run 35237303372](https://github.com/Russianranger/trasc-server-android/actions/runs/35237303372). Superseded run 35236998398 was canceled automatically; the replacement includes Surface-reader shutdown and input-only RFB startup fallback race fixes. All seven jobs passed; run completed **2026-09-17 15:24:53 UTC**. Windows DLL, database import/restore, WineD3D, graphics dependencies, APK and the full original direct/PRoot graphics/audio/input/model/shader/exit/restart gates passed. Browser tests cover preset editing, alternate-layer persistence and display options; downloaded screenshots were reviewed. CI reports 118 Python tests, host JVM/controller/RFB checks, strict C protocol/color tests and Android compilation/lint/preserved signing. Real Wine software, DXVK and VirGL tests verified correct pixels and consumer reconnect through both transports directly and under PRoot. DXVK fullscreen tests checked over 95% expected-color coverage at 1280×720; downloaded direct/PRoot frame reports confirm eight sequenced frames across two connections, 3,686,400 bytes/frame and MIT-SHM capture. CI uses software graphics and cannot establish Thor Surface output, controller feel, heat or performance. No failed gate was waived.

**Independent candidate verification:** Actions artifact **10503674199**, ZIP SHA256 `2f3b0fd2381fb890e7c0b83d00db5f005bff3ab3d5eea67bba970c5ed2655344`. APK v2 RSA/SHA256 signature and complete signed-content digest verified; certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869` preserved. Manifest is 0.4.12/code29, package `io.github.russianranger.trasc.preview`; all 31 Python/UI assets match the tested source. Both new binaries are ARM64 ELF, the helper matches its bundled manifest hash, and all five new native sources/build recipes match the corresponding source archive. Candidate APK is **10,484,630 bytes**, SHA256 `c5a0a75f9e20ce1c0494c2b626875bb295caaa50c24b204962e865796b58135a`; source archive is **114,551,951 bytes**, SHA256 `99a1c5bd9648b47c02fa73f36c1aa5282196359deb23958e1a10fd5a98758a14`. Local copies are under ignored `runtime-work/`. Public release APK/source digests and sizes match those exact candidate files; `preview` points to the tested commit. Public build manifest is 368 bytes, GitHub digest `ce68ce796bfc0190e29a1b235d4a178fb7bedb0620af28ea7cc3264146307908`. Verification used the downloaded Actions candidate plus public release metadata, not a second download of the public APK.

The camp follow-up bundle `logs-4182792564389881283.zip` showed the server close at13:59:50, client logout attempt14:00:20 and recovered character selection14:00:25. Settings save preceded restart. GM camp bypasses Custom:CampTimerMs; live GM flag/rule value remain unproven. User was offered #rules get and self #set gm off, then explicitly parked this issue. Do not resume camp work during this milestone.


**Install and device comparison:** [0.4.12 APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=1bf596e). Stop the game/runtime and update in place. No runtime reinstall, server/DLL rebuild, database change, client reimport or Mac tooling is required. Keep working driver/CPU/NPC settings. In-game gear → Controller mappings → EQ adventure/hotbars → Apply preset → Save; verify movement, camera, targets, hotbars/inventory, held-input release and saved bindings. For display comparison, use the same warmed-up scene for roughly one minute each: Current/30, Current/60, Native Surface/60. Export Logs afterward. Native remains optional; Current/30 remains the default and manual recovery choice. See the milestone document for the full mapping and measurement limits. This final handoff update uses `[skip ci]`; the tested release is complete.

---

# Released: 0.4.11 log retention and camp investigation (2026-09-17)

User confirms gameplay works, reports a disconnect when camping, and requests at most 5 older logs per process with 2/3/4 alternatives. Read [log-retention-0411.md](log-retention-0411.md) for source/log evidence, implementation and device checks. The attached `logs-6442597055042792067.zip` shows a server-initiated close at 13:15:53 alongside three successful character saves, no Wine crash, zone idle one minute later, and explicit server stop at 13:17:24. Current source's default `Custom:CampTimerMs=100` uses the `fast_camp`/`OnDisconnect(false)` path; normal logout also sends pre-logout packets. Live rule values were not exported, so this is a strong source-supported diagnosis, not packet-level proof. User has been given the existing-app test: set the active ruleset's value to 29000, save and restart server. Do not patch the graphics stack or silently alter database rules. GM and Bazaar/EC logout paths differ.

New native log retention runs without Python/runtime availability. Default 5, allowed 2/3/4/5, saved in `work/logs/retention-count.txt`. Immediate cleanup and a one-minute maintenance timer prune recognized closed PID logs by process family; zone files share one quota. Active/unknown PID state, symlinks, unknown files, chat, exports and backups are preserved. Python/native writers now retain the selected count of `.previous[.N].log` archives, with bounded 8 MiB startup/tail captures. Active server output is not a total storage quota. App 0.4.11/code28; preserve signing cache and all release gates. No new runtime payload or server/DLL build is required on the device.

**Published and verified:** 0.4.11/code28, tested source **f05bc6660892066f869a53bfb577c2d6bfbe7bf0**, all seven jobs successful in [run 35229662711](https://github.com/Russianranger/trasc-server-android/actions/runs/35229662711), finished 2026-09-17 14:15:06 UTC. CI passed **115 Python tests**, native JVM retention/session/controller/audio tests, browser flows for all four retention choices with the runtime closed, Android compilation/lint/preserved signing, real Windows MSVC source compilation, full database import/restore, and all original direct/PRoot graphics/audio/model/input/exit/restart gates. CI graphics rendering is not a physical Thor camp or retention acceptance test.

Run 35227717333 was superseded by the cleanup concurrency fix. Run 35227875998 caught an actual UI race: returning to Logs while a saved-setting read was pending could overwrite an edit. The picker now disables edits during loading, ignores stale reads and waits for pending writes before reloading. The unchanged browser regression passes on the released source. No failed check was waived. Local Chromium download was unavailable (HTTP 502); browser verification and reviewed screenshot came from CI.

**Independent artifact verification:** candidate **10501690086**, ZIP SHA256 `3c8dbd23a8ccc96dc1805c851d304215560cde7ec99dd9f4d01d41d3dd49c6cd`. Verified APK v2 RSA/SHA256 signature and complete APK content digest, preserved certificate, manifest package/version/code, and all **30** packaged Python/UI assets against the exact tested source. The public release APK and source-archive digests match the downloaded candidate; the preview tag points to f05bc66. This was an Actions artifact download plus public release metadata comparison, not a separate download from the public APK URL.

- [APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=f05bc66): **10,460,169 bytes**, SHA256 `a7013d1912bef497dcc4350f2f485a8fdedea6c69358f8a29efbef4321177cd7`.
- Corresponding native sources: **114,548,227 bytes**, SHA256 `c8870a61d2d130d76d4c647ec8b0331d62f98e813599084db8db24707271838e`.
- Public build manifest: **368 bytes**, GitHub digest `3b20caec39bf635d08bcb9bf9a6544891acb3561e2aef7058cea120203d06071`.
- App ID `io.github.russianranger.trasc.preview`; certificate SHA256 `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Keep signing cache `trasc-preview-signing-v2`.

**Next device checks:** stop ROF2/runtime and install in place. Logs → Log storage → choose 2/3/4/5 → Save & clean older logs, then check retained history and persistence after reopening. Keep working client/rendering settings. For camp, set the active ruleset's `Custom:CampTimerMs` to 29000, save, restart server and test a normal camp/relogin. No source import, runtime download, database replacement or DLL rebuild is required. A static UI footer still says 0.4.10; the verified APK manifest and native diagnostic version are 0.4.11 (cosmetic follow-up only). This final handoff update uses [skip ci]; the release itself is complete.

Additional device milestone from `logs-4187556654133198681.zip`: all 49 MSVC units linked successfully into a staged x86 DLL (1,695,232 bytes; SHA256 `556ead4699b1af797eb2c676ef40dacc92ade932526ebe551f36382de7424c28`). Prior missing-library failure is resolved by source refresh. Warning C4172 is a real source issue in the disabled custom-old-animation hook; most other warnings are repeated legacy header diagnostics. Do not characterize all warnings as harmless. User subsequently reports working gameplay; no DLL/compiler change is requested here.

---

# Thor compiler result: all 49 units compile; refresh old source to supply libraries (2026-09-17)

User supplied `logs-5989479384230296195.zip` and a screenshot of `LINK : fatal error LNK1104: cannot open file 'detours.lib'`. Read-only inspection of the bundle confirms successful SDK import on 0.4.10, completion of all 49 source compilation commands through the real Android Wine/Box64/MSVC path, and failure only at the final link step. This is new device evidence that the imported toolchain and C++ compiler work; it is not yet a successful DLL build or live-hook acceptance.

**Cause:** `control.log` records the only source import as `18141ae0c9a11813733f08fa77db986951b853d6` on September 13. That Git tree has Detours headers but no `Detours/lib/detours.lib` and no DirectX `.lib` files. The already-synced current source `main`, **4c653ca2d16aaede33b7011d07254c520ac5f5df**, contains all three required libraries at the launcher's existing search paths. Its Detours header and Release/Win32 project are byte-identical to the old snapshot. The three library archives were inspected and contain x86 COFF objects, not Git LFS pointers. This is also the exact source revision that passed the real Microsoft v142 Windows compilation job for release0.4.10.

**Immediate fix in the existing APK:** stop the game/compiler and game server, leaving the server runtime open. In **Setup → Import server**, use `https://github.com/Russianranger/Triptych-Triumvirate`, branch `main`, then **Import from GitHub**. Verify the imported source commit begins `4c653ca` (or deliberately review a later revision). Return to **Client → Build dinput8.dll on this device → Compile dinput8.dll**. The source import stores the previous source generation and rescans available SQL files; it does not import the database or rebuild/deploy server binaries. Do not select database replacement or server compilation for this retry. Keep the already-imported Microsoft toolchain and existing prefixes. After a successful build, deployment remains a separate action with the prior DLL backed up.

No new APK or toolchain download is needed for this diagnosed failure. An initially proposed launcher patch/build was dropped once the old-source dependency omission was proven. No application code or test gate changed; this handoff update uses `[skip ci]`. The missing-libXcomposite startup warnings did not prevent all49 units from compiling and are not the demonstrated cause of LNK1104. A successful final link and ROF2 hook behavior still need the device retry.

Verified current-source library SHA256 values: `detours.lib` = `526c275495912b604dbbc0d317f0e2dca0ab5b06b632df0dcd42c77fdf4f458f`; `d3d9.lib` = `d56bc70d6e8ae71594262434a3c1dee2c6dfacbfea11bc43568b8ac4e1631fc1`; `d3dx9.lib` = `d656e690f07fdf00a4e912f1a8a1587363703d180337b44f2cf00ec8bd1cbed5`.

---

# Toolchain preparation without a personal Windows/Mac setup (2026-09-17)

The user currently has only a work Mac and **cannot download/install extra tools on it**. Do not ask them to install Homebrew, Python, msitools, Wine, Windows or a VM on that computer. Added `tools/pack-client-sdk.py` and [Thor-only instructions](toolchain-without-windows.md): use the existing Termux/Ubuntu environment on the Thor once to fetch Microsoft's original VS2019/v142 and SDK19041 packages, package the existing `trasc-msvc-sdk-1` format, and import it through the current 0.4.10 APK. Compiler execution remains inside TRASC afterward. This is a one-time preparation alternative; an in-app toolchain download button is not implemented.

The helper pins/checksums the open-source downloader, retains its interactive Microsoft license prompt, forces x64-host/x86-target tools for Box64, validates 14.29 and required DLL/header/library files, and rejects wrong executable architecture or overwrite of a completed ZIP. Microsoft's live 16.11.60 catalog resolved the exact arguments successfully (973,531,982 download bytes). Three focused tests pass, including a produced fixture ZIP through the production app importer and rejection of missing CRT, wrong host/toolset and symlinks. Actual full package extraction on Android and Microsoft compilation through Wine/Box64 remain device acceptance; do not describe them as proven. No Microsoft payloads are committed or publicly distributed. Application/runtime code and the published APK are unchanged. This tooling/documentation commit uses `[skip ci]`.

---

# Release record: 0.4.10 workspace published; Thor acceptance next (2026-09-17)

**Published and verified:** **0.4.10/code27**, tested source **ad33e5ab81acad35b9a5b0f33d5015d9fde05dc4**. All seven jobs passed in [run 35162291386](https://github.com/Russianranger/trasc-server-android/actions/runs/35162291386), completed September 16 at 23:49:38 UTC. The preview publication step succeeded and the `preview` tag points to this tested source. Runs 35160338077 and 35161803496 were superseded/cancelled; they are not outstanding builds. The previous handoff stopped at "implementation in progress" and omitted the successful release. The resumed session completed verification and this documentation-only record using `[skip ci]`; it did not rebuild or replace working application code.

The authorized request is implemented: reversible persistent option5 spell filtering; source client-overlay compare, individual/bulk copy and persistent locks; player/account snapshots and staged restoration; top Client launch controls and collapsible settings; bundled fantasy backgrounds/fonts; and an experimental in-app MSVC DLL compiler. See [implementation](workspace-0410.md), [release notes](preview-notes.md), and [step-by-step device tests](workspace-testing-0410.md).

**Validation evidence:** CI logs confirm 108 backend tests, host JVM checks, browser flows, Android build/lint and preserved signing, all existing graphics/runtime gates, and actual upstream source compilation using the original Microsoft v142 toolset on Windows. The ARM64 database job completed the full pinned seed player export/stage/restore while preserving world content. Separate real MariaDB cases passed schema/default additions, account IDs, Unicode/binary/NULL values, inventory/currency/progression, repeated parcel foreign-key/cascade restoration, and rejection of orphaned, corrupt or unsupported migration data before changing live rows. Persistent spell compatibility passed export/root/Resources/ZIP checks and complete-table restoration without changing server spell rows. These are automated results, not new Thor acceptance results.

**Independent release artifact check:** downloaded CI candidate artifact **10473249043**, verified its SHA256 **40353d6b376193b7fff35f1db4187a4dd079f4e1a2468857b43395be0836405d**, and extracted the APK, corresponding native sources and signing identity. The APK and source archive sizes/hashes exactly match the current public release assets' GitHub-reported digests. All 16 packaged Python modules and all 13 UI/font/scenery assets match the tested source. Independently verified the APK v2 RSA/SHA256 signature, full signed content digest and preserved certificate. Direct public release-asset download was unavailable in this session due to a proxy timeout; verification used the authenticated Actions artifact plus GitHub release metadata instead. Do not describe that as a direct public download.

- [APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=ad33e5a): **10,453,356 bytes**, SHA256 `1272a9742a519fa6c30c566829f29739599e3c7f1898c2e3248eaa11facbe35d`.
- [Corresponding native sources](https://github.com/Russianranger/trasc-server-android/releases/download/preview/launcher-sources.tar.gz): **114,548,223 bytes**, SHA256 `84a9d41d134b25061630e6136aa9757e38e83ed0bbc29aac7bb5c96b544b1e3f`.
- Published `preview-build.json`: **368 bytes**, GitHub-reported SHA256 `7ba10e7b3d43ac72d7a47b14ac6c2b72ad94108bf249c071d2ff20e7cf6855e7`; its contents were not independently downloaded in this resumed session.
- App ID `io.github.russianranger.trasc.preview`; certificate SHA256 `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Preserve signing cache `trasc-preview-signing-v2`.

**Next:** stop ROF2 and the runtime, update in place, reopen the runtime and retain Turnip26/Balanced/exact0.4.2 NPC/native-helper/fullscreen settings and existing caches. Confirm Spell compatibility remains enabled after Export & sync, then check Skin Like Wood/Minor Healing sound and particles. Compare source add-ons and exercise one locked/unlocked individual copy before bulk copying. Save a player snapshot outside the app before replacing the database; review and restore it, then verify characters/inventory/progression and newer world content. Restore replaces the complete player dataset, not an account-by-account merge. Unknown custom tables and upstream content-ID changes can require a separate migration.

**Compiler acceptance remains outstanding:** prepare/import the Microsoft v14214.29 + Windows10 SDK ZIP using `tools/pack-client-sdk.ps1` on the user's Windows PC, then compile on the Thor. CI proves that the unchanged upstream project compiles with MSVC on Windows; it does not prove the imported compiler runs successfully through Android Wine/Box64 or that the resulting game hooks work. The game prefix is separate, output is staged, and deployment is explicit with a previous-DLL backup. Export Logs if compile or loading fails. Do not claim this device path is already tested.

Full high-ID remapping remains future work; option5 intentionally excludes those abilities from the client. Prior cold-start name corruption and the isolated late stack overflow remain under observation, with no new fix claimed. No additional implementation or repeated CI run is needed for this release unless the next device test reveals a concrete issue. Preserve the historical evidence below; do not resume older superseded comparisons.

---

# Current device result: names recover after restart on both drivers (2026-09-16)

User reports the first 0.4.9 launch had corrupt names; fully exiting/relaunching the client fixed them, and they remained correct after exiting/reopening the app and switching back to 24.3.4. User prefers 26.0.0 performance. Both uploaded bundles were inspected, including their previous-session logs. Read [the device follow-up](turnip-selection-049.md#device-follow-up-2026-09-16).

The logs independently verify real Adreno 740 rendering with Mesa26.0.0 and the later Mesa24.3.4 fallback, unchanged Balanced/exact0.4.2 NPC/native helpers, and the successful eight-ID spell exclusion in both folders. The first26 session creates a DXVK state cache; later26 sessions read22 then37 entries. The fallback reads its own7 then35 entries. Both DXVK and Mesa caches are separated by driver. This supports a cold/warm-state hypothesis, **not proof that shader compilation caused or permanently fixed the name corruption**. Shared Wine-prefix initialization is another changed condition: first launch updates it in43.958s, later launches reuse it in5.425–5.627s. Time until game launch request falls from47.792s to8.038–8.329s; these are not zone-loading times.

One real Wine stack-overflow error occurs late in the first26 session, around21:31:39UTC during graphics reinitialization after camping/character selection; frame delivery then stops. It does not recur in the three later sessions. Do not describe the first session as error-free or link that late exception to the earlier name glitch without evidence. The old missing SkinMeshCBS1_VSB.fxo warning also persists in reportedly working runs and is not sufficient to explain the names. Different scenes/durations prevent a controlled FPS comparison.

**Next:** stay on0.4.9 with26.0.0 selected, preserve caches/prefix and working settings, and check names after one full Thor reboot plus ordinary play/camp/relaunch. If the glitch or hang recurs, export Logs before more than one additional launch overwrites the previous-session evidence. No cache clearing, prefix repair, reimport, renderer/emulator retest, new APK or speculative runtime change is warranted by this result. Hardware driver selection is accepted on this device; long-term name stability and the isolated late stack overflow remain under observation. This is a documentation-only update using [skip ci].

---

# Release record: 0.4.9 selectable Turnip comparison (2026-09-16)

**Published and verified:** **0.4.9/code26**, tested source **2276f3497e1062c37aae31a5c4308a50f8cd57ad**. All six jobs passed in [run35148927421](https://github.com/Russianranger/trasc-server-android/actions/runs/35148927421). The public APK, corresponding native sources and build manifest were downloaded and checked against the tested candidate and release digests; preview tag points to the tested source. This final documentation update uses [skip ci].

User correction: their ARM64EC Winlator cannot select Box64. **Withdraw the emulator-switch request below; do not ask again.** The user requests another Turnip driver or driver selection. 0.4.9/code26 adds bundled **24.3.4 (default/fallback)** and **26.0.0 (comparison)** choices. Read [turnip-selection-049.md](turnip-selection-049.md) for implementation, source pins and device instructions. Standard Winlator Android/bionic driver ZIPs cannot be dropped into this glibc runtime; arbitrary ZIP import is not implemented. The new driver is upstream Mesa26.0.0 built for TRASC, not Winlator R5.

**Validation:** 96 backend tests, host JVM checks, Android compilation/lint/preserved signing, browser flows, real-seed database backup/restore, and all original direct/PRoot Software/VirGL/DXVK audio/model/input/shader/fullscreen/restart/strict-exit gates passed. Both real ARM64 Turnip libraries loaded independently in the unchanged runtime and retained the strict missing-KGSL rejection. CI has no Qualcomm GPU; its actual Vulkan rendering test remains explicitly Lavapipe, not device rendering with either Turnip. New unit/browser checks cover driver selection/reversion, independent caches, invalid/corrupt packages, observed-version mismatch, request propagation, saved restoration and the running-client lock. Downloaded browser screenshot reviewed. Local browser installation was unavailable due to download timeouts; required CI browser coverage passed. No gate was relaxed or failed test bypassed.

The first build35148490668 caught glslang15.1 requiring CMake>=3.27 while bookworm supplied3.25.1. Commit2276f34 pins CMake3.31.6 for the build only; the complete new run passed. No runtime dependency upgrade was needed. No physical name-fix claim. Preserve DXVK2.5.3, Wine/Box64, exact0.4.2 NPC settings and the successful spell exclusion. Default driver remains24.3.4.

Verified deliverables:
- [APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=2276f34): **9,659,110 bytes**, SHA256 `a7a312cc7ffcbd253989eb7559dbec7217f4960144ccc83fae26920dc5a5d1c6`.
- [Corresponding native sources](https://github.com/Russianranger/trasc-server-android/releases/download/preview/launcher-sources.tar.gz): **114,547,992 bytes**, SHA256 `4330b15fd63c4881c7905a38b1869a872ef0cb59ccdd286a408ff1b6d76262ff`.
- Build manifest **367 bytes**, SHA256 `910d019a9478f91d7dc6f6bcb918f35486a4df7f538c1bbff666bf49df00bfe3`.
- App ID `io.github.russianranger.trasc.preview`, certificate SHA256 `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Preserve signing cache `trasc-preview-signing-v2`.
- Candidate archive10468898035 SHA256 `b32a2f51cd5ca3651c02e7652eb28b8b0cf2032a3111a8d80138582172deae80` matches GitHub's digest. All16 packaged backend/UI assets match source; compiled driver request/deployment strings, manifest version and certificate checked. Mesa24/Mesa26/glslang/DXVK source hashes and both exact Dockerfiles/build/probe recipes checked in the source archive.
- Turnip24.3.4 SHA256 `33c1b89cfe8a56aa689dc13139d5754ba61db40c55d1d5caf19c17151ff733e3`; Turnip26.0.0 `6ec75939bc0d3dc6d8265b9c627398ba682156db32e6d4ce14474cc8060e2408`; updated probe `e5d9f6f11b471415a981d2ae7aac3566ebe7f9d687409437b15fd89aacc69d8b`. The baseline is rebuilt from unchanged source/flags and is not byte-identical to0.4.8. DXVK, audio plug and wineserver retain their previous hashes; WineD3D differs only in PE timestamp/checksum, verified after normalizing those fields.
- Local verified copies: `runtime-work/trasc-server-android-0.4.9.apk`, `launcher-sources-0.4.9.tar.gz`, `preview-build-0.4.9.json`.

**Next device test:** update in place with client/runtime stopped, then start runtime/server. Select **Client -> Turnip + DXVK -> Turnip driver26.0.0**. Keep Balanced, exact0.4.2 NPC compatibility, native helpers,1280x720 fullscreen and the successful spell exclusion. Leave verbose logging off. Observe character-selection labels, inspect the same NPCs and confirm spell effects still work; stop/export Logs. New-driver caches begin cold, so use a second same-driver run before judging FPS/loading. If it regresses, stop/select24.3.4/relaunch and export a separate log. No reimport, prefix repair, client data export or runtime download is needed. Hardware acceptance remains pending.

---

# Previous investigation: Winlator character-selection labels confirmed working (2026-09-16)

The user answers the previously outstanding comparison: **names work in Winlator**. The new `Winlator Ludashi_2026-09-16 14_43_44.mp4` visibly shows readable `Yehaos [1 Dru]` and `Greater Faydark` labels through character animation. Both settings screenshots were successfully inspected; do not ask to reattach them. Read [winlator-labels-048.md](winlator-labels-048.md).

Working Winlator selects **DXVK2.4.1-fix / Turnip26.0.0-R5 / Vulkan1.3**, compared with our **DXVK2.5.3 / Turnip24.3.4 / Wine10.0 WoW64 / Box64 0.4.4**. **The follow-up 15:21 screenshots now identify Proton11.0-1 arm64ec and FEXCore2601 / Performance for the 32-bit client. The runtime-details request is satisfied; do not repeat it.** Both new images were inspected successfully. Selected settings are not independently verified loaded binaries. Names previously failed with both our Turnip and VirGL paths, so shared Wine/Box64/configuration differences remain relevant; this does not prove a particular graphics version or emulator is the cause.

**Withdrawn comparison:** the source-based FEXCore -> WowBox64 suggestion is unavailable in the user's installed ARM64EC environment. Do not request it again or assume a result. The working Proton11.0-1/FEXCore2601/Performance configuration remains recorded, and the user has now chosen a Turnip comparison. Porting FEX/ARM64EC into TRASC would require a compatible runtime and launcher integration, not copying a DLL into Wine10.

Preserve the confirmed spell exclusion and exact0.4.2 NPC/native-helper/Balanced settings. No code, native binaries, client data, or APK changed; published APK remains0.4.8. Do not repeat the failed broad renderer/CPU/native-DLL OFF comparisons. This documentation-only update uses [skip ci].

---

# Previous investigation: spell exclusion succeeded; September 14 does not fix upstream data (2026-09-16)

**Device success confirmed by user, with installed-file evidence:** `logs-7096767172731379028.zip` proves both spell tables contained40,914 rows, max43,019 and SHA256 `034b5635049a9ef6e549f3f7d5b32f265a386d82a3fd3e8286b04d44b58a5e77` during the successful 0.4.8 run. The journal excludes exactly50000–50007. Native dinput8 and both model helpers were restored/enabled, Turnip/Balanced/exact0.4.2 NPC settings retained. User says the name glitch persists. Read [upstream-spells-048.md](upstream-spells-048.md) for full findings and source links.

**Upstream history:** inspected all five database snapshots. August4 has no IDs >=45000; August31 UTC first publishes50001–50007 (Edict of Command); September3 adds50000 (Dire Charm); September9 retains the same eight. September14 keeps every existing spell row byte-identical and adds50008–50014, for40,929 total rows and15 high IDs. No exporter range filter, client spell-capacity expansion, or remapping migration was added. The source imported on-device is18141ae; the undeployed merged fork is4c653ca, upstream content commitb14a1e7 (committed September14; author timestamp September13). Source merge/rebuild and live content deployment are distinct.

**Deployment consequence:** keep/reapply the successful exclusion on the current eight-ID installation. The0.4.8 action deliberately accepts exactlyeight IDs; it will reject the newer fifteen-ID table without replacement. A durable compatibility policy must be implemented and validated before syncing a changed database. Exclusion leaves those custom abilities absent from the client; preserving them needs consistent remapping of all references into verified unused compatible IDs. No server rows, seed, source deployment, native code or app behavior changed in this investigation; APK remains0.4.8. Do not repeat the full unfiltered export as a proposed sound fix.

**Names:** no corresponding fix found in the September14 client/character-selection changes. The videos available at this point showed in-world play, not a comparable character-selection screen. **The later 14:43:44 Winlator video now supplies that comparison and confirms working labels; see the current investigation above.** Existing missing SkinMeshCBS1_VSB.fxo warning remains an unproven cause. Do not repeat the failed native-DLL OFF, broad renderer or CPU comparisons. Preserve backups, signing identity and all gates. This documentation-only update uses [skip ci].

---

# Release record: 0.4.8 reversible spell exclusion test (2026-09-16)

**Published and verified:** **0.4.8/code25**, tested source **1b5016604e163f40d8bef5a0370c646c053aea91**. All six jobs passed in [run35135626719](https://github.com/Russianranger/trasc-server-android/actions/runs/35135626719). Public APK, corresponding native source archive and build manifest were downloaded and verified against the tested candidate and release digests; preview tag points to that source. This final documentation commit uses [skip ci].

**User lifted the filtering pause:** Winlator also has no spell sound; “Lets go ahead and try that exclusion.” Input `Winlator Ludashi_2026-09-16 13_26_28.mp4` is 10.8 seconds. Sampled frames show in-world play and Sound Volume100%; accept the user's sound report. No Winlator file hashes/logs accompanied the clip, and it does not establish the character-selection name result or independently verify identical installed data. Do not claim it proves the cause.

0.4.8 adds **Client → Apply spell exclusion test / Restore full spell files**. Both installed originals must match, and the excluded IDs must be exactly50000–50007. Every other row stays byte-exact, including spells26/200 and their sound/particle fields. Backups/journal precede replacement; cancellation/failure rolls back; interrupted changes block launch and allow Restore to finish. Restore verifies both backups before changing either destination and refuses unrelated intervening edits. Original backups remain after Restore. Every launch verifies both installed hashes/counts and records evidence in client-state.json. Normal export/Prepare retain the complete table; export/Prepare/import are blocked while the comparison is active. Server DB, generated exports, other client files and configuration stay untouched. Native submission/launch locking covers both operations. All helper code stays in the existing deployed module lists.

Validation: **93 backend tests**, host JVM/native management/audio/input checks, CI browser flows, Android compile/lint/preserved signing, real-seed database import/export/sync/restore and all original direct/PRoot Software/VirGL/DXVK gates passed. The real-seed comparison retained40,914 rows byte-exact in both folders, excluded exactlyeight IDs, restored the complete40,922-row originals and preserved the server/export data. Coverage includes cancellation, partial replacement failure, interrupted state recovery, changed files, damaged backups, ambiguous paths and isolated Android helper deployment. The local browser executable was unavailable; the required CI browser check passed and its new spell-comparison screenshot was downloaded and reviewed. No gate was relaxed or failed test bypassed.

Verified deliverables:
- [APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=1b50166): **6,620,757 bytes**, SHA256 `01e069fa842defbeb429883db67f4fe411325b56adb9d226deb3447661f8f5be`.
- [Corresponding native sources](https://github.com/Russianranger/trasc-server-android/releases/download/preview/launcher-sources.tar.gz): **66,973,617 bytes**, SHA256 `92f96e7c1164346276a69530394a510a8c8f75c7f4305fc5ab6e1873ee29e530`.
- Build manifest **367 bytes**, SHA256 `245596dc9d5ae826578c5b9e55a1b64e6ec6fa3760f68bf60250cf7b54c1a320`. App ID `io.github.russianranger.trasc.preview`, signing certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`; preserve cache `trasc-preview-signing-v2`.
- All16 packaged backend/UI assets match source; compiled Apply/Restore/export launch interlock and journal path checked. Candidate archive10463565386 SHA256 `79c2a051f7336b3ef91222d6d22c39bc37fe1b255caef73139dd669b1344db7a` matches its GitHub artifact digest.
- Native source pins and recipes are unchanged and the corresponding source archive was verified. Audio plug, wineserver, DXVK and Vulkan probe retain0.4.7 byte identities. WineD3D `c1b1cf94bee8e7ae36e88c489dcfeb7140d98e8f200b7631224b6c075d820967` differs only in PE timestamp/checksum. Rebuilt Turnip24.3.4 `acf9825ae74e9c830831230788527c4484d8cec9c0bc72087ca9c17199e16199` is not byte-reproducible; do not claim all rebuilt binaries are byte-identical.
- Local verified copies: `runtime-work/trasc-server-android-0.4.8.apk`, `launcher-sources-0.4.8.tar.gz`, `preview-build-0.4.8.json`.

**Next device test:** stop client and server runtime, update in place, open server runtime, then **Client → Apply spell exclusion test**. Expect8 excludedIDs and40,914 rows in each folder. Keep native dinput8, native model helpers, Turnip+DXVK/Balanced/exact0.4.2 NPC compatibility/1280×720fullscreen. Enable Sound diagnostics and leave verbose diagnostics off. Check character-selection names, then Skin Like Wood and Minor Healing particles/sound separately. Stop client and export Logs **before Restore**, then Restore full spell files and disable Sound diagnostics. Do not use the excluded high-ID abilities during this comparison. No export/reimport/runtime download/prefix repair is needed. [Full instructions](preview-notes.md).

**Device outcome is pending.** This is an authorized comparison, not a verified fix for spell effects or name corruption. Preserve working model settings and do not repeat the failed native-DLL bypass or broad renderer/CPU tests. The earlier pause/file-request instructions below are historical and superseded. No more client asset uploads are needed for this test. No subagents unless explicitly requested. Future GitHub writes must match the exact staged tree; final documentation commits must wait for workflow completion and use [skip ci].

---

# Previous investigation: supplied assets inspected; filtering was paused (2026-09-16)

User supplied **all four requested files** in `trasc_launcher.zip`. Read [client-assets-047.md](client-assets-047.md) before continuing. Do not request them again or import this ZIP as a whole client. Read-only inspection found 675 complete EFF records, 2,577 EDD records, all 2,928 nonzero emitter references in range, and all nonzero sound IDs mapped. Animations 216/278 both specify casting `SpelCast.WAV` and landing `SpelGdHt.WAV`, with emitter minimum levels zero. The earlier device inventory proves the casting WAV is packed in snd2.pfs; it does not specifically inventory the landing WAV or the referenced DDS textures.

Actual uploaded DLL SHA256 `0a59be5dbd0c746ecd4168d3628b056dc490e0eabbaa6050c3d533802b13465b` has x86 PE metadata dated August 12, imports D3DX9_35, and contains nine spell lookup helpers bounded below 45,001, indexing the expected SPELLMGR table. This strengthens the high-ID hypothesis using actual binary evidence; it does not prove effect-loader failure or a fix. The supplied DLL is not verified against the current source revision. No client/game bytes, app code or native binaries changed; published APK remains 0.4.7. Proprietary uploads and disassembly stay outside the repository.

Next proposed action is a **single backed-up client-only spell-table comparison**, omitting the eight installed IDs 50000-50007 from root/Resources test copies, keeping all 40,914 supported rows byte-exact and the full export/server DB intact, then restoring the full copies. The user explicitly paused filtering; request lifting that pause before implementation/application. The detailed scope, expected hashes and acceptance checks are in the new report. No more files or broad renderer/CPU/audio changes are needed before deciding on that test. Name rendering remains unresolved. Keep native dinput8 enabled and do not repeat the failed bypass. This documentation-only update uses [skip ci].

---

# Previous investigation: native-DLL bypass did not fix names (2026-09-16)

Latest input `logs-8247054629101021827.zip` and `TRASC Server Preview_2026-09-16 11_00_54.mp4` completes the proposed bypass test. User reports it is worse. The video shows continued repeated/extruded yellow selection labels and a shortened `Ye` character-list entry. Full Wine trace proves `D:\\DINPUT8.dll` loaded as **builtin**, so the imported native add-on was bypassed. The state summary missed this because its builtin matcher requires a Windows system-folder path; do not mistake `system_dinput8_loaded:false` for no builtin load. Details and the diagnostic follow-up are in [diagnosis-047.md](diagnosis-047.md#completed-native-dll-bypass-comparison).

**Restore Load the imported native dinput8.dll for normal play. Do not repeat the OFF test.** Existing 0.4.7 remains current; no code, native binaries or game files changed and no new APK/fix is claimed. Keep Turnip/Balanced/exact 0.4.2 NPC compatibility/model helpers. Both installed spell hashes still match the verified full export; do not repeat export/reset/reinstall. Filtering remains paused. The actual `dinput8.dll`, `spellsnew.edd`, `spellsnew.eff`, `soundassets.txt` are still needed to inspect the installed add-on/effect mappings; logs contain only presence/sizes. This result does not identify the shared name-rendering fault, prove all add-on interactions irrelevant, or test casting without the add-on. Record future evidence rather than proposing more broad setting changes. This documentation-only update uses [skip ci].

---

# Previous investigation: 0.4.7 verified export (2026-09-16)

User reports spell sounds and name corruption persist after0.4.7 and asks to proceed with troubleshooting. Read [diagnosis-047.md](diagnosis-047.md) before further changes. Latest input `logs-6881254485975500908.zip` proves the real exporter connected to local triune and copied fresh spell data to BOTH installed folders: source/root/Resources all SHA256 `4762679840e788b016d83b6651866690dd344b119ff49cc4d953b292e06d7110`. All40,922 rows, including50000–50007, remain; filtering is still paused. Export/copy failure is ruled out for this run, not content compatibility.

No app/native changes or new APK in this investigation. The native-dinput8 OFF comparison proposed here is now completed; see the current investigation above. Need actual dinput8.dll/spellsnew.edd/spellsnew.eff/soundassets.txt from the imported client (or unchanged original ZIP) to inspect effect mappings and identify the installed add-on; current logs contain only asset presence/sizes. Keep uploaded proprietary bytes private. Do not repeat failed renderer/CPU/direct-mapping tests, re-enable filtering, or replace sound/particle assets speculatively.

The current published APK remains the verified0.4.7 below. This documentation-only update uses [skip ci].

---

# Active handoff: 0.4.7 automatic unfiltered client sync (2026-09-16)

**Published and verified:** **0.4.7/code24**, tested source **546e1fca2dbd77d29dc52bb8d506b7a2cae49961**. All six jobs passed in [run35112375539](https://github.com/Russianranger/trasc-server-android/actions/runs/35112375539). Public APK, corresponding source archive and build manifest were downloaded and verified against the tested candidate and release digests; preview tag points to that source. This final handoff update skips CI.

**User correction:** hold off on spell filtering; the client data may never have been copied into the imported local client. Export must automatically overwrite all four files in **both root and Resources**. This instruction supersedes the 0.4.6 filter/Prepare guidance below. Do not resume filtering without new authorization/evidence.

0.4.7/code24 implements that request. `export_client` generates the full data ZIP and installs all eight local copies with original-file backups and rollback. Existing Windows-style filename capitalization is preserved; ambiguous paths, symlinks and non-file destinations fail before replacement. Exclusive temporary files protect staging. No imported client still produces a ZIP. The local transaction finishes before Android opens the ZIP picker, and cancelling that picker keeps a clear local-copy success message. Prepare uses the same transaction but also sets login/display; it does not copy twice.

The native submission/launch interlock now includes export_client. The active export and Prepare paths never call the dormant `client_spells.prepare_export` filter. Read-only installed-table/particle diagnostics remain available; their hypothetical compatibility counts must not be described as rows actually removed. `client-spell-export.json` now explicitly records filtering disabled; `client-data-sync.json` retains file hashes, completed-copy status and backup path. No GPU/CPU/audio or game asset changes.

Local validation: 86 backend tests and host-JVM management/audio/input checks pass. New regression coverage checks exact bytes (including high IDs), all eight overwrites/backups, case handling, missing Resources, absent client, rejected destinations, cancellation rollback and injected partial-copy failure. Existing Prepare and isolated Android helper-copy tests remain. CI's real-seed database gate now exercises the production export/sync/ZIP paths with all 40,922 serialized spell rows unchanged. Browser coverage tests normal ZIP save and cancellation after local sync. **All gates passed:** browser/Android lint/signing, full-seed database and original native audio/input/models/shaders/display/prefix/restart/strict-exit checks, including direct and PRoot Software/VirGL/DXVK. The real-seed test copied all 40,922 rows byte-exact into root, Resources and ZIP while preserving the server table and both original client files.

Verified deliverables:
- [APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=546e1fc): **6,617,113 bytes**, SHA256 `d503204e43ec4a56699f8638e2b4f025c4413ddf8d51cf70af869bde781c4d42`.
- [Corresponding native sources](https://github.com/Russianranger/trasc-server-android/releases/download/preview/launcher-sources.tar.gz): **66,973,611 bytes**, SHA256 `91c3322fef57e02aa95e9f003d30b7ddad63fb1975ef4bc7ff55c883808a8413`.
- Build manifest367bytes, SHA256 `7286cf07f1bbfb908a07f91769555a07335fa3746274afb384dcc7fd8437838c`. App ID `io.github.russianranger.trasc.preview`, preserved certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`, signing cache `trasc-preview-signing-v2`.
- Local verified files: `runtime-work/trasc-server-android-0.4.7.apk`, `launcher-sources-0.4.7.tar.gz`, `preview-build-0.4.7.json`. All16 packaged backend/UI assets match source; compiled export-launch interlock and runtime helper-copy string checked. Candidate archive10453156787 SHA256 `290c8826d41be3b564b2f52ed2664e365e59cb9924625392f1dadef978efd6d6` matches its release artifact digest.
- Native source/recipes remain pinned. Rebuilt Turnip24.3.4 SHA256 `ccfb6efed732f9489c1a90a61a9494495d6150664848946795cb6628fe157c63`; WineD3D `df60d65d6a9ac8821f190f44f9a04bfd9aa2b839217e3b46de5d2ff83b275e30` differs from0.4.6 only in PE timestamp/checksum. Exact Mesa/DXVK source checksums and recipes verified. Audio plug, DXVK, Vulkan probe and wineserver retain their prior byte identities. Do not call all rebuilt native binaries byte-identical.

Preserve app identity/signing cache and all original test gates. No new backend module or native recipe. Keep exact 0.4.2 NPC settings and Balanced. Do not reset/reimport the client or runtime. No subagents unless explicitly requested. Future GitHub writes must match the exact local staged tree; final documentation commits must wait for workflow completion and use [skip ci].

Next device test: stop client, install, open server runtime, **Server → Export & sync client data**, wait for both-folder overwrite confirmation, save or cancel ZIP, then launch and test spell particles/sound. No separate Prepare required. See [preview-notes.md](preview-notes.md). Name corruption remains unresolved; no physical effect fix is claimed.

---

# Release record: 0.4.6 ROF2 spell-table compatibility (superseded by 0.4.7)

**Published and verified:** **0.4.6/code23**, corrected source **079de26e8a1df1d134d89eb9e9d8f9e975646c94**. All six jobs passed in [run35108416908](https://github.com/Russianranger/trasc-server-android/actions/runs/35108416908); public APK/source/manifest match the tested candidate byte-for-byte. Preview tag points to that source. This final documentation update skips CI.

Version0.4.6 addresses a concrete client-data defect after the user clarified that Skin Like Wood and Minor Healing have **neither particles nor sounds**. The original pinned seed has exactly 40,922 spell rows (matching the device export log), including eight IDs 50000–50007. ROF2's effect loader has a developer-confirmed failure with out-of-range IDs. Read [spell-effects-046.md](spell-effects-046.md) and [device-followup-045.md](device-followup-045.md).

The new `client_spells.py` validates the complete generated table and filters IDs >=45000 **only in client exports**. Every supported row remains byte-exact. The complete export remains in `server/export/spells_us.unfiltered.txt`; server database rows are untouched. `Prepare client for this server` uses the filtered table in root and Resources with the existing original-file backups/rollback. Logs retain counts, IDs, hashes and numeric effect fields for spells26/200. Opt-in sound diagnostics also check both installed tables and allowlisted particle settings. No game/DLL/sound asset replacements or GPU/CPU/audio engine changes.

**Applying the fix requires Prepare once after installing.** Keep the existing native add-on/model helpers, Turnip + Balanced + exact0.4.2 NPC compatibility, 1280×720 fullscreen. Stop client, open the server runtime, Prepare, then launch and test both particles and sounds for the two spells. Observe character-selection names without changing other settings. No runtime download, prefix repair, reimport or server rebuild. Name corruption persists across both previous renderers; this build does not claim its root cause is fixed. If it persists after the spell-table correction, the next independent isolation is a brief native-dinput8-disabled selection-only test, restoring it before play.

78 local backend tests pass. A seed-derived table with 40,922 rows validates:8 excluded,40,914 retained,max43019; spells26/200 animation216/278 preserved. CI now additionally requires actual MariaDB serialization of the pinned full seed to give those exact results and preserve all server rows. Full Android/browser/signing/database/native and direct/PRoot Software/VirGL/DXVK gates passed, including strict exits, input, audio restart, models and display checks. Preserve app ID/certificate/signing cache; no subagents unless explicitly requested. GitHub writes require exact staged-tree comparison. The public release now contains the corrected0.4.6 build.

Deployment correction: initial source86dc3d7 omitted the new helper from both Android runtime-copy lists. Corrected source **079de26e8a1df1d134d89eb9e9d8f9e975646c94** adds it to RuntimeManager and ClientRuntime and adds a regression executing from exactly those copied files in isolation. New backend modules must be checked at this deployment boundary, not only inside the APK. Corrected CI **35108416908** passed all six jobs. The earlier workflow35106364997 published just before cancellation; the bad APK hash05d4cfe... was temporarily published, then withdrawn by separate recovery run35108771623, restoring verified0.4.5 bytes/tag/notes. Always inspect actual release assets after cancellation. Do not deliver the rejected `/tmp/trasc-046-candidate` files; corrected files are in `/tmp/trasc-046-fixed-candidate`, with metadata `/tmp/trasc-046-fixed-verified-candidate.json`.

Verified deliverables:
- [Corrected APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk?build=079de26):6,616,053bytes, SHA256 `fbdf7a8ad7e1a720b4de9574af8fb07235ba4279f72d1eb2b9268da2560b7a35`.
- [Corresponding native sources](https://github.com/Russianranger/trasc-server-android/releases/download/preview/launcher-sources.tar.gz):66,973,639bytes, SHA256 `bff155b0c1d37c64a2485ec310fb93a3de6bedc986e992e2776852ee3069a068`.
- Build manifest367bytes, SHA256 `1d3575f38499677a4059349db23c4ed5f90e3ba286edbb450492a61797a8f7dd`. App ID `io.github.russianranger.trasc.preview`, preserved certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`, signing cache `trasc-preview-signing-v2`.
- Local verified copies: `runtime-work/trasc-server-android-0.4.6.apk`, `launcher-sources-0.4.6.tar.gz`, `preview-build-0.4.6.json`. All16 packaged backend/UI assets and the compiled helper-copy string checked. Do not confuse with the rejected first0.4.6 candidate, which has the same version/code but a different hash/source.
- Native rebuilds use unchanged pinned source/recipes: Turnip24.3.4 SHA `e76d35a6b2d63224dd83e9d84008257db8ddf0b03211cd3d0101eaef60f3440f`; WineD3D SHA `4e7c9c503181f64129cd89bfc2ed58dfdae1c8ad60164d1a68a481b31891b6a7`. WineD3D differs from0.4.5 only in PE timestamp/checksum; Turnip is not binary reproducible. Exact Mesa/DXVK source checksums and packaged recipes verified. Audio plug, DXVK, Vulkan probe and wineserver identities retain the prior values. Do not claim all graphics binaries are byte-identical.

**Next: device acceptance with the corrected table.** Installing alone does not change client spell data. Prepare once, then test particles and sound for both spells and inspect the selection name. Sound diagnostics captures actual root/Resources table IDs and particle settings; stop/export after the short test, then turn it off. A physical spell/name success claim must wait for that result.

---

# Release record: 0.4.5 diagnostic follow-up (2026-09-16)

**Published and verified:** **0.4.5/code22**, source `49e26507c6b04968bdcf2b4fd999f8b5f5717a3f`. All six jobs passed in [run35095063228](https://github.com/Russianranger/trasc-server-android/actions/runs/35095063228), including browser/Android lint/signing, database, native audio, direct/PRoot Software/VirGL/DXVK, original pixel/model/input/shader/720p/prefix/restart and strict-exit gates. Public APK, matching sources and build manifest were downloaded and verified against candidate artifacts/release digests. Preview tag points to the tested source; this documentation update skips CI.

- [APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk): **6,613,320 bytes**, SHA256 `93f29605c371309d0ce61643542145fca83385d98c2f8945dc71d3c02ba319fb`.
- [Corresponding native sources](https://github.com/Russianranger/trasc-server-android/releases/download/preview/launcher-sources.tar.gz): **66,973,613 bytes**, SHA256 `9b930f7f2ff56a743e2bfb90741d667457ddb938d50b0848601f9436db46ae6d`.
- Build manifest367bytes, SHA256 `b7caad843ac8969c5abeb4e32b13bde742c85ab3960b301f0cdf3b4b8b6ddae1`; source/app/version/certificate match. Local verified copies: `runtime-work/trasc-server-android-0.4.5.apk`, `launcher-sources-0.4.5.tar.gz`, `preview-build-0.4.5.json`.
- Preserve app ID `io.github.russianranger.trasc.preview`, certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869` and signing cache `trasc-preview-signing-v2`; never regenerate.

Latest user tests reject Legacy math accuracy: music, sit/stand and swing work, spells do not, names still corrupt, FPS worse. Use Balanced + exact0.4.2 NPC compatibility. Do not repeat failed CPU/direct-mapping tests or claim synthetic API fixtures reproduce the game.

Read [audio-and-labels-045.md](audio-and-labels-045.md) for uploaded evidence and exact changes. This release fixes evidence collection: bounded PFS spell-asset inspection and separately retained audio lifecycle samples that mixer-log rotation previously erased. It does not change playback/rendering source or claim a spell/name fix. The next required evidence is **one Turnip spell test with diagnostics, then a short VirGL character-selection comparison**, exporting each and returning to Turnip/quiet logging. See [preview-notes.md](preview-notes.md). No prefix repair, runtime download, reimport or server rebuild.

71 backend tests and all packaged backend/UI assets verified. Native rebuild identities: Turnip24.3.4 `1b3520b2492b350aa7f3348fb711d663490105b1d109076be3878920978f79ae`; WineD3D `a3179ef426d4084b43d5cb3ece2844a3d27dcda5a4153c3ad95bbfada16ff78d`. Both are rebuilt from the unchanged pinned source and recipes; do not call the APK's graphics binaries byte-identical to0.4.4. WineD3D differs only in PE timestamp/checksum, verified by normalized comparison. Turnip is not a reproducible binary in this build; exact Mesa/DXVK source checksums and packaged build recipes were verified. DXVK, Vulkan probe, audio plug and wineserver remain byte-identical. No runtime-image/source upgrade was requested; existing installed runtime remains supported. No subagents unless explicitly requested. Compare exact staged tree before GitHub writes.

---

# Active handoff: 0.4.4 NPC recovery and effects/name diagnosis (2026-09-16)

**Published and verified: 0.4.4/code21**, tested source `aed9fa333bdded3b8affa42b0e0ba5404b8dcf5e`. All six jobs passed in [run 35089866735](https://github.com/Russianranger/trasc-server-android/actions/runs/35089866735). Public APK, corresponding sources and build manifest were downloaded and matched to the tested artifacts and GitHub asset digests. Preview tag points to this tested source; this final handoff commit uses `[skip ci]`.

New device test confirms **music works** with 0.4.3 but spell effects remain silent. Its direct-buffer mode regresses NPCs and does not fix the name glitch. **0.4.2 NPC mode is still confirmed working.** Do not carry forward the old UI/summary claim that labels were fixed. Read [audio-and-labels-044.md](audio-and-labels-044.md) and [preview-notes.md](preview-notes.md).

0.4.4/code21 restores `compatibility` to exact 0.4.2 settings and retains its explicit alias. `direct_043` is the rejected comparison, not recommended. Working AudioTrack changes are retained. New experimental `accurate` CPU math profile is independent of GPU settings. Add focused opt-in Windows sound tracing and bounded read-only sound inventory/settings reports; no asset replacement or prefix/runtime reset. Existing app ID/signing key must be preserved.

New tests isolate mono/8-bit/22.05 kHz positional DirectSound output from preceding music and require nonzero PCM independently in both profiles; actual PE32 x87 precision test runs on the pinned ARM64 Box64. All original gates remain strict, including old synthetic live-buffer positive/negative controls. Local backend/JVM/Java compilation pass; CI browser/native/device boundaries documented. **Spell sounds and actual name geometry remain unconfirmed; call this a recovery/diagnostic build unless on-device evidence changes that.** Next device test is two matched short sessions (Balanced, Legacy math accuracy), NPC compatibility recommended, music 0/sound 100, focused sound tracing on and general verbose off; export after each.

No subagents unless user explicitly asks. GitHub writes must match the local staged tree. Preserve signing cache `trasc-preview-signing-v2` and certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`.

- [APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk): **6,610,624 bytes**, SHA256 `60170918b06b893d8b52e38c24a6aecdf79381e1ce27928092f67531a5a4c1c5`.
- [Corresponding native sources](https://github.com/Russianranger/trasc-server-android/releases/download/preview/launcher-sources.tar.gz): **66,973,618 bytes**, SHA256 `c3e071c244b34b13d4ff77d009e3171f0e0f94bb756d7dda209e302210917142`.
- Build manifest SHA256 `c75d3bd2ac76da3b70d36cf1ce61d5eaa44c21445aba67aec324ffdb74ca3195`; application ID remains `io.github.russianranger.trasc.preview`.
- Verified local copies: `runtime-work/trasc-server-android-0.4.4.apk`, `launcher-sources-0.4.4.tar.gz`, `preview-build-0.4.4.json`.

Final validation: 69 backend tests; browser flows; JVM archive/controller/RFB/audio checks; Android compilation/lint and preserved signing; database backup/restore; native ALSA rate conversion/pause/reconnect; all direct/PRoot Software/VirGL/DXVK model/shader/pixels/fullscreen/input/CPU-affinity/quiet-log/warm-prefix/audio-restart/strict-exit gates. The new positional effects deliver independent PCM in both profiles on every runtime path; actual translated x87 precision passes. Downloaded Vulkan evidence gives nonzero deltas 143954/143965 (Balanced/Accurate direct), 143953/143966 (PRoot). Original staged/direct buffer controls still require exact 42/0 and pass; **they did not reproduce the user's actual name bug**. APK version/code, compiled option strings, all 15 backend/UI assets, icon, native checksums and source archive were checked. No assertion relaxed or test blindly retried. Physical Thor spell/name acceptance still pending.

---

# Active handoff: 0.4.3 audio startup and character-label regression (2026-09-16)

**Published and verified: 0.4.3/code20**, source `5dfcae27403df33f26f928f9a56d7ce3457e3cea`. All six jobs passed in [run 35080301081](https://github.com/Russianranger/trasc-server-android/actions/runs/35080301081). Downloaded public APK, native-source archive and build manifest match the tested artifacts and GitHub digests. Preview tag remains on the tested source; this final handoff commit uses `[skip ci]`.

- [APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk): **6,606,936 bytes**, SHA256 `c63965e6b43ed4c0b0379549d5be0ab17da51b5144e039cc54cc6f0f7dde6864`.
- [Corresponding native sources](https://github.com/Russianranger/trasc-server-android/releases/download/preview/launcher-sources.tar.gz): **66,973,643 bytes**, SHA256 `f4a181d700ccef4a01903988838fb0a83fa453244b0b8ea3718967fd45fcbe18`.
- Application ID `io.github.russianranger.trasc.preview`; signing certificate SHA256 `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Preserve signing cache `trasc-preview-signing-v2`; never regenerate it.
- Local verified copies: `runtime-work/trasc-server-android-0.4.3.apk`, `launcher-sources-0.4.3.tar.gz`, `preview-build-0.4.3.json`.

User confirms 0.4.2 NPC compatibility fixes models, but sound remains silent and character-selection name labels twist/duplicate. Attachments: logs-3726963507059387515(1).zip and September 16 03:40/03:44/03:47 videos, analyzed in ../analysis-042. Last exported session is Standard, previous compatibility. This is rendered name geometry, not keyboard entry. Both audio sessions reached Android with granted focus/nonzero media volume but stopped at tiny all-silence buffers (480 and 1920 frames). Prior logs did not record actual AudioTrack buffer limits/head, so a physical-device root-cause claim remains premature.

0.4.3 bounds the Android write buffer to Wine's producer ring and lowers the API 31+ startup watermark, preventing a concrete producer/sink sizing deadlock. Reset/start/routing-size changes reapply this policy. Actual Android playback head remains the clock; no PCM is fabricated or discarded. Bounded logs now expose buffer/start sizes, written/played frames, nonzero samples, underruns and play state. Legacy MIDI remains unimplemented.

NPC + name compatibility keeps strict float math and sampler specialization but restores direct dynamic-buffer updates. The exact old configuration is still available as **NPC compatibility (0.4.2)** /`compatibility_042`; Standard remains unchanged. Do not claim this new setting preserves the observed model fix or resolves the exact proprietary ROF2 screen before Thor testing. Keep the old mode as fallback.

Validation: 66 backend tests, Android Java compilation/lint and preserved signing, JVM audio/protocol/archive/input tests, browser flows, native ALSA conversion and pause recovery, database backup/restore, and all direct/PRoot Software/VirGL/DXVK client gates including real Windows sound and stop/relaunch, shader/model/input/720p/fullscreen/strict exits. The JVM watermark regression reproduces the old stalled 1920-frame producer and requires progress after sizing at 480/1920/3840 frames and repeated underruns. Real PE32/DXVK label regressions, direct and PRoot, require stale pixels/exit 42 with old staged mapping and all live pixel updates/exit 0 with the corrected configuration. Original success/negative-control checks remain strict; no SIGKILL acceptance or blind retry. APK manifest/signing identity, all packaged backend/UI assets, new audio policy class, native hashes and source archive were independently checked.

**Next: Thor acceptance.** Stop client/runtime and update in place; confirm 0.4.3. Start runtime at the top, then the server. Keep Turnip + DXVK /1280×720 /Fullscreen /Balanced /Automatic /available cores/native helpers, with verbose logging and shadows off. Explicitly select **NPC + name compatibility** (last user run was Standard) and enable Game audio. Observe character-selection labels for 30 seconds, then previous NPCs in the world; note FPS. Test known spell/combat effects with media/game volumes raised, then complete client stop/relaunch and test again. Stop and export Logs immediately. If models regress, compare **NPC compatibility (0.4.2)** and retain both session logs/video. No runtime download, prefix repair/reimport, reset, server rebuild or proprietary asset replacement is required.

See [audio-and-labels-043.md](audio-and-labels-043.md) for evidence/source references and [preview-notes.md](preview-notes.md) for user testing. No subagents unless explicitly requested. GitHub writes use exact staged-tree comparison before updating main.

---

# Active handoff: 0.4.2 audio and NPC compatibility (2026-09-16)

**Published and verified:** **0.4.2/code19**, source `b85246dbd2b255d9d4bb9e45cca647bb7ff9b4b1`. All six jobs passed in [run 35044510331](https://github.com/Russianranger/trasc-server-android/actions/runs/35044510331). Public APK, corresponding native sources and build manifest were downloaded and matched to the tested artifacts and GitHub asset digests. The preview tag points to this tested source; this handoff update uses `[skip ci]`.

- [APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk): **6,605,684 bytes**, SHA256 `49ebbc7f37948096eeb91b3d8faeaf44c98f35d6821b470704dec5e06e689160`.
- Application ID `io.github.russianranger.trasc.preview`; preserved signing certificate SHA256 `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Keep signing cache `trasc-preview-signing-v2`; never regenerate it.
- [Corresponding native sources](https://github.com/Russianranger/trasc-server-android/releases/download/preview/launcher-sources.tar.gz): **66,973,623 bytes**, SHA256 `fbdfc960fb9d54e4d54a90f010facaeec0f056ad441c2cb7e4a01d9bcd5b4f8c`. Includes the new MIT ALSA endpoint and build recipe alongside existing Wine/Mesa/DXVK sources.
- Local verified copies: `runtime-work/trasc-server-android-0.4.2.apk`, `launcher-sources-0.4.2.tar.gz`, `preview-build-0.4.2.json`.

User finds 0.4.1 FPS/loading acceptable. Latest requests and attachments cover NPC texture glitches, missing sound, accessible runtime controls and fantasy TL icon: logs-1971474319175552093.zip and September 15 19:31:50 video, analyzed in ../analysis-041. The video shows intermittent flat-colored hair/skin on Guard Sunblaze. A failed skin-effect log entry does not establish the cause. See [audio-and-npc-042.md](audio-and-npc-042.md) for findings, protocol, boundaries and icon prompt; [preview-notes.md](preview-notes.md) contains the device acceptance sequence.

0.4.2 adds a bounded private ALSA→AudioTrack PCM bridge enabled by default, a reversible strict DXVK NPC compatibility option, missing-effect presence/hash diagnostics, sticky top runtime Start/Stop/status, and a fantasy TL adaptive launcher icon. The circular-mask review caught clipping and increased the icon inset to 20%; the whole monogram now fits. Android playback uses actual AudioTrack progress and handles partial nonblocking writes, focus, bounded buffers and shutdown. MIDI synthesis remains unimplemented. Preserve the existing wineserver cleanup patch and exact-path overlay, installed rootfs, game files and prefix. No runtime redownload, reimport, repair or server rebuild is required.

Validation: 66 backend tests; JVM protocol/input/archive tests; browser action, persistence and sticky-control checks; Android compilation/lint/preserved signing; database backup/restore; real native ALSA conversions at 48000 stereo, 22050 mono and 44100 stereo with a deliberate 120ms scheduling pause; actual PE32 waveOut/DirectSound through Wine/Box64, direct and PRoot, including stop/relaunch; original strict normal-exit, texture/shader, animated-model, framebuffer/input, fullscreen 1280×720, Software/VirGL/DXVK, prefix reuse and quiet-log gates. APK payloads, adaptive icon, native library identities/hashes and corresponding native sources were independently checked. No exit assertion was relaxed and no failure automatically retried.

**Next: Thor acceptance.** Update over the installed app after stopping both runtimes. Start runtime from the top bar, then start the server. Keep Turnip + DXVK /1280×720 /Fullscreen /Balanced /Automatic /available cores/native helpers, verbose logging and shadows off. Enable Game audio and NPC compatibility. Check spell/combat/UI sounds with Android media and game sound volume raised, then repeat after a complete client stop/relaunch. Compare the same Guard Sunblaze view for 30–60s in Compatibility and Standard, noting FPS. Export Logs immediately after stopping; include a short video labeled with each NPC mode. Physical-device audible output and the exact NPC flicker remain unverified; do not claim either is fixed before that test. Native CI validates sample delivery, not a Thor speaker. Older MIDI zone music may still be silent.

## 0.4.2 build history

First candidate d014c86/run35042509174 passed Wine/database builds and compiled the audio plug, but the native conversion test rejected rate changes with EINVAL. Local tracing reproduced the exact failure before the endpoint callback for 22050/44100 Hz (48000 stereo reached it). ALSA's conversion plugins need an mmap-capable slave; the endpoint advertised only RW. Added MMAP_INTERLEAVED alongside RW, letting ALSA use its local ring and invoke the existing transfer callback at commit. All three rates now reach the endpoint setup locally (socket execution remains prohibited there). Also corrected drain's already-DRAINING state handling and reset counters after flush. Keep every sample, timing and exit assertion; full ARM64 retest pending.

Source78e2b94/run35043044888 passes the real native ALSA test at 48000 stereo, 22050 mono and 44100 stereo: each delivers exactly24000 output frames with nonzero left/right samples, ~0.5s timing and no receiver errors. Browser flows and rendered top controls pass. Added a concrete scheduling-pause safeguard: use ALSA's large boundary for the playback head, rather than modulo the small ring, so a fully drained buffer cannot be mistaken for no progress after a pause. The native fixture now pauses120ms midstream and still requires complete samples and timing. Nonblocking drain waits for completion with a bound; EAGAIN is not accepted as completed playback. Added the Windows audio fixture to the VirGL test directory as well. Full Wine/API/PRoot/Android final run pending.

The same78e2b94 run also passed Android compile/lint/preserved signing and the complete direct Wine suite, including actual waveOut and DirectSound PCM and a full stop/relaunch. Downloaded verification artifact10426262232: first stream24000 frames/47988 nonzero samples, second40315 frames/47860 nonzero samples, both stereo, no receiver errors. Warm Windows audio also exits0 and prints its completion marker. This is CI sample delivery, not a physical-speaker claim. Superseded by the clock-pause safeguard candidate before final publication.

---

# Active handoff: 0.4.1 fullscreen and gear controls (2026-09-15)

**Published and verified:** **0.4.1/code18**, source `8c9a9526b6761f4e62d6224784080cf2d3f45c3f`. All six jobs passed in [run 35026495250](https://github.com/Russianranger/trasc-server-android/actions/runs/35026495250). Public APK, matching source archive and build manifest were downloaded and verified against the tested artifacts and release digests. The preview tag points to that tested source; this final handoff update uses `[skip ci]`.

- [APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk): **6,544,545 bytes**, SHA256 `b35ea616596c86093d6d010384d7005eae1d2143a108cb19ee0369b02b2f1948`.
- Application ID `io.github.russianranger.trasc.preview`; signing certificate SHA256 `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Preserve signing cache `trasc-preview-signing-v2`; never regenerate it.
- [Corresponding sources](https://github.com/Russianranger/trasc-server-android/releases/download/preview/launcher-sources.tar.gz): **66,970,126 bytes**, SHA256 `d1fae5a3e1993d05fe575e989865a6abff94eb972fb04e219d803fa4e5a72ebe`. Contains matching Wine source, cleanup patch and Bookworm build recipe.
- Bundled Wine server: `translated-exit-grace-v1`, SHA256 `09933d17ab07fe9c79245b22c9b8f4ff3fbc42d237595bb693271d168c8cb7d3`. Android binds its prepared private executable onto `/opt/wine/bin/wineserver`; startup hashes that actual preferred path. Wine 10 checks its adjacent binary before WINESERVER, so an environment override alone is insufficient. The server overlay preserves the installed rootfs and prefix.

Final validation: 63 backend tests, browser/JVM/archive checks, Android compilation/lint/signing, database backup/restore, complete direct and PRoot runtime suites, actual 1280×720 fullscreen DXVK framebuffer/pixels/models/input, Software and VirGL recovery paths, original/patched legacy shader controls, prefix reuse and quiet logging. The controlled ARM64 exit comparison requires stock SIGKILL (-9, 1.667s, incomplete cleanup), patched exact 0/23 (3.071/3.073s, completed 2s cleanup), and explicit Stop (2.286s). Every original normal-exit assertion remains strict. The test-only native delay is confined to a disposable CI container and is absent from the APK. APK manifest, all 14 backend/UI assets, compiled gear resource, native bundle hashes and corresponding sources were independently checked. Physical Thor gear/IME behavior, real ROF2 fullscreen and device performance remain the next acceptance test.

Local verified copies: `runtime-work/trasc-server-android-0.4.1.apk`, `runtime-work/preview-build-0.4.1.json`, `runtime-work/launcher-sources-0.4.1.tar.gz`.

User confirms 0.4.0 is substantially better and requests fullscreen 1280×720 with Back/Keyboard/Esc in a collapsible transparent top-right gear. Provided first-run video and `logs-4814310534970933887.zip`; extracted locally under `../analysis-040`. See the new top section of `client-performance.md` for exact timings/windows and the unresolved subjective second-run slowdown. Both runs verify real Adreno 740 Turnip/DXVK, native models/input, recovered CPU affinity and normal game quit. Do not claim thermal throttling, a device-crash fix or a measured fullscreen FPS improvement.

Implementation: 0.4.1/code18. Native ClientActivity is immersive sensor-landscape, with an overlay gear/panel rather than a layout row. Outside dismiss taps are consumed; opening controls releases all game input and suspends controller capture; closing restores focus. Android Back first collapses the panel. Keyboard focus and immersive mode recover after dialog dismissal. Errors open the panel or show the existing failure dialog. Insets keep controls away from cutouts/revealed bars. Existing touch coordinate scaling keeps aspect ratio; 1280×720 fills a 16:9 screen.

All launch/Prepare validators accept 1280×720. Fullscreen game is enabled by default; saved resolutions remain unchanged. The request is persisted, and the supervisor applies only WindowedMode and the four resolution keys immediately before client launch. New shared client_display.py preserves original/previous INI bytes in prefix/trasc-display-originals, rejects ambiguous/symlink/unsupported inputs, and replaces atomically after backups. Both ClientRuntime and server RuntimeManager copy the new module. No client/server runtime image, GPU driver, input DLL, CPU policy or display-rate change. The existing display cap is 30 updates/sec. The existing bounded five-second log now records thermal status (Android 29+), power saver, focus, menu/keyboard state and view size to aid the next comparison.

Resolved build history follows. First run 35014179237 on f5976143de69e263d9bb47d12cdc10d73cc69ad0 passed backend/browser/JVM/database and native driver builds, but APK resource compilation caught a missing closing vector tag. The resource was corrected; all Android XML parses and Java sources compile locally against the official Android 35 jar. Both direct and PRoot real 1280×720 fullscreen/DXVK/input gates passed on that first run before a follow-up push cancelled remaining work.

Corrected source 3d609ad2960d5740252f136d120414c94011f53a/run35015185369 passed APK compilation/lint/signing and all direct client tests including 720p DXVK. The later PRoot run failed BEFORE apply_display/game launch: existing cmd.exe /c exit 0 preflight returned Unix -9 (SIGKILL). It loaded its 32-bit modules successfully; the available trace does not establish the signal sender/cause. Do not confuse the successful direct test's cgroup counters with the failing PRoot process. This resembles previous intermittent native exits but is at a different stage, not evidence of a fullscreen/device regression. Publication was correctly skipped.

Added tests/client_runner_diagnostics.py, a CI-only wrapper around the production entry point. It preserves original spawn/wait/timeout/exit-zero checks, adds process/thread traces ONLY for the small PE32 cmd check, and retains bounded native process/wait state plus before/after memory/cgroup counters separately for cold/warm checks. No automatic retry, accepted -9, app/runtime change or assertion relaxation. Subsequent integration launches invoke this wrapper; all production checks and the full existing fixtures still run. Local 63 backend tests, JVM checks, XML parsing and Android Java compile passed. Browser flows passed CI. Physical Android gear/IME/immersive behavior and ROF2 fullscreen still need Thor acceptance.

The unreleased 3d609ad candidate was independently downloaded and verified: APK 6,223,204 bytes, SHA256 56a15dd3286a1c62346c28b0285bd35ef7f76565e597b691567e57f870acf1c2; source bundle SHA256 d5569e4cdfb81d5e781f4ede28da9738bf66f5893e9de4c67019ce698e993071. Android manifest is 0.4.1/code18, package and pinned certificate unchanged, bundled backend/UI equal source, bundled Vulkan/DXVK hashes valid. Do not present it as a published/fully passed release; verify the final candidate after the diagnostic run.

Diagnostic run 35016870662 on 450f607a1982d037c60146b5dedb726c782b380b passed APK/database/drivers, direct runtime, and direct plus PRoot 720p fullscreen/input. The PRoot PE32 startup check now exited zero (0.608s). A subsequent Balanced texture/shader fixture rendered all expected pixels, released its D3D device/window and printed stage=return result=0, then Unix wait reported -9. It failed before the already-instrumented Compatibility fixture. Publication remained skipped; do not call the intermittent signal fixed. Candidate APK 6,222,576 bytes, SHA256 2808a3a75c6d7c41b707f2c19eee236d83de4c92966c047b25a44d74d6331058; sources c63f695203f915122a9bc32c5cf992696a174405149af1af5458a0165e81dff1. Manifest/certificate, all 14 backend/UI assets and Vulkan bundle hashes verified.

Next diagnostic change adds an isolated CI tracefs instance filtered only to SIGKILL, retaining kernel sender/target identity and timing; no ptrace attachment, runtime image change or exit assertion relaxation. Reports explicit tracefs unavailability if unsupported. All small auxiliary PE32 fixtures and the PE32 startup check additionally enable Box64 debug/limited Wine teardown traces. Production app logging stays unchanged. Wine 10 server/process.c has a ~half-second forced-exit timer; pinned Box64 core.c endBox64 can wait up to two seconds for emulator workers. This is a source-based hypothesis, not an identified signal sender: require captured evidence before changing runtime shutdown or claiming a fix. Keep all original strict exit-code checks and never automatically retry failures.

Kernel-trace run 35018317440 on 86ba48d850849082f65a05dd05a97c3ee33ac154 stopped at the existing quiet/verbose log-size assertion because the added BOX64_LOG=2 leaked into that benchmark (10,765,901 vs 21,701,730 bytes). This is a diagnostic-harness error, not an app regression or weakened assertion. Corrected by keeping a separate production_env and using it unchanged for the explicit normal/verbose comparison; teardown probes retain debug observations. Kernel tracing itself works and records wineserver as sender for Wine helper shutdown signals, plus container PID-namespace cleanup; this run did not reach the failing texture case, so it does NOT identify the earlier texture/check signal sender. Preserve the original <1/5 log-size and exact return-code requirements.

Run 35019111190 on 0c016d0909b8dbb4a47dd43ee3c2bb6fb30a7d43 passed APK, database, native builds, complete direct tests and PRoot 720p fullscreen/DXVK/models/input. The later warm WineD3D recovery fixture returned -9. Crucially, the kernel trace now identifies wineserver PID6812 sending SIGKILL to textures.exe PID6882 at monotonic1046.411420 (SI_USER, result0). The same PID's Wine/Box64 trace had already requested exit(0) around1045.993 and entered Unix finalizers; a remaining thread called _exit(0). This confirms a premature Wine-server cleanup kill for THIS failure, not OOM/user Stop. It does not retroactively prove every older signal had the same cause. No endBox64 worker-wait message was observed, so do not claim that specific branch caused the delay.

Implemented a focused Wine10 server patch: raise the bounded process-exit SIGKILL threshold from approximately0.5s to approximately8s, allowing Box64's existing5s Unix-exit watchdog and PRoot cleanup to finish. Windows process exit codes/handles and game execution remain unchanged. Owned TRASC Stop retains its independent terminate/kill deadlines. Build only wineserver in Debian Bookworm for the installed glibc, alongside the existing Wine graphics helper; APK bundles binary/hash manifest and corresponding Wine source/patch/recipe. ClientRuntime copies it to private backend storage and makes it executable. Supervisor selects it using Wine's WINESERVER variable, verifies its hash before any Wine launch, reports patch/grace in state, and uses the same helper for Stop. Installed rootfs, Box64, client and prefix files are not replaced; no runtime redownload or repair.

Added a controlled ARM64 regression: a native preload (test-only, cleared before guest startup) delays cmd.exe Unix destruction for2s without changing exit status. The stock server must reproduce SIGKILL before the destructor completes; the patched server must finish it and return exact Windows0 and23. A separate running Windows ping is explicitly stopped within15s. All original fixture return-code/log-size assertions remain strict. The full suite also asserts the bundled server patch is active. Exhaustive Box64 call tracing is now disabled after identifying the sender; limited Wine/kernel/process observations remain. Local native delay self-test preserves23 and completes its2s destructor;63 backend tests pass; Wine patch applies cleanly to verified upstream Wine10 source. Full CI and updated APK verification pending.

First cleanup-fix source e33bc25b83d299f20bf91c6858c18d9312282076/run35021496662 built the new Bookworm wineserver and passed the complete direct Wine rendering/input/model/warm-prefix suite using the verified bundled server. Its new controlled test stopped because the native preload did not survive Wine's loader restart: stock cmd returned0 and no destructor marker existed. Corrected the fixture to build an x86-64 Unix shared library and use BOX64_LD_PRELOAD across loader restarts, selecting only cmd.exe for the delay. The original expected stock -9, patched0/23, required begin/done markers and explicit-stop checks remain unchanged. This is a fixture correction; the product cleanup patch is unchanged. Third-party notices now explicitly describe the bundled Wine server and matching source/patch recipe.

Run35022681067/source6e71bcf86ece195a5f79d966e0df8f2ba1d06ed1 again passed the full direct Wine suite. The x86 preload was visibly loaded, but its destructor still did not execute (stock exit0, no marker). Box64's fast _exit path bypasses finalizers, and preload ELF finalization is not a reliable injection point. Replaced the fixture with a native exit/_exit interposer, installed through /etc/ld.so.preload only in the disposable CI container and removed in finally. It delays only a process whose leader is cmd.exe and whose explicit test-report variable is set; all exit callers retain the supplied code. The native loader preserves this hook across Wine exec without involving Box64 guest preloading. Stock must still return -9; patched must still return exact0/23 with completed2s markers; explicit Stop and all production assertions remain strict. No product/runtime-image change.

Run35023751665/source9a9fb28cd67d76f3d8a2b79c441c1c9093e951c6 successfully activated the native delay and reproduced stock SIGKILL, but the nominal patched case also returned-9 after1.518s with only begin. Kernel trace identifies wineserver PID4792 killing cmd PID4788. The captured spawn and verified Wine10 loader.c explain the integration defect: exec_wineserver tries bin_dir/wineserver BEFORE WINESERVER. Earlier status/hash assertions verified the private helper, not the binary Wine actually launched; do not call those earlier runs proof the patch was active. Fixed by an ephemeral PRoot binding of the APK helper onto /opt/wine/bin/wineserver, exactly like the existing WineD3D overlay; no rootfs file replacement on Android. Supervisor now hashes that actual preferred path before any Wine launch and records it. Direct GPU and PRoot CI use the same path overlay. The isolated regression swaps that adjacent path between saved stock and patched bytes, requires unchanged negative/positive exit checks, restores it in finally, and runs before the main suite. The main direct CI container then copies the patched helper onto that path for all auxiliary Wine launches; this only changes a disposable container. Also made artificial delay markers host-readable so failure artifacts upload. No automatic retry or relaxed exit condition.

Sourcea99e99058696dfdd46fb9f47c3e70afbbd2f74b1/run35024923714 PASSED the controlled ARM64 cleanup comparison: stock -9/1.567s with begin only; patched0/3.069s and23/3.073s with begin+done; explicit Stop returned in2.295s. This is the first actual stock-versus-patched proof. The fixture then incorrectly issued a second -k after its already-verified Stop; Wine correctly returned1 for no running server, so the job stopped before the main suite. Corrected only redundant final cleanup: skip it after the successful -k/-w/child.wait sequence, retain it for incomplete tests, and restore binary/display in a nested finally. All main exit, delay, negative-control and Stop assertions remain unchanged. APK compilation/lint/signing passed; publication still pending the complete suite.

Run35025704070/sourceddf37364ecd2f9e8282ef0b42aeea715e9e15240 passed the controlled stock/patched exit comparison, explicit Stop and the complete direct rendering/input/model/quiet-log/warm-prefix suite using the actual patched server path. APK/lint/signing and native/database jobs passed. The next Vulkan Docker test failed before Wine setup: Box64 reported the mapped server was not executable. GitHub's artifact extraction drops executable bits; workflow had restored execute permission on backend/wineserver, while new GPU/PRoot mounts mistakenly used backend-assets/wineserver. Corrected all those mounts to the already-prepared backend/wineserver, matching Android's private executable. Android already calls setExecutable and is unchanged. No runtime/content/exit-assertion changes.

Next: collect the Thor acceptance sequence in preview-notes.md: stop both runtimes, update in place, select Turnip+DXVK /1280×720 /Fullscreen game /Balanced /Automatic /Allow available cores; retain native helpers and disabled shadows/verbose logging. Check corner taps, gear collapse, Keyboard/Esc, held input release and Back/Return. Repeat the same route for 30–60s per run at 720p and export logs plus DXVK FPS observations after the second run. New thermal/power/focus telemetry can help investigate the still-unconfirmed slower feel. No runtime download, Prepare, prefix repair, reimport or server rebuild. Keep VirGL and windowed recovery. Preserve the signing key and app ID. GitHub writes use create_tree/create_commit/update_ref and exact-tree comparison; no subagents unless explicitly requested.

---

# Active handoff: 0.4.0 Turnip/DXVK (2026-09-15)

User tested both 0.3.10 modes and reports no improvement, possibly regression; explicitly requests Turnip. Bundle `../upload/logs-5171237087911039269.zip`, extracted `../analysis-0310`. Selected world movement windows: Single 16:55:27–16:56:37 UTC, 15 samples, median Wine 6.46/s and display 6.77/s; OpenGL worker 17:06:57–17:08:02, 14 samples, Wine 6.10/s and display 6.47/s. Different routes/order; no controlled speedup claim. Both prefixes reuse in ~5.43 s (~6.75 s until launch). Global assets 40/38 s and zone UI 51/52 s. Both game logs now retained. Both main game threads become restricted to CPU 0; the worker also uses CPU 0. Main thread ~79%/75% of one CPU in selected windows. Parent allows multiple cores. Do not attribute the restriction to a particular INI/DLL without evidence.

Released 0.4.0/code17 implements APK-bundled ARM64 glibc Mesa 24.3.4 Turnip (KGSL, upstream unchanged), x86 DXVK 2.5.3 d3d9, native Vulkan 1.3 device+Xlib-swapchain preflight, and selectable Turnip renderer. Mesa MESA_VK_WSI_DEBUG=sw selects X11 CPU-copy PRESENTATION while Turnip still renders on the GPU. The production probe requires driver ID 18, Qualcomm vendor 0x5143, non-CPU device and three successful presents. No silent Lavapipe fallback. This first Turnip path still uses Xtigervnc/RFB; it is NOT a native/zero-copy display integration. Driver and DXVK are copied from APK assets; no runtime update or separate download/import needed. Keep patchme, native/system dinput8, D3DX30/35, VirGL and Software.

CPU affinity selector defaults to available: every ten seconds rechecks the current launch marker, UID and thread start time, then expands only matching eqgame.exe threads to the supervisor's current app-allowed mask. No INI edits, root, priority changes, Android policy change or server/helper mutation. `game` retains previous behavior on next launch. New Windows fixture reproduces single-core affinity and native test checks actual Linux mask recovery. Local 59 backend tests and JVM management/input/archive checks passed. Local browser test could not run because its Playwright browser binary is absent; CI installs its pinned browser.

**Published and verified:** source commit `25d541a0baf36d82c0797126c0e536a775c37f4a`, all six jobs passed in [run 35005200443](https://github.com/Russianranger/trasc-server-android/actions/runs/35005200443). Published APK, build manifest and corresponding sources were downloaded and match the tested candidates and GitHub asset digests. Binary Android manifest, signing identity, packaged Python/UI sources and Vulkan/DXVK/Wine module checksums were verified. Exact builder source is in `vulkan/` and `scripts/build-vulkan.sh`; corresponding sources accompany the APK.

- [APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk): **6,216,473 bytes**, SHA256 `f56333651692c7aca58de9fd2bc8a7f10e5fe2daeb1b335987293e8820d8aea4`.
- Application ID `io.github.russianranger.trasc.preview`, version **0.4.0/code17**; certificate SHA256 `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Preserve signing cache `trasc-preview-signing-v2`; never regenerate if missing.
- Corresponding sources: **66,967,891 bytes**, SHA256 `4ad8b26133ddaa6d05f61a2b34cecf5d07b6ce7027b54f12e3540aaf359ffde1`. Includes pinned Mesa/DXVK source archives and build recipe.
- Turnip SHA256 `238e616626a5c552b34d0effed6972a0a85e2fe04ea23903076922b409a030ac`; probe `f4ad3e5169b23d8f78b200cf8e0bcc802e799418773c6d33a175a59e5de20779`; DXVK D3D9 `3d6b529dc4f7f55639aad580e81b2939ab1489cdbb9f1550dce74e41656bbf5b`.
- Existing Wine `legacy-specular-fog-v1` patch, rebuilt DLL SHA256 `63ca0fb292693b92175ad5005ebd062fe3f2895bfb038fd4b3e77e3f862301af`. Same underlying Wine10/Box64 0.4.4 runtime; server runtime unchanged.
- Local published downloads: `runtime-work/trasc-server-android-0.4.0.apk`, `launcher-sources-0.4.0.tar.gz`, `preview-build-0.4.0.json`. The matching candidate filenames contain `0.4.0-diagnostics`; earlier `0.4.0` and `0.4.0-fixed` candidates were not released.

CI history: native staging commit bd04fe6901e3c752d1884ea82a393e856e04e06f/run35000511445 compiled Mesa and the probe, then the DXVK release download repeatedly returned HTTP500. An explicit download query fixed it: d1bb876fe338680a950317bd295f16dac44e83e6/run35001126877 passed. First integrated commit de5c9b2ddb023122a93dc77c9e91a6d706e8b32e/run35002183382 passed APK build/lint/signing, browser/backend/JVM/database and direct/PRoot Software/VirGL rendering, input, models and actual affinity recovery. The new Vulkan negative control then failed its expected error-message assertion: the Vulkan loader returned VkResult -3 from enumeration on the GPU-less host, instead of the expected zero-device message. Corrected preflight checks opening /dev/kgsl-3d0 first and reports the actual OS error. The negative control verifies the absent device and exact error after independently loading the real Turnip library; it now preserves its diagnostics. No pixel or input assertion was relaxed. The first candidate APK is not a released artifact.

Important new test gate: `scripts/check-client-vulkan.sh` adds Lavapipe ONLY to a CI copy of the runtime, explicitly labels it software_test, checks real DXVK textures/shaders/models/input and fallback to built-in WineD3D after native d3d9 installation, both direct and through PRoot. The real Turnip DLL dependencies must load in the unchanged runtime and its probe must reject CI's missing Qualcomm device. Passing these gates does NOT prove Adreno rendering or FPS; first device launch must validate KGSL and presentation. Preserve strict assertions; investigate failures. No subagents unless explicitly requested.

Run35003854309 built and signed the corrected APK, then PRoot/Software's Compatibility texture fixture again returned Unix -9 after every pixel check passed (job104499719759), before Vulkan ran. This repeats the older CI shutdown caveat; it is not a Turnip failure. Added test-only teardown-stage markers, Wine module/process traces and a bounded native-process/cgroup-memory snapshot in compatibility-exit.json. The exit-zero assertion remains mandatory, with no automatic retry or acceptance of -9. Moved the Vulkan gate earlier so later compatibility failures cannot hide its evidence. These test changes do not alter the shipped runtime or app. The final run passed all fixtures with diagnostics enabled; the earlier termination's cause remains unproven. Do not claim a fixed device crash. Preserve and inspect these diagnostics if it recurs.

Final validation includes 59 backend tests, JVM/browser flows, Android compile/lint/signing, database import/backup/restore and complete runtime archive roundtrip. Real ARM64 Wine/Box64, both direct and accelerated PRoot, passed Software, VirGL and DXVK/Vulkan artwork/shader pixels, animated native models, native/system DirectInput forwarding, held-key input/release, actual affinity recovery and warm-prefix reuse. Existing original/patched Wine shader negative controls passed.

Vulkan artifact10411119431 (`client-vulkan-verification`, local runtime-work/turnip-vulkan-verification.zip) additionally proves actual native d3d9 load and DXVK 2.5.3, three Vulkan swapchain presentations, and real WineD3D texture/shader recovery using the same prefix after DXVK. The test device is explicitly llvmpipe/Lavapipe, driver ID13, vendor65541, software=true. The unchanged production runtime separately loads the bundled Turnip library and rejects absent /dev/kgsl-3d0. This validates the integration, not actual Adreno rendering or game FPS.

**Next device test:** stop both runtimes and update in place. Select **Turnip + DXVK / 800×600 / Balanced CPU / Automatic runtime / Allow available cores**; native dinput8 and model helpers enabled, verbose diagnostics and shadows off. Launch twice to distinguish one-time prefix/shader work from a warm comparison. Check menu artwork, character models, movement and the DXVK device/FPS HUD; export logs after the second run. On a failed check or corrupted graphics, stop and export logs before further launches rotate them, then choose VirGL for recovery. No runtime download, client reimport, prefix repair or server rebuild. If needed, compare affinity `game` separately. Actual hardware success and performance remain pending; the CPU-copy presentation path can still limit FPS.

This follow-up is documentation-only `[skip ci]`; the preview tag stays on the tested code commit. GitHub writes use create_tree/create_commit/update_ref, with exact tree comparison before updating main. Local git push has no credentials. Keep this latest handoff above historical entries.

---

# Active handoff: 0.3.10 native OpenGL worker comparison (2026-09-15)

**Published and verified:** source commit `9974560e8562119f1d036745f964b770b1f14a61`, version **0.3.10 / code 16**. All five jobs passed in [run 34991355081, attempt 2](https://github.com/Russianranger/trasc-server-android/actions/runs/34991355081/attempts/2). Downloaded published APK, build manifest and corresponding launcher sources match the tested candidates and GitHub asset digests. APK version, application ID, signing certificate, packaged Python/UI sources and Wine DLL were checked.

- [APK](https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk): **2,101,864 bytes**, SHA256 `4e60ec4e8e25b6bf2453faa58f9583696e3cf6d73298984c006f71089f2951b3`.
- Application ID `io.github.russianranger.trasc.preview`; certificate SHA256 `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Preserve signing cache `trasc-preview-signing-v2`; never regenerate it if missing.
- Launcher sources: **35,115,916 bytes**, SHA256 `69b01fabc2c73643d1dd919be9760b9c64602e42687bccfa31e4d18dc9fcc222`.
- Same Wine patch `legacy-specular-fog-v1`; rebuilt DLL SHA256 `4a8e74f11c70922d32e0820bfe227c3e15b1ea4472197c9ed51c496b2c8090da`. Underlying Wine 10 / Box64 0.4.4 client runtime images and server runtime remain unchanged.
- Verified local downloads: `runtime-work/trasc-server-android-0.3.10.apk`, `preview-build-0.3.10.json`, `launcher-sources-0.3.10.tar.gz`. Candidate files and first-attempt direct Software diagnostics are also under `runtime-work`.

**Latest device evidence:** user tested 0.3.9 Multithreaded first, Single second; Single felt faster in game but slower to load. Bundle `../upload/logs-5272698694105119380.zip`, extracted `../analysis-039`. Selected movement windows show median Wine **5.99 → 7.73 presents/s**, with Android display generally tracking and decoding around 1–1.5 ms. Different routes/window durations preclude a controlled speedup claim. Single's global assets took 39 s and zone UI 53 s; the first game's `dbg.txt` was overwritten. First prefix 33.211 s (update), second 6.648 s (reuse). Do not attribute cold/warm differences to threading. Actual Adreno 740, PRoot acceleration and native DLL modes remain verified; normal quit. No proven CPU-core or GPU bottleneck.

**Implemented:** third Graphics threading choice `opengl_worker`, displayed as **Single + OpenGL worker (experimental)**, combines Wine csmt=0 and Mesa mesa_glthread=true. Existing modes explicitly set the Mesa option false; saved Single selection persists. Every ten seconds the sampler records bounded CPU ticks, last CPU, allowed masks and actual `:gl0` worker observations for this launch. A random inherited marker finds Wine processes even after they detach from the original launcher. Only same-UID environments are inspected for an exact match; environment contents and marker values are never logged. Limits are 2,048 proc entries, 32 matched processes and 256 threads. Full observations are in bounded `client-threads.log`; once-per-second state uses a compact summary. No affinity or priority changes.

Before another launch, preserve the previous state and bounded head/tail game startup log as `client-state.previous.json` and `client-game.previous.log`. The imported game log is not modified. Existing exports include these logs. Preserve `eqgame.exe patchme`, dinput8=n,b, native D3DX30/35, graphics corrections, controls, prefix, database, server and maps.

**Validation:** 54 backend tests plus JVM and browser management checks; Android compile/lint/signing; database and complete runtime backup/restore. Real ARM64 Wine, direct and accelerated PRoot, passed Software and VirGL worker observation, artwork/legacy shader pixels, all three threading modes, animated native models, DirectInput forwarding, held-key movement/release, shader negative controls and warm-prefix reuse. The observed game worker was `eqgame.exe:gl0`. Host collection took about 3–4 ms per ten-second sample in direct Software; this is not a Thor measurement. The clear/Present counter fixture is not a game benchmark. Physical-device FPS improvement from the new option remains unverified.

**CI history / unresolved caveat:** first candidate `1ad450e1e12eed5a6736575434b093ca41d6be04`, run 34990461984, failed worker observation because Wine reparents away from the launcher. The marker-based sampler fixes that discovery failure, with regression coverage for detached and other-launch processes. On corrected commit 9974560, attempt 1 observed the worker and passed the rendering/model/input tests, but the later Compatibility CPU texture fixture returned Unix -9 (SIGKILL) after printing correct pixels and PASS. Job `104457176920`. Retried the failed job once on unchanged code; job `104458996316` passed every gate. No assertions were weakened. The cause of the intermittent termination is unproven; a similar event occurred in 0.3.8 CI. It is not evidence of a confirmed or fixed device crash. If it recurs, collect targeted process/exit diagnostics instead of repeated retries or accepting -9.

**Next device comparison:** stop both runtimes and update in place. Keep VirGL / 800×600 / Balanced CPU / Automatic runtime, native helpers on, shadows and verbose diagnostics off. Warm the prefix after the APK's one-time refresh, then compare Single thread with Single + OpenGL worker along the same route for 30–60 seconds each. Export after the second run. Return to Single if graphics or performance regresses. No runtime download, repair, server rebuild or client reimport is needed. Next analysis should check actual worker presence, Wine/display rates, per-thread CPU deltas and both retained startup logs; do not infer CPU saturation from a single last-CPU field. Turnip/DXVK remains unimplemented and is a separate architecture milestone below.

This release-provenance follow-up is documentation-only `[skip ci]`; the preview tag remains on tested code. GitHub writes use connector create_tree/create_commit/update_ref; local git push has no credentials. No subagents unless explicitly requested.

---

# Active handoff: 0.3.9 frame delivery and threading comparison (2026-09-15)

Current turn: user confirms loading substantially improved on 0.3.8 but FPS remains very poor; shadows off, screenshot/video/logs attached. Scratch was pruned automatically; repo re-cloned from GitHub. Latest attachments are present, extracted under `../analysis-038`; video contact sheet inspected. Previous artifacts can be recovered from releases/Library if needed. See `docs/client-performance.md` for exact timings and source references.

**Published and verified:** source commit `d41468a32f2330becc3052d10c1e38079e117e01`, **0.3.9 / code 15**. All five jobs passed on the first attempt in [run 34975546828](https://github.com/Russianranger/trasc-server-android/actions/runs/34975546828): database, Wine graphics module, APK, client runtime and preview publication. Downloaded published assets match the tested candidates, GitHub asset digests and APK build manifest. Packaged backend/UI files and Wine module match their sources/manifests.

- APK: https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk — **2,098,790 bytes**, SHA256 `9adf3df5c29781cd503ee98884c08c30a5f890f2c4f6dd2c1ee76526cb863dfb`.
- Same application ID `io.github.russianranger.trasc.preview`; certificate SHA256 `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Preserve signing cache `trasc-preview-signing-v2`; never regenerate on a missing cache.
- Launcher sources: **35,115,915 bytes**, SHA256 `0c57d5074612fb7ffdf0a5a55338e98a30acaf07ad2fa5be870cdd2e518fc953`.
- Same Wine patch `legacy-specular-fog-v1`; rebuilt module SHA256 `eecf024ae92bfb3bab74de8442df92d8932c228d4397b6a8eee50e4dbe5280c6`. Underlying Wine 10 / Box64 0.4.4 runtime images remain unchanged.
- Local verified downloads: `runtime-work/trasc-server-android-0.3.9.apk`, `preview-build-0.3.9.json`, `launcher-sources-0.3.9.tar.gz`. Candidate APK/source and direct Software verification artifacts are also under `runtime-work`.

Implemented: pooled RFB decode/CopyRect storage; Graphics threading multi(default)/single using `WINE_D3D_CONFIG=csmt=1/0`; actual Wine aggregate fps parsing (exclude first interval per process/swapchain, retain timestamped evidence); independent Android RFB/new-bitmap draw/work counters and bounded `client-presentation.log`. No changes to GPU backend, model/native DLL fixes, CPU scheduling, runtime images, prefix registry or server. Preserves `eqgame.exe patchme` and `dinput8=n,b`.

Validation: 51 backend tests, JVM buffer reuse/counter semantics/archive/controller/preflight tests, browser management flows, Android compilation/lint/signing, database import/backup/restore and full runtime archive roundtrip. Real ARM64 Wine, both direct and through accelerated PRoot, passed Software and VirGL artwork/shader checks and actual aggregate presentation samples for both threading modes, plus native model animation in single mode. Existing native/system DirectInput, held-key release, prefix reuse and original/patched shader controls passed. The presentation fixture deliberately clears/presents with a 30-ms sleep; its rate is a counter/configuration check, NOT a Thor performance benchmark. Physical device FPS improvement from this APK remains unverified.

Latest 0.3.8 device evidence: repeat launch request 18.138 → 8.175 seconds; global assets 102 → 46 seconds; game UI 48 → 35 seconds across separate user sessions. Adreno 740, PRoot acceleration and app-private internal storage confirmed. Video shows uneven movement with correct graphics and disabled shadows. No measured game FPS or proof that display decoding dominates the remaining cost. Inherited CPU mask 0–5 observed, but no scheduling changes or assumptions about game-thread core placement.

Next: collect the two-mode device comparison below. Low Wine and Display rates suggest game/translation/render/presentation work upstream; much higher Wine than Display while moving suggests delivery/readback/display work. Neither distinguishes CPU from GPU alone; Canvas CPU submission time is not GPU completion. GitHub writes use connector create_tree/create_commit/update_ref; local git push has no credentials. This follow-up is documentation-only `[skip ci]`, leaving the preview tag on the tested code commit. No subagents unless explicitly requested.

Device test after release: same scene with VirGL/800×600/Balanced/Automatic, native DLL/model helpers on, verbose off, shadows still off. Run Multithreaded, then stop/relaunch Single thread. Record 30–60 seconds of movement each and export after the second run. Wine presents and Android bitmap draws are distinct rates, not physical refresh FPS. First post-APK launch may update the prefix; mode switches do not require repairs/reimport. Return to Multithreaded if worse. Turnip still not implemented.

---

# Active handoff: 0.3.8 runtime acceleration (2026-09-15)

Latest user bundle `logs-2383206107737699672.zip` in `upload/performance-037`: 0.3.7 repeat prefix works (102.731 → 15.697 seconds), globals 121 → 102 s, UI 63 → 48 s; performance remains unacceptable. Client already uses internal `getFilesDir()/work/client/current`; D: is not shared storage. Changing the drive letter would not bypass PRoot. Inherited CPU mask 0–5 noted, with no affinity changes.

**Published:** source commit `45311704d0f03f6004d7abe0f9866831b3a7d9f5`, **0.3.8 / code 14**, same application ID and signing certificate. All jobs in [run 34915871917, attempt 2](https://github.com/Russianranger/trasc-server-android/actions/runs/34915871917/attempts/2) passed. APK and corresponding sources match the tested build and GitHub asset digests; packaged backend/UI sources and Wine module were checked.

- APK: https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk — **2,094,986 bytes**, SHA256 `abeab0a5a1782109a70737077721d700e3bd481295796d1969e63e4bbbe9d84a`.
- Certificate SHA256: `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`.
- Launcher sources: **35,115,914 bytes**, SHA256 `2d6910ab0b60dcd7fbd3eb4cdc8c98fc277440a3e5348a78b57c2e49fe83c168`. Includes the native acceleration patch and matching generated PRoot diff/build recipe.
- Wine patch stays `legacy-specular-fog-v1`; rebuilt module SHA256 `aa849648ec81c50ee9481b1a36a0500393096caf031b09cf1699e0b7cbe178c7`. Same underlying runtime images; existing installations only need the APK.
- Local files: `runtime-work/trasc-server-android-0.3.8.apk`, `preview-build-0.3.8.json`, `launcher-sources-0.3.8.tar.gz`; diagnostic bundles `client-038-proot-retry.zip` and `client-038-gpu.zip`.

Implemented Automatic/Compatibility runtime mode, independently of the existing CPU profile. Automatic runs an isolated read/seek/stat/socket/child-process preflight against the installed runtime and bindings, requiring success plus an actual native acceleration event. Only then does it launch with the app's `PROOT_NO_SECCOMP` override removed. Failed/unsupported completed checks fall back; a hung check aborts and manual Compatibility is available. This enables PRoot's optional selective-syscall filter; it does not remove Android security policy. Same pinned PRoot revision, with one opt-in observation message. Server remains in its previous mode. Preserve graphics patches, native D3DX30/35, dinput8=n,b, patchme, controller settings, database and prefix.

Diagnostics: `client-runtime-probe.log`, `client-proot.log` and `.previous.log`; actual acceleration, requested mode and private-storage location in `client-state.json`. Older `client-runtime.log` remains in exports but new supervisor output is in `client-proot.log`.

Validation: 49 backend tests plus host JVM preflight success/failure/timeout and existing archive/controller tests; Android compile/lint/signing, management UI, database and complete runtime backup/restore. Direct ARM64 and accelerated PRoot Software/VirGL gates cover native/system DirectInput forwarding, held-key release, D3D artwork and legacy shaders, native models/animation, quiet logs and real prefix reuse. Native preflight and main PRoot acceleration events observed in both renderers. Successful retry PRoot/VirGL prefix: **27.252 s full update, 3.006 s repeat**, repeat total **3.840 s**; previous 0.3.7 values 89.356 / 6.415 / 7.974 s. These are separate CI runs using host llvmpipe GLES, NOT Thor timings or game FPS.

**First-attempt caveat:** the accelerated PRoot/VirGL negative control returned Unix `-9` (SIGKILL) instead of expected 23 after normal rendering/input passed. Retried the failed job once on the exact same commit, without changing assertions or settings; every gate passed, including negative 23 and positive 0. Cause remains unproven. Original job `104213592192`, successful retry `104215329134`. Wine 10 has a server-side exit SIGKILL timer (`server/process.c`), but logs do not establish it caused this event. Do not claim this was a confirmed/fixed product crash or silently ignore recurrent terminations; collect targeted process/exit diagnostics if it recurs.

**Next device test:** stop both runtimes, update in place, select VirGL / 800×600 / Balanced CPU / Automatic runtime, keep native DLL/model helpers on and verbose logging off. First launch after rebuilt Wine module may update the prefix; stop and launch again. Compare the same character/zone; then try Runtime Compatibility with the other settings unchanged if performance/behavior regresses. Export Logs after comparison. Look for `runtime_acceleration_observed: true` and actual preflight output; a selected Automatic setting alone is not proof. No client move, reimport, prefix repair or server recompilation is needed.

Turnip/DXVK remains the next graphics architecture milestone described in `docs/client-performance.md`; it is not included in this APK. Keep VirGL and Software recovery paths. This handoff follow-up is documentation-only `[skip ci]`; preview tag stays on the tested code commit.

---

# Active handoff: 0.3.7 performance release (2026-09-15)

User confirms 0.3.6 graphics correct but loading/in-game performance poor, asks about Turnip. Latest bundle `logs-810542259983773733.zip` extracted locally to `upload/performance-036`. See `docs/client-performance.md` for exact evidence, CI measurements and Vulkan implementation requirements.

**Published and verified:** source commit `f66b19accbabe8ff090987d16137b3e69f85d17b`, version **0.3.7/code13**, same preview application ID and signing certificate. All five jobs in [run 34911899931](https://github.com/Russianranger/trasc-server-android/actions/runs/34911899931) succeeded: database, wined3d, APK, client-runtime, preview. Release assets were downloaded and checked against both GitHub digests and the build manifest. Packaged Python/UI sources match the repo, and the packaged Wine module matches its manifest.

- APK: `https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk`, **2,091,609 bytes**, SHA256 `206ee8eda061236f260fad22a6d8fd9ee6be479bb6be67d8a42bca656b236dea`.
- Signing certificate retained: `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`.
- Launcher source archive: **35,114,918 bytes**, SHA256 `b1fa7b21e1f577b3e5195cae0f4566a17294639c382468e244084c7f1fecd1de`.
- Same Wine patch `legacy-specular-fog-v1`, rebuilt DLL SHA256 `e401ab183852422ab300d397e26399965b9d5deccc73ea6c1ad5e21f461fbac2`. No runtime download required. The underlying client runtime remains 1.0 (Wine10/Box64 0.4.4); server runtime unchanged.
- Local copies: `runtime-work/trasc-server-android-0.3.7.apk`, `preview-build-0.3.7.json`, `launcher-sources-0.3.7.tar.gz`. CI diagnostic bundles: `client-037-verification.zip`, `client-037-proot.zip`, `client-037-gpu.zip`.

Implemented: Balanced/Compatibility CPU profiles (Balanced default, BIGBLOCK2/SAFEFLAGS1, retains STRONGMEM1), private Box64 rcfile removing the 64-core Wine override while keeping explorer's small-block workaround, verified prefix reuse (`wineboot -i` after one successful update), startup timings/CPU affinity, persisted client launch options under `client/launch-options.json`. Compatibility retains previous CPU configuration. The `.trasc-prefix-ready.json` marker uses runtime/graphics content fingerprints, checks PE32 files and actual cmd execution, and is invalidated before setup so interruption/failure retries a full update.

Validation: 48 Python tests plus JVM/archive/controller checks, management UI tests, Android compile/lint/certificate verification, full database import/backup/restore, real ARM64 native dinput8 forwarding, texture/shader/model/animation/held-key tests with Balanced, Compatibility D3D texture checks, original/patched Wine shader negative control, full runtime backup/restore and native helper lifecycle. Real second launches reuse the checked prefix without rewriting kernel32.dll. Through PRoot/VirGL, Wine prefix setup measured **89.356 s full update → 6.415 s repeat**; total until launch requested **90.948 → 7.974 s**. Host uses llvmpipe GLES; these are startup test observations, NOT Thor game FPS.

**Next device test:** stop both runtimes and install in place; use VirGL + Balanced at 800×600, native dinput8/model helpers enabled and verbose logging off. First post-update launch still forces one prefix update; stop and launch again for the reuse comparison. Visit the same character/zone. Export logs after the second run. Use Compatibility if behavior regresses. Preserve graphics corrections, native model helpers, dinput8=n,b and `eqgame.exe patchme`.

**Next graphics milestone:** Turnip/DXVK is promising but NOT implemented/shipped. Requires ARM64 glibc/KGSL driver or explicit bridge, matching Vulkan loader/Box64 wrapping, actual Adreno device enumeration and compatible presentation, x86 D3D9 DXVK and per-backend cache/override selection. Keep VirGL/Software fallback. Pinned Winlator source references and staged gates are in `docs/client-performance.md`; inspected source clone is `research-winlator` at `a030f552f452158a2db64fdb32b490fa19c0b48d`. Do not claim swapping a driver ZIP alone connects Vulkan to the current native GLES VirGL/VNC path.

Repo writes still use GitHub connector create_tree/create_commit/update_ref (local git push has no credentials). Test gates before release; preserve preview signing cache. This handoff/measurement update is documentation-only `[skip ci]` after release; preview tag remains the tested code commit.

---

# Development handoff — 2026-09-14

## Published: 0.3.6 texture and legacy GLSL corrections

- Release/code commit **3bf09d7437d0c59151c7014a5157ab6c2c4f2075**. All gates passed: https://github.com/Russianranger/trasc-server-android/actions/runs/34908354131 . The handoff-only commit that follows uses `[skip ci]` and does not rebuild the APK.
- APK: https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk . Version **0.3.6 / code 12**, application ID `io.github.russianranger.trasc.preview`, **2,089,669 bytes**, SHA256 **2321f9efe7fe8c224cedc2f1c39b56bcac142268fe7cd7d31a92e1a2b8894d34**. Downloaded the published file and checked its hash, packaged backend scripts, PE32 Wine fix and release metadata.
- Preserved signing certificate SHA256: **ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869**. Install over the existing preview after stopping server/client; no uninstall, server rebuild, runtime/client reimport, prefix repair or DirectX reinstall is needed.
- Corresponding launcher sources: **35,114,918 bytes**, SHA256 **4ce79de44df017fc4b2d2e370674c1d3de8f987f6286052030b5eb834e6bf1f2**. Includes checksum-pinned Wine 10 source, patch and complete build recipe in `wined3d-sources.tar.gz`, alongside VirGL/epoxy/PRoot/cabextract sources.

### Device evidence and changes

Latest user bundle `logs-5414003610187626190.zip` confirmed actual **Adreno 740** forwarding, guest **OpenGL 2.1 / Mesa 22.3.6**, and correctly loaded native dinput8/system DirectInput/D3DX30+35. Screenshots showed cyan-striped EULA artwork and untextured black character/trees. Host emitted repeated invalid texture uploads/redefinitions; Wine emitted three undeclared `ffp_varying_specular` shader errors. Latest session stopped explicitly; do not call this another loader/DLL crash.

Replaced the imported always-on gl4es upload patch with `native/virgl-patches/0008-correct-s3tc-storage.patch` plus the bounded MIT decoder `native/trasc_dxt.h`. GLES allocates RGBA8/SRGB8_ALPHA8 host storage for DXT1/3/5 once; packed guest BC transfers are decoded and uploaded with SubImage, preserving mipmaps, partial-update offsets, cube/array layers, alpha and sRGB. Other compressed formats retain their own path; desktop GL retains native compressed storage. The original patch incorrectly redefined immutable compressed storage on every upload, and mishandled subrectangles and sRGB pixel types.

Wine 10's legacy GLSL vertex epilogue read `ffp_varying_specular.w` even though GLSL 1.20 outputs were mapped to `gl_FrontSecondaryColor`. `native/wine-patches/0001-legacy-specular-fog.patch` corrects that expression. `scripts/build-wined3d.sh` builds the matching PE32 module from pinned Wine 10 sources. APK carries it plus a manifest; `ClientRuntime` binds it over `/opt/wine/lib/wine/i386-windows/wined3d.dll` before Wine starts. The underlying runtime is preserved. Runner verifies its SHA256 and records `wined3d_patch` / `wined3d_sha256` in client-state. Published module SHA256: **d29107ee4344a27514a1a9416dd47089a400c9deab30c87e44c19dc7e95a75f1**, patch ID `legacy-specular-fog-v1`.

### Verification and next device pass

- Passed 46 backend tests, Android build/lint/signing, UI/JVM management checks, full offline database import, complete server/client runtime backup/restore, and native GPU parent/worker cleanup.
- Passed Software and VirGL through both direct ARM64 Wine and real PRoot: native proxy forwarding, held-key release, model libraries/animation, D3D compressed artwork, SM1/2 shader pixels and bounded logging. GLES CI uses host Mesa, not a physical Adreno.
- `graphics_probe.py` now samples all eight S3TC formats, partial updates, 4/2/1-pixel mip levels, sRGB and blended alpha before GPU launch. Local decoder tests also passed address/undefined-behavior sanitizers (leak detection unavailable in this workspace).
- Direct VirGL tests explicitly restrict guest GL to **2.1 / GLSL 120** to exercise the Thor capability path. The original unmodified Wine module reproduces the named undeclared-specular compiler error plus wrong pixels (exit 42); patched Wine renders both shader versions correctly (exit 0). Keep both controls. D3D9 SM1 fixtures need explicit input declarations too, and Mesa may open diagnostic identifiers with a backtick rather than an apostrophe. The test normalizes only that punctuation.
- **Next:** on the Thor, stop both runtimes, update in place, start the server, select **Android GPU / VirGL (experimental), 800×600**, retain native dinput8/model helpers, leave verbose diagnostics off. Check EULA/menu backgrounds, loading artwork, character textures, then enter the same zone and test animation/movement/performance. Export Logs. Confirm `wined3d_patch=legacy-specular-fog-v1` and all texture checks pass. Actual game visuals and playable FPS remain pending this device test; Software stays available for recovery.
- Preserve `eqgame.exe patchme`, `dinput8=n,b`, native D3DX30/35, quiet default Wine logging and all server/client/prefix data. Never ship a higher GL version override as a substitute for actual driver capability. Test-only GL downgrades live in scripts, not Android launch settings.

## Published: 0.3.5 experimental Android GPU path

- Latest device bundle `logs-7569883861190000028.zip` is app 0.3.4. User confirms visible models, animations and movement. Both Microsoft D3DX helpers and native/system DirectInput load; HMD failures are zero. Global asset initialization 20:59:42–21:01:40 UTC (1m58s, previously 9m14s). Greater Faydark main loop ready 21:05:09; normal camp 21:08:37 and quit 21:09:42. Wine log is 31,524 bytes. Actual rendering remains llvmpipe/Accelerated:no; the game's Nvidia adapter label is emulated. A shader-effect failure and a few texture/backbuffer messages remain, without a proven new fatal launch error. VNC updates are not game FPS.
- Published code / both release tags: `c8bf91416a05396ad101a232fede1e85574854f3`. App **0.3.5**, version code **11**. Main release workflow **34902060609** passed database, APK, full client runtime and publication: https://github.com/Russianranger/trasc-server-android/actions/runs/34902060609 . This handoff-only follow-up changes no packaged code.
- APK: https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk — **767,434 bytes**, SHA256 `dc483a95c48ea262e91b9c945895e8c65b6d5f339edbc0d16802af6d4aa1e4a9`. Downloaded APK hash/size match the manifest and GitHub asset digest. Archive includes the ARM64 VirGL/PRoot/cabextract executables and both client_runner.py/graphics_probe.py. Preview manifest confirms commit/version/application identity; preserved-certificate verification passed in CI.
- Application ID stays `io.github.russianranger.trasc.preview`; certificate stays `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`. Never regenerate the signing key if the pinned main cache is unavailable. Server runtime remains 1.1. Client Wine 10 WoW64 / Box64 0.4.4 / Mesa 22.3.6 image is unchanged; the newly repacked client 1.0 archive is 353,710,711 bytes / SHA256 `87981e3ec141716dcb40a96bfa788c0b6454c1f45daf53de12ad0206b88919b6`. Existing installations only need the APK. Do not request a runtime download, helper reinstall, Prepare, prefix repair or client/database reimport for this update.
- **Client → Graphics** offers Software/default and **Android GPU / VirGL (experimental)**. Guest Mesa virpipe forwards WineD3D commands over an app-private Unix socket to native Android EGL/GLES, outside PRoot. The existing embedded TigerVNC display/controller input remains. Native dinput8=n,b, `eqgame.exe patchme`, Microsoft D3DX30/35, quiet logging and conservative Box64 settings are unchanged. Prefix repair explicitly uses Software. No Termux installation, ANGLE, Vulkan driver or external client/display app is needed.
- Native build: pinned VirGL 1.3.0 and static libepoxy 1.5.10, selected Android patches from Termux commit `4f48a30edadeff906a3cad7adfaa17e5f8d10605`, MIT/gl4es notices included. Source SHA256: VirGL `56170f8caa1bb642a2624b649e3bcca095ec2834814e5c308efc8a85a709e4ce`; epoxy `a7ced37f4102b745ac86d6a70a9da399cc139ff168ba6b8002b4d8d43c900c15`. Published launcher-sources includes archives, patches and build/wrapper sources; its SHA256 is `314d9c9f15ee18b4b13f754a8780228cd5c6d7b9b3debfaad0172f383595ae10` (3,095,665 bytes). See THIRD_PARTY_NOTICES.md.
- Lifecycle: `GraphicsBridge` owns startup, bounded logs, private socket and stop/failure cleanup; `ClientRuntime` owns it for the client session and backup checks. A native-driver probe runs in a separate reaped child so the renderer parent never initializes Android graphics threads before forking. The parent has a death signal; patch 0014 gives every vtest worker a race-checked SIGKILL parent-death signal. An open-connection JVM regression verifies parent and workers all terminate on Stop. **Do not switch to --no-fork --multi-clients:** installed Mesa's older protocol rejects that mode. Retain the supported worker model and enforced ownership.
- GPU preflight uses `graphics_probe.py`: map a GLX drawable, query actual driver strings, render/read back a red pixel, swap and clean up. This avoids the reproduced glxinfo X_GetImage BadMatch on an unmapped window. Software retains glxinfo. `client-gpu.log` records actual native vendor/renderer/version; `client-graphics.log` records guest renderer and drawing check. State records graphics_backend, host_gl_renderer and graphics_acceleration. Do not equate a requested GPU mode with actual acceleration. Log exports work through the existing native path, including after a failed launch.
- Verification: 46 Python tests; JVM archive/log/controller/RFB/prefix checks; browser launch/graphics selection and recovery; full ARM64 database seed/rules/backup checks and complete runtime archive roundtrips; Android build/lint and signing. **Both Software and VirGL** pass real PE32 Direct3D9 output, native/system DirectInput, held W and release, quiet/verbose diagnostic comparison, Microsoft model registration/sampling/compression/skinning and visible animated-model pixels, **directly and through pinned PRoot**. Native bridge lifecycle and guest pixel tests also passed in 34901143010. CI uses Linux llvmpipe GLES as the host, validating the graphics/protocol path rather than physical Adreno performance.
- Test infrastructure pitfalls fixed: the PRoot GPU fixture is `runtime-work/gpu-proot`, keeping the translated display socket below 108 bytes (the previous longer name caused bind EINVAL). Windows fixtures publish marker/result files with close+atomic MoveFileEx; existence checks previously raced an empty file and failed JSON parsing. Keep the full pixel/model/input assertions and main publication gate.
- **Next device pass:** stop both runtimes; install 0.3.5 in place; start server; choose **Android GPU / VirGL (experimental)** explicitly at the same **800×600**, native DLL/model helpers enabled, verbose diagnostics off. Check character models, animation and held movement in the same zone; report loading/responsiveness and export Logs. If GPU setup or rendering fails, Stop → Software → Launch for comparison using the same prefix. Physical Adreno 740 / Android 13 compatibility and playable FPS are **not yet confirmed**. Preserve the working server, database, client files and bindings.

## Published: 0.3.4 model helper compatibility fix

- Device bundle `logs-3194322454755682154.zip`: app 0.3.3 on Android 13 / AYN Thor, 800x600, native and system DirectInput loaded, quiet Wine logging. User reached character selection and Greater Faydark with invisible models, limited movement and responsive UI.
- Global asset initialization improved from 53m11s to 9m14s (19:44:48–19:54:02 UTC). Character selection 19:54:07; gfaydark main loop ready 19:58:39; normal camp 20:01:51 and quit 20:02:42. Latest Wine log 31,746 bytes. Server accepted/saved the character and saved 404 zone spawns on shutdown; this alone does not verify later state restoration.
- Game log contains 573 HMD/model initialization failures. Wine loads built-in d3dx9_30/35. Previous verbose bundle includes repeated RegisterAnimationSRTKeys, Compress and ConvertToIndexedBlendedMesh stubs. Wine 10 source returns E_NOTIMPL: https://github.com/wine-mirror/wine/blob/wine-10.0/dlls/d3dx9_36/animation.c and https://github.com/wine-mirror/wine/blob/wine-10.0/dlls/d3dx9_36/skin.c. Native-model tests reproduce this failure with built-ins and pass with Microsoft libraries. Actual ROF2 model rendering remains pending device verification.
- Graphics remains llvmpipe LLVM 15 / Mesa 22.3.6, Accelerated:no. This update provides model compatibility, not hardware acceleration or promised playable FPS. A few texture/framebuffer errors remain in the device log. Movement hold/release passes open-fixture tests; verify the game's own movement bindings and performance on device before diagnosing further.
- APK-only installer: official Microsoft June 2010 DirectX redist 100,275,120 bytes / SHA256 `053f76dcbb28802e23341b6a787e3b0791c0fa5c8d4d011b1044172dbf89c73b`. Client tab supports online installation or picking the matching offline EXE. Original source installer retained; only app cache copy removed. Microsoft DLLs are not included in the APK, Git repository, runtime image or published artifacts.
- Native Android cabextract 1.11 source SHA256 `b5546db1155e4c718ff3d4b278573604f30dd64c3c5bfd4657cd089b823a3ac6`, downloaded from Debian's mirror of the same upstream tarball after the upstream host returned 403 on an x64 CI runner. Source/build recipe accompanies APK in launcher-sources.tar.gz. Built ARM64 PIE with /system/bin/linker64 and 16 KiB alignment. Android SDK setup requests platform-tools explicitly; obsolete SDK tools package caused the initial CI setup failure.
- Portable Java installer verifies the entire redist, extracts only x86 D3DX30/35, verifies PE32 headers, stages both files and a hash manifest, preserves previous install, and recovers interrupted directory swaps. Helper source directory work/client/directx; previous generation work/client/directx-previous. Runner validates both files before replacing only prefix/drive_c/windows/syswow64/D3DX30/35, snapshots originals under prefix/trasc-directx-originals, and rolls back partial-copy failure.
- Verified library hashes: d3dx9_30.dll 2,388,176 bytes `5edeed79f2359527a55b8189cfa8b9b121cd608d44eead905a0f3436938ad532`; d3dx9_35.dll 3,727,720 bytes `2198022938156b790e9cfb0f7997494b66a11a1ad49b395be58251d635b66b26`.
- Native helpers default on for client launch (installation required), off for Wine desktop. Disable model helpers explicitly for built-in comparison. Actual native/builtin model-load evidence is recorded separately from DirectInput and retained across log rotation. Preserve patchme, dinput8=n,b, quiet logs, CPU translation settings, existing client/prefix/server data and both runtime recipes.
- Tested code commit `af45c8af790aa143fa43fcf1cc6202dab7d2b2e3`. Branch Android/UI/database https://github.com/Russianranger/trasc-server-android/actions/runs/34893658488 and ARM64 client https://github.com/Russianranger/trasc-server-android/actions/runs/34893658055 passed. 44 Python tests, native session/controller/RFB tests, browser management flows, full seed/database backup+restore, APK compilation/lint, native Android extractor/source packaging all passed.
- Production Java extraction test downloaded the official package, extracted both correct 32-bit DLLs, rejected a corrupt same-size archive before extraction, retained existing installation, and recovered an interrupted swap. Full client runtime extraction/backup/restore with all hashes passed.
- Direct ARM64 and pinned PRoot both passed: real PE32 launch, native proxy/system DirectInput forwarding and its native-only negative control; W held/release in polled DirectInput state; native D3DX30/35 registration, sampling, compression, indexed skinning and visible animated model pixels over private RFB. Built-in negative control exits 31 on E_NOTIMPL. Test code uses C vtables for MinGW D3DX interfaces and DIDFT_OPTIONAL for standard 256-key format; initial fixture failures were corrected without changing user input settings.
- Local exec workspace disconnected during implementation. Logs were read and upstream downloads verified before disconnect; source changes continued through the GitHub plugin, with CI providing compile/runtime validation. No claim of testing the modified APK directly on Thor.

- Published Preview 0.3.4 / version code 10 from `af45c8af790aa143fa43fcf1cc6202dab7d2b2e3` on main: https://github.com/Russianranger/trasc-server-android/actions/runs/34894293758 passed APK, database, full ARM64/PRoot and publication jobs. Pinned preview signing identity was reused and verified by apksigner; application ID remains `io.github.russianranger.trasc.preview`, certificate SHA256 `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`.
- Published APK: https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk — 298,346 bytes, SHA256 `b784e444b23b1fc1e9a0fa555cb4caea497d6a1538d9aa7ec375c51950753d41`. Launcher source archive 1,499,943 bytes, SHA256 `a113082ac8fc944a1470e5567e831cc42e7363ef4c8db89edc3d25d1be0a6328`; includes cabextract source/build recipe.
- Repacked client runtime 1.0: 353,710,669 bytes, SHA256 `5248e5d3720881b8d8c1fbaea10667b944a28f86e2fcd78ca75c349a7a3d736f`. Runtime recipe/image unchanged; existing device installations need only the APK and one-time DirectX helper installation. Both published tags point to tested code `af45c8af790aa143fa43fcf1cc6202dab7d2b2e3`.
- Verification read back release tags, versioned release notes and GitHub asset digests, plus successful CI signing/build gates. Local execution remained unavailable; a local APK download/hash computation was not performed. The connector/browser could not fetch the release JSON asset bodies. Do not describe those as independently downloaded/verified.
- Next user test: stop both runtimes, install APK in place, Client → Install DirectX model helpers (or matching offline Microsoft EXE), keep current runtime/prefix/imported client, start server, launch 800x600 with native dinput8 and model helpers on, verbose off. Verify character models, world rendering and movement, then export Logs. Check `native_d3dx_requested`, both native `model_libraries_loaded` entries and game HMD errors. Preserve user movement bindings; compare with the game's configured movement keys if movement still fails. No Prepare, prefix repair, runtime redownload, server rebuild or client/database import is required.
- After device model validation, major next work is a feasible hardware graphics path for the Thor's Adreno GPU; llvmpipe is still CPU-only. Audio remains later work. This documentation follow-up does not change the published APK.

## Previous release: 0.3.3 client logging performance fix published

- Latest device evidence: `logs-547215203498946041.zip`, Android 13 / AYN Thor, app 0.3.2, native and system dinput8 loaded, 800x600 llvmpipe. User confirms client boots but is abysmally slow. `patchme` is present. No prefix repair needed.
- Full new bundle read from scratch. Wine trace: 1,059,025,481 bytes / 9,258,244 lines. Most output is routine DBG_PRINTEXCEPTION_C / unwind tracing plus 240,177 D3DX RegisterAnimationSRTKeys fixme lines. Do not misdiagnose debug-print exceptions as crashes. Game dbg.txt: initialization starts 17:38:02 UTC, global data 17:42:54–18:36:05 (53m11s), character selection 18:36:11. Session stopped explicitly at 18:51:59.
- Graphics reports llvmpipe LLVM15 / Mesa22.3.6, Accelerated:no. Software GPU emulation is a remaining limit. Xvnc's 3,230 updates across the long session include idle time; not an FPS benchmark. Box64's Cortex-A510 banner does not establish forced CPU affinity. No unsupported CPU/GPU tuning in this pass.
- Implemented normal WINEDEBUG `-all,+timestamp,+pid,err+all,trace+loaddll`; removed always-on module/seh/fixme output. Explicit verbose checkbox defaults off and records mode in state. Kept patchme, dinput8=n,b, conservative dynarec flags and current images. Source reference: Wine 10 dlls/kernelbase/debug.c (OutputDebugString still raises a debug exception even with tracing off).
- Wine output uses a dedicated pipe drain/rotation thread, captures DLL/fatal evidence before rotation, retains two 8MiB segments plus one capped previous session. Old huge logs are shortened to start/final excerpts on next launch, avoiding another gigabyte export. State includes emitted byte/rotation counts, not inferred FPS. Module events cannot evict actual DLL-load proof.
- App version 0.3.3/code9; application ID, pinned certificate, server runtime1.1 and client runtime1.0 unchanged. User only needs APK update; no runtime download, prefix repair, Prepare or data import.
- Local 41 Python tests passed, including rotation, early-load evidence, fragmented fatal messages and oversized previous-log migration. Host JVM/native input/archive checks run. ARM64 CI adds a 512-call OutputDebugString comparison with output-volume assertions and informational timings; DirectInput negative/positive controls, D3D pixels and controller input remain required.
- Tested code commit: `321d42103336b2a5a41d3a1fa47aa31149283634`. Branch APK/UI/database https://github.com/Russianranger/trasc-server-android/actions/runs/34884617744 and ARM64 client https://github.com/Russianranger/trasc-server-android/actions/runs/34884617402 passed all checks. APK compilation/lint, 41 Python tests, native archive/input, browser flows and database seed/cold migration passed. Native/system forwarding, D3D visible pixels and mouse/keyboard passed both directly and through PRoot. Full runtime extraction/backup/hash roundtrip passed.
- 512-call OutputDebugString workload, including process startup: direct ARM64 normal 0.365s / 8,389 bytes; verbose 0.577s / 2,396,722 bytes. PRoot normal 1.573s / 8,471 bytes; verbose 2.638s / 2,396,804 bytes. This confirms suppression and reduced overhead in that fixture; do not extrapolate to ROF2 FPS or the whole 53-minute asset load. Wine still handles debug-string exceptions with tracing off.
- Main release https://github.com/Russianranger/trasc-server-android/actions/runs/34885237951 passed all jobs and published both assets. Main PRoot comparison repeated successfully: normal 1.522s / 8,471 bytes; verbose 2.486s / 2,396,804 bytes. Pinned-certificate verification and Android lint passed. Both release tags point to `321d42103336b2a5a41d3a1fa47aa31149283634`; this follow-up changes only the handoff.
- APK 0.3.3/code9: https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk — 256,626 bytes, SHA-256 `65e8ec0ebfca100999dc61afe973b39ad99652b3d92f01146dc83ab7f2e082e9`. Preview manifest confirms commit, version, application ID and the preserved pinned certificate.
- Repacked client-1.0 archive: 353,710,659 bytes, SHA-256 `b370ec771fb4d6d9b2b6ff6ae6af2efebc1e731007284cb623d462af96078250`. Runtime recipe/image unchanged; existing installations need only the APK.
- Publication verified by reading back both tags/release asset digests, downloading the APK, matching its SHA-256/size, inspecting its packaged supervisor/UI fixes, and checking both release manifests.
- Device retest at same 800x600 with verbose diagnostics off, native DLL on; report time to character select/responsiveness, then attempt world entry if practical and export Logs. No device speedup/playable FPS is claimed before retest.

## Previous release: 0.3.2 system DirectInput fix published

- Workspace access recovered. Full `logs-5544659252330987853.zip`, client-runtime.log, client-state.json and client-wine.log have now been read. Earlier text-only notes below are historical.
- Device is Preview 0.3.1, Android 13 / AYN Thor, 800x600 software graphics. Prefix repair completed at 12:13:54 UTC; subsequent 32-bit preflight passes and native DINPUT8 loads.
- Confirmed configuration defect: during native DLL process attach, `C:\windows\system32\dinput8.dll` resolves to syswow64 and Wine reports environment override `n`, then status c0000135. The add-on's `Release-NMS-Client/eqgame_dll/dllmain.cpp` loads that absolute system path and returns E_FAIL from DirectInput8Create if its function pointer is missing. This is an input initialization blocker, not another missing kernel32 failure.
- The later `wined3d_swapchain_resize_buffers` back-buffer diagnostic occurs before teardown. Wine 10 source logs that error and continues to return WINED3D_OK in that path; it does not alone establish the game's exit cause. No unsupported renderer/DXVK changes in this pass. Original game exit code remains uncaptured: launcher_exit is Explorer's result.
- Fix: `dinput8=n,b`, separate native-imported and built-in-system load evidence, sticky evidence across log tail windows, +seh exception traces, timestamped session boundaries and stop_request/signal reasons. Do not equate two load traces with successful plugin functionality.
- Native log inventory/export now includes selected root client and Logs/logs startup diagnostics, with case-insensitive names and symlink/traversal checks. Settings, game binaries and character chat are excluded. This addresses the missing dbg.txt / dinput8.log evidence in the old bundle.
- Expanded open fixture matches the add-on's GetSystemDirectory + LoadLibrary pattern and creates DirectInput keyboard/mouse devices. ARM64 tests must pass native forwarding plus D3D/display input, with negative control native-only exit 23 and fixed n,b exit 0, directly and through production archive extraction + pinned PRoot. No proprietary client files in CI.
- Verified code commit: `52d3a1b6d0187eb9e47517f180ea79e12c0e44d1`. Branch APK/UI/database checks passed: https://github.com/Russianranger/trasc-server-android/actions/runs/34868688109 . ARM64 runtime/PRoot checks passed: https://github.com/Russianranger/trasc-server-android/actions/runs/34868687404 . Both environments reproduced native-only exit 23 and corrected n,b exit 0; keyboard/mouse device creation, D3D rendering and display input passed. 38 Python tests and JVM archive/log/input/prefix checks passed. APK 0.3.2 / version code 8 keeps the application ID and signing certificate. Runtime images are unchanged; no runtime download or prefix repair required.
- Main release workflow: https://github.com/Russianranger/trasc-server-android/actions/runs/34869328238 — all jobs passed, including both DirectInput controls in PRoot, preserved certificate verification, database/session tests, browser flows and APK compilation/lint. Both release tags point to `52d3a1b6d0187eb9e47517f180ea79e12c0e44d1`. This handoff follow-up changes no packaged code.
- Published APK: https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk — 254,894 bytes, SHA-256 `7ea59d659b483f999772772548db73f9372468b9db3175628801b0e129745d66`.
- Repacked client-1.0 archive: 353,710,716 bytes, SHA-256 `3d9a423f750c00fcdb72545690cb07f6b5850ff6b580b58465ab0924a9f1a2cb`. Both runtime images remain unchanged; existing users only need the APK.
- Publication verified: release tags and asset digests read back; downloaded APK matches the published SHA-256 and size. Preview manifest confirms 0.3.2, the expected commit/application ID and pinned certificate; client manifest matches the same commit and published archive hash.
- Next device pass: stop runtimes, update in place, keep current prefix, start server, launch native ROF2, export logs. Preserve server/client data. No new physical ROF2 acceptance is claimed.
- Primary diagnosis references: https://github.com/Russianranger/Triptych-Triumvirate/blob/main/Release-NMS-Client/eqgame_dll/dllmain.cpp and https://github.com/wine-mirror/wine/blob/wine-10.0/dlls/wined3d/swapchain.c .

## Previous text-only device follow-up: 32-bit Wine and native dinput8 confirmed

- Following the 0.3.1 update, the user reports that the ROF2 window appeared for approximately ten seconds before disappearing. Latest bundle is `logs-5544659252330987853.zip`; individual client-runtime.log, client-state.json and client-wine.log were also attached. The analysis workspace failed to initialize, so these attachments have NOT been read. Evidence below comes only from text pasted by the user.
- Pasted client-state.json reports `prefix_files_ready: true`, `wine32_ready: true`, `native_loaded: true`, mode client, resolution 800x600, launcher_exit 0, final phase stopped, and no error field. Wine's actual native-load trace identifies `D:\\DINPUT8.dll` at 7AE60000 in process 0194. This confirms imported native DLL loading, not successful plugin behavior or game acceptance.
- The pasted Wine tail contains process-detach/shutdown traces at approximately 146758–146759. It does not include the reason for shutdown. Do not diagnose a missing dependency, graphics failure, plugin failure, or clean game exit from this tail alone.
- Code review confirms `launcher_exit` is the return code of Wine Explorer, not a captured eqgame.exe exit code. The final supervisor phase stopped can follow a stop request/signal and cleanup; it does not identify why the earlier game window disappeared.
- Next evidence needed: client-runtime.log and the earlier client-wine.log section immediately preceding the first shutdown/detach sequence, including any errors/exceptions. Clarify whether the user pressed Stop after the window disappeared to distinguish later supervisor cleanup from the original game exit. Preserve the working prefix; no further repair/reimport is indicated by the pasted state.
- No new APK or runtime change has been made for this follow-up. Keep 0.3.1 and the existing published artifact hashes below.

## Previous milestone: ROF2 black-screen recovery, Preview 0.3.1 published

- Published code commit: `01138918df6d9a68a45af8f36a3860be3b4771b5`. Both `preview` and `client-runtime-v1` tags point to this commit. This handoff-only follow-up changes no APK/runtime code.
- Main release workflow: https://github.com/Russianranger/trasc-server-android/actions/runs/34800190134 — database, APK, client runtime and publication jobs all passed. Includes 36 Python tests, JVM archive/input/prefix preservation, browser management flows, Android compile/lint, preserved signing certificate, full server database/runtime integration, client archive roundtrip and PE32/DLL/Direct3D/input probes both directly and through pinned PRoot.
- APK 0.3.1 / version code 7: https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk — 253,622 bytes; SHA-256 `f81d0d826ec3883ee904de5b01f3d96ce1647e9ac7f7f2020fda09003548966c`. Application ID and certificate remain the pinned values documented below.
- Repacked client-1.0 archive: 353,710,662 bytes; SHA-256 `e1f37b8b29f47ded4eb859d6b7cd8add86b113843a3aec06933be309fbf3bece`. The Wine/Box64 image and recipe are unchanged; no client runtime redownload is required. Server runtime remains 1.1.
- Publication verification: both tags and release asset digests read back from GitHub; downloaded APK hash/size and preview/client manifests match the code commit, version and preserved application identity.

- Previous 0.3.0 failure bundle: `logs-8799882545351199396.zip`, user confirms right-stick mouse input but black display on ROF2 launch.
- Device evidence: VNC frames/input and llvmpipe OpenGL 4.5 initialize. `eqgame.exe patchme` is correctly passed and the PE32 EXE loads. Wine then reports `could not load kernel32.dll, status c0000135`; native dinput8 is not reached. Explorer returns 0 and the old supervisor continues presenting an empty display. First prefix setup was interrupted by Stop. This suggests a prefix or Android loader problem; do not claim the exact underlying cause is established from these logs alone.
- Existing optional libXcomposite/codec/Bluetooth and CPU-info warnings are separate from the confirmed fatal loader error. No runtime dependency upgrade is required for this APK checkpoint. `/sys` is now bound for CPU discovery.
- Working branch `codex/client-loader-fix`, base main `662b24f8a2fdbcb4fd73828909fa1e284c22af85`. Baseline commit `80663be45652a436b2c9e37e577ec747e19159c6` added real production archive extraction followed by the same pinned PRoot code compiled for Linux ARM64. Baseline PRoot probe passed: https://github.com/Russianranger/trasc-server-android/actions/runs/34799154722 . This narrows the failure but does not reproduce Android SELinux/kernel behavior.
- Shipped changes: 32-bit runtime/prefix core-file inventory (`client-prefix.json`), actual SysWOW64 cmd.exe bootstrap before launching ROF2, separate prefix logs, fatal-loader/dependency detection and visible failure dialog, normal Stop handled without a spurious failure traceback, backed-up Repair Wine prefix action, and launch `patchme` retained.
- Repair moves only `client/prefix` to a unique `client/prefix-backups/` directory and starts a fresh prefix/Wine desktop. Game files, controller profile and server data remain intact. Host JVM regression verifies settings/drive symlinks preserved and game untouched.
- ARM64 tests passed with a non-large-address-aware PE32 fixture with a 20 MiB image and 32-bit preflight enabled, both directly in the runtime and through PRoot: https://github.com/Russianranger/trasc-server-android/actions/runs/34799675371 . This remains an infrastructure test, not actual ROF2 acceptance.
- APK/UI/database gates also passed for `7641d28e6196a77ffad3b4ff28f8465fb76ba59a`: https://github.com/Russianranger/trasc-server-android/actions/runs/34799675523 . Final refinements disable duplicate launch controls and require a terminating loader error before treating optional missing dependencies as fatal; the main release gates passed with these refinements included.
- Next device sequence: update in place, stop the client, Repair Wine prefix, wait for desktop and 32-bit check, stop, start server and Launch ROF2. No reimport/recompile/runtime redownload is needed. Export logs after the attempt, including client-prefix.log/json and client-wine.log.


## Previous milestone: embedded client 0.3.0

The user confirmed: “Session backup and restore worked perfectly. control inputs work, client input works. Lets move to the next step with the client.” Latest bundle `logs-338733800756477711.zip` reports 0.2.1 on Thor Android 13, completed client ZIP import, and no current runtime-start failure. The confirmation concerns the previous input diagnostic; actual ROF2 execution inside this app remains the next physical test. Do not redo server/database installation.

### Code and verification

- Client milestone merged into main: `a472dea0d914f6279a2d4179920d0b050823ec07`, developed on `codex/client-bootstrap`. APK 0.3.0 / version code 6. Server runtime stays 1.1.
- Final branch APK/UI/database workflow: https://github.com/Russianranger/trasc-server-android/actions/runs/34788669730 — passed.
- Final branch ARM64 client workflow: https://github.com/Russianranger/trasc-server-android/actions/runs/34788669699 — passed.
- Main release workflow: https://github.com/Russianranger/trasc-server-android/actions/runs/34788877454 — all jobs passed; signed APK and client runtime published. Both release tags point to the code commit above. This handoff-only follow-up changes no APK/runtime code.
- 34 Python tests passed; host JVM archive/RFB/input tests passed; management browser flows passed; Android compilation/lint passed. Existing full server-runtime archive, offline seed import, SQL/rules and cold database migration checks passed.
- Real ARM64 probe passed: Box64/Wine executes our own PE32 Windows EXE, loads our native test dinput8 DLL, presents Direct3D9 pixels through the private RFB socket, and receives mouse/keyboard events. The probe contains no EverQuest code. This does not establish compatibility with the user's modified ROF2 client.
- The actual client runtime archive passed production TarExtractor/SessionArchive extraction and full roundtrip: executable permissions, multiarch paths, file inventory and all restored hashes verified. Existing full server runtime has its own separate gate.
- Development issues resolved: missing emulated x86 libgcc/libstdc++/libunwind; display test waited too early and assumed a value for RGB888's undefined padding byte; client-only archive fixture needed the required server marker. Do not weaken production archive checks to accommodate test fixtures.

### Published artifacts

- APK: https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk — 250,918 bytes; SHA-256 `e96e53c6347470ea3f19f7be1edb67e64004fec0ed6bc6310eda859e0f9381df`.
- Client runtime: https://github.com/Russianranger/trasc-server-android/releases/download/client-runtime-v1/client-runtime-arm64.tar.gz — 353,710,645 bytes; SHA-256 `31fa8092b9679d861e853542d0ee05a7762c5740398b5efe8271486b64a98e18`.
- `preview-build.json` identifies 0.3.0 / code commit above / the preserved application ID and certificate. `client-runtime-manifest.json` identifies client-1.0 / the same source commit / matching archive hash and bytes. Both tags and asset digests were read back from GitHub; the downloaded APK hash and manifest contents were independently checked.
- Main release gates passed: 34 Python tests, host JVM, browser flows, APK compile/lint/preserved signing certificate, full server database/runtime integration, actual ARM64 Windows/DLL/Direct3D/input probe, actual client-runtime archive roundtrip.
- Reuse the existing server runtime and imported game. Only the separate client runtime is new. Actual Thor Wine/ROF2 acceptance remains pending.

### Implementation

- Separate Debian rootfs in `work/client/runtime`, Wine 10.0 WoW64 + pinned Box64 0.4.4, WineD3D/llvmpipe baseline. Existing server rootfs/database is retained.
- `ClientRuntime` uses the packaged PRoot loader for the second environment; it installs the release runtime online with checksum validation or from an offline archive, and preserves the prefix/game during replacement.
- `backend/client_runner.py` supervises Wine and TigerVNC. RFB listens only on a private Unix socket, mode 0600; no TCP RFB/X11 listeners, random X11 authorization cookie. Prefix initialization is bounded and supports stopping.
- `ClientActivity`, `RfbConnection`, `DisplayInput` implement native display, saved controller mappings, touch, physical keyboard/mouse, text Type/Send + Enter, focus releases and combined input reference counts. Android Back returns to management without stopping the client.
- Client tab offers runtime setup, Wine desktop, resolution, ROF2 launch, return/stop and preparation. Preparation generates the four handshake files into the root/Resources, writes login endpoint and windowed resolution, preserves unrelated INI settings/DLL, and retains originals in `backups/client-setup/` with rollback on errors.
- Native dinput8 is the default request (`dinput8=n`); PE32 executable/DLL architecture must match. Only Wine's actual native loaddll trace marks it confirmed. Built-in mode is an explicit diagnostic comparison. Desktop mode does not claim DLL loading.
- Client start is coordinated with queued/running import/preparation jobs. Rejecting a second launch does not kill the current client. Stopping the server runtime retains the foreground service while the client is active; notification Shut down stops both.
- Client runtime, imported files, controller profile and Wine prefix are part of the complete session client component. Temporary sockets/auth/process state stay under home/tmp outside archives. Complete backup/restore stops the client first. Prefix D: maps to `/client`.
- Logs include client-runtime.log, client-wine.log, client-display.log, client-graphics.log and client-state.json; native log export remains usable with the server closed.
- Build recipes/workflow live in `client-runtime/`, `scripts/build-client-runtime.sh`, `.github/workflows/client-runtime.yml`. Main preview publication is gated on the client workflow as well as APK/database jobs. Sources accompany runtime binaries.

### Next device pass

Follow `docs/client-runtime.md`: update in place, download the separate client runtime, try Wine desktop at 800x600, stop, prepare the already imported client, start the server, then launch ROF2 with native dinput8 enabled. Return and export Logs after the attempt, reporting visible screen/error and native DLL status. No server rebuild, seed import or client ZIP reimport is needed for the update.

Software graphics only. Hardware acceleration, sound, performance tuning and actual ROF2 login/world entry remain unverified/later work. Keep the existing Winlator fallback. Preserve application ID `io.github.russianranger.trasc.preview` and pinned certificate `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`; never regenerate a lost preview key.

## Previous request: 0.2.1 export fixes

Latest device report: Preview 0.2.0 session export fails with `Unsafe session path: runtime/var/lib/dpkg/info/binutils-common:arm64.conffiles`. Log export then fails with `Failed to connect to /127.0.0.1:18775`, because backup preparation closed the runtime. Fix both and publish an in-place APK update; preserve the existing world and signing key.

### 0.2.1 fixes published

- Base: `fe84687345bcb4328ec6ab85a71c453829bd2073` (handoff after the published 0.2.0 code).
- `SessionArchive.confined` accepts ordinary Linux colons and still rejects drive prefixes, traversal, absolute paths, backslashes and NULs. Both export and restore share the fix.
- New platform-independent `LocalLogs` reads/lists/zips app and nested server logs without a backend; checks symlink parents/leaves, tails 64 KB for viewing and streams full snapshot lengths into bundles. Native status is included without loading credentials/settings/API tokens.
- MainActivity routes logs/export_logs directly to Android. Both UI log export buttons now consume the direct result instead of polling Python jobs. Backup failure persists to app.log and explicitly reports stopped-runtime recovery. The game server is not automatically restarted.
- Version 0.2.1 / code 5, same application ID and pinned certificate. No new runtime or server rebuild required.
- Local checks: 29 Python tests passed; native host-JVM roundtrip/log regressions passed; JavaScript syntax and whitespace checks passed. CI now also backs up/restores the complete published Debian ARM64 runtime, verifying every file hash; UI regression simulates backup failure with backend unavailable and tests both log buttons and readers.
- Published code commit / preview tag: `e61761bd0ea725061ffcd2305ff82edd158b1cdc`. This handoff-only follow-up changes no APK code.
- Successful workflow: https://github.com/Russianranger/trasc-server-android/actions/runs/34780884423 — database, APK and preview jobs all passed.
- Actual runtime roundtrip passed using the production TarExtractor/SessionArchive classes: more than 28,000 regular files restored with SHA-256 verification, plus inventory counts and executable-mode checks. The exact `binutils-common:arm64.conffiles` path roundtripped.
- ARM64 full seed import, rules/SQL checks and cold database migration passed. Browser tests passed both native log export buttons and nested log reading after a simulated session failure with the backend unavailable. APK compilation, Android lint and the pinned-certificate check passed.
- APK: https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk — **225,637 bytes**, SHA-256 `5667d5fc697cc48f347b2952b5065c8405083c3ad0aeae652a7fffa66136a2aa`. Release asset and preview tag were read back from GitHub after publication.
- Runtime 1.1 and the pinned signing certificate remain unchanged. The user subsequently confirmed session backup/restore and input on Thor; preserve those working paths. No reset, recompile or database reimport is required for this APK update.

## Previous feature request (retained context)

Implement in `Russianranger/trasc-server-android`:

1. All database rules in searchable, collapsible categories, with typed controls and field-specific validation for established limits.
2. Setup actions to back up and replace `maps/base/nektulos.map` and `maps/nav/nektulos.nav` from their matching `maps/legacy/` paths, and revert the pair.
3. Complete session ZIP export/import, usable by a fresh app before runtime installation. Include runtime, database, binaries, maps, logs, source, settings and client files. Preserve permissions/symlinks and validate before replacement.
4. Client tab, client ZIP import with removal of only the app's temporary ZIP copy, and configurable gamepad-to-keyboard/mouse input groundwork. Client execution/Wine/dinput8 loading is a later phase, not an acceptance claim for this build.
5. Publish a test APK and keep this handoff current.

The user explicitly declined any faction/deity change. Yukovis was a disposable Iksar beastlord testing login/persistence; Prexus (209) explained Cabilis hostility. Do not change the server fork or seed for that issue.

## Starting state and verified device progress

- Main starts at `f9dfa3ee14c7f0d4655780743a4967e24911c0d2`, app 0.1.2 / runtime 1.1.
- User confirmed on-device compilation, connection and character testing on AYN Thor Max, Android 13 ARM64.
- Runtime 1.1 includes uuid-dev. APK 0.1.2 fixes offline MariaDB initialization and nonzero default ruleset IDs.
- Current server source: `Russianranger/Triptych-Triumvirate` commit `18141ae0c9a11813733f08fa77db986951b853d6`.
- Preview application ID must stay `io.github.russianranger.trasc.preview` for in-place updates.
- Preserve the signing certificate pinned in `docs/preview-signing.sha256`. GitHub Actions cache `trasc-preview-signing-v2` contains `runtime-work/signing/preview.keystore`. Never generate a replacement key if the cache is missing.
- Main's Android workflow tests ARM64 MariaDB, builds/lints APK and publishes the preview release automatically after all gates pass.

## Implementation decisions

- Read every applicable `rule_values` row and source `common/ruletypes.h`; do not infer numeric bounds merely from a name containing Min/Max. Preserve sentinel negatives and very small XP multipliers.
- Nektulos layout comes from the server README: legacy/base → base, legacy/nav → nav. Do not copy files directly into the maps root or change water files.
- Native Android session archive is needed because a new installation has no Python/runtime yet. Stop server/database cleanly; stage and verify all files before swapping app data. No source ZIP deletion outside the app's private staging copy.
- Input mappings belong only to a focused client surface. Release held inputs on focus loss/tab change, disconnection or profile change. The initial surface is an input diagnostic; no game launch is advertised.

## Previous feature implementation: 0.2.0

- Feature implementation is committed and published: `rule_catalog.py`, `managed_content.py`, expanded Engine; native `SessionArchive`, `ControllerInput`, `ControllerManager`; RuntimeManager/MainActivity integration; rules/client UI and docs.
- APK version 0.2.0 / code 4; application ID/signing unchanged. Runtime 1.1 remains compatible.
- Local Python: 29 tests passed (including detection of map edits/imports after Apply). Host JVM archive/input tests passed (the local javac launcher is missing, but `java -m jdk.compiler/com.sun.tools.javac.Main` works; script includes fallback).
- Source parsing found all 1,122 active rules, 47 categories; no definitions missed. Static JS syntax and HTML-ID checks passed.
- Expanded ARM64 integration now checks all-rule visibility, escaped values, invalid-batch atomicity, clean snapshot/shutdown and cold physical database migration. The ARM64 job in workflow 34770980656 passed these checks, including the actual full seed import and cold database restore.
- Final workflow **34771227274** passed all gates and published the APK from **bf9c8d718f177c08af6c0bd1036c9e4d249a85c8**. This includes detection of map edits/imports after Apply: revert first, preserving intervening files. No new device acceptance claimed.
- Browser tests ran successfully in CI with Playwright 1.55.0. They cover 1,105 fixture rules, collapsed/lazy categories, tiny/negative values, bounds errors, changed-only saves, controller binding save and capture release, and mobile horizontal overflow. UI screenshots are in the workflow artifact `management-ui-reports`; downloading its temporary file URL locally returned HTTP 403, so do not claim manual screenshot inspection.
- Session archive excludes incoming/exports/run; includes rootfs, database, binaries, maps, logs, server data, source, builds, backups, configuration and client. Native restore works without Python and stages/validates before swapping. Previous session stays in work-session-previous/rootfs-session-previous.
- Physical Android acceptance still pending: in-place update, rule editing, Nektulos apply/revert and complete session migration. Client execution remains unimplemented by design; controller input is a diagnostic/future injection boundary.

## Previous release: 0.2.0

Published **0.2.0**, version code **4**, on 2026-09-13.

- Code commit / preview tag: `bf9c8d718f177c08af6c0bd1036c9e4d249a85c8`. This handoff-only update follows it; no APK code changes accompany these notes.
- Final successful workflow: https://github.com/Russianranger/trasc-server-android/actions/runs/34771227274
- APK: https://github.com/Russianranger/trasc-server-android/releases/download/preview/trasc-server-android-preview.apk
- APK size: **221,721 bytes**. GitHub release asset SHA-256: `1627425c11235269cbf629fc1107ddc4d349afe2e2ccb8a1c21480c93d351a98`.
- Pinned certificate: `ff9c09cdc3e2404d1d7f72d61ce2f8651464f5e03dff340f70bd4df28c70e869`; APK verification passed in CI before publication.
- `python3 -m unittest discover -s tests -v`: **29 passed**.
- `bash scripts/check-management.sh`: passed JVM archive/input checks.
- `node tests/ui_management.cjs`: passed browser management-flow checks in CI.
- ARM64 database job: reproduced original hostname failure, imported the full pinned seed offline, then passed rule inheritance/editor validation, SQL/backup/restore and cold physical database migration.
- `gradle --no-daemon :app:assembleDebug :app:lintDebug`: passed; stable-signature verification and preview publication passed.
- Preview tag and final APK release asset were read back from GitHub to verify the published commit and checksum.

## Current device follow-up

The 0.2.1 backup/restore and input pass is confirmed by the user. Continue with the 0.3.0 client sequence at the top of this file and in `docs/client-runtime.md`. Retain `docs/device-tests.md` for server regressions if a later support bundle indicates one. Do not claim actual ROF2 device acceptance until the user reports results.
